# Roadmap

Ordered, low-churn narrative of where this repo goes next.
Detail lives in the linked briefs and plans; this file orders them.
Item 1 is the active stream (scope rule: `config/repo-state.md`).
Ideas not yet scheduled live on the backlog - see the `BACKLOG.md` mirror (regenerated by `scripts/gen-mirrors.sh .`, backend per `tracker:`).

## 1. Reviewer-prompt blacklist (idea #31)

Pulled from the backlog by explicit owner choice 2026-08-16 (#30's second half): read-only reviewer prompts must be barred from running install or state-changing scripts.
Next stage: `/loop-brainstorm` with the #31 issue body as the raw idea, then brief, `/loop-plan`, `/loop-drive`.

## 2. Packaging phase 1 close-out (owner checkpoints)

Executed and merged 2026-08-16 (run-ticket #35; record in `docs/handoffs/2026-08-16-packaging-loop-run.md`).
Remaining, all owner-fired: H2 host-2 WSL rollout (finish: remove the stale `skills/frontier-sandwich` leftover, suite to 45/45), H3 one real loop on host 2, H4 `scripts/tracker.sh close 16` on ship, then close #35.
Backlog #32 (public phase) and #33 (RIT-team phase) stay parked until H2/H3 are green, since both build on the phase-1 parameter surface.

## 3. Steady state

- Migrate sprawl repos (pokemine, vaultwise, substack-scraper) to the repo-state convention as each is next touched (idea #1).
- Revisit parked backlog ideas as they come due: see `BACKLOG.md`.
