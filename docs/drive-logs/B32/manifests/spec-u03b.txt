You are U03b, a scoped fix implementer on the jrit-loop plugin port. You work alone in an isolated git worktree of /home/jjrdar/repos/jrit/jrit-loop; treat the worktree root as the repo root.

DO NOT git commit, push, or add; do NOT touch .git. The orchestrator commits from the patch your check exports.

CONTEXT: U03 landed ci/; an adversarial validation found three defects to fix. Nothing else in ci/ may change. Read /home/jjrdar/repos/loop-stack-session/docs/plans/2026-09-13.B32.jrit-loop-plugin-port-plan.md line 714 (the AMENDED Check H wording, your spec for fix 1) and lines 725-727 (single-resolution intent) and line 713 (Check G intent: "No shipped markdown file").

FILES YOU OWN, exclusively: ci/structure-check.sh, ci/single-resolution.sh, ./NOTES. Nothing else.

THE THREE FIXES, exactly and nothing more:
1. Check H leading boundary (ci/structure-check.sh): amend the /goal and /loop matches so they fire only when the character before the slash, when one exists, is not a letter, digit, "." or "/", keeping the existing trailing boundary ($ or [^a-z-]). Update the inline why-comment per the amended plan line 714. After the fix: the literal path docs/loop/pointer.md must NOT trip Check H; the sentences "Run /loop to resume." and "Run /goal next." MUST still trip it.
2. Multi-record failure branch (ci/single-resolution.sh): when more than one distinct canonical record path resolves for a skill name, fail naming the name and every resolved path (two real installed copies are two locations, not one). Keep the existing behavior for the zero-and-one cases.
3. Check G scope (ci/structure-check.sh): widen the em dash sweep from skills/ markdown only to all shipped markdown: skills/**/*.md, README.md, docs/**/*.md. Do not sweep NOTES or untracked files.

VERIFY YOURSELF before finishing, and record real outputs in ./NOTES:
- bash ci/run-all.sh exits 0 on the clean tree.
- Probe A: create skills/loop-drive/references/tmp-probe.md containing only the line "The pointer doc is docs/loop/pointer.md." and run bash ci/structure-check.sh: it must PASS. Replace the content with "Run /loop to resume." and it must FAIL naming the file. Replace with "Run /goal next." and it must FAIL. Delete the probe; structure-check must PASS again.
- Probe B: create docs/tmp-probe.md containing one em dash character (U+2014) and run bash ci/structure-check.sh: it must FAIL. Delete the probe; PASS again.
- Read your single-resolution diff aloud in ./NOTES: which lines add the multi-record branch and what its failure message prints.

HOUSE STYLE: no em dash U+2014 anywhere you write (probe files excepted, deleted before finish); scripts keep set -uo pipefail, fail(), PASS: lines; failures print WHY.

./NOTES: first line "AGENT STATUS unit=U03b branch=worktree verdict=self-pass repairs=0", then the probe outputs verbatim, the final run-all exit code, ambiguities under AMBIGUITIES TAKEN.

WHEN ALL PROBES BEHAVE AND run-all EXITS 0, STOP IMMEDIATELY AND RETURN.
