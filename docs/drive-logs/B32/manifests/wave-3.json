{
  "run_name": "jrit-loop-port",
  "workdir": "/home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/ringer/wave-3",
  "repo": "/home/jjrdar/repos/jrit/jrit-loop",
  "worktrees": true,
  "max_parallel": 3,
  "tasks": [
    {
      "key": "u04-receipt",
      "engine": "claude-zai",
      "model": "glm-5.3",
      "task_type": "code-feature",
      "timeout_s": 3600,
      "expect_files": [
        "/home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/patches/u04.patch"
      ],
      "spec": "You are U04, an implementer on the jrit-loop plugin port. You work alone in an isolated git worktree of /home/jjrdar/repos/jrit/jrit-loop; treat the worktree root you start in as the repo root. Five sibling workers are running right now on other files; they do not touch yours.\n\nDO NOT git commit, do NOT git push, do NOT run git add yourself, and do NOT touch .git. The orchestrator commits your work on main from the patch your check exports. This overrides your task's step 13 (its step-final add-and-commit is performed by the orchestrator with that exact message); every other step is yours, including step 12's chmod +x, which is real work and not a commit.\n\nREAD FIRST, read-only, in this order:\n1. /home/jjrdar/repos/loop-stack-session/docs/plans/2026-09-13.B32.jrit-loop-plugin-port-plan.md lines 736-778 - this is YOUR task (Task 4), in full, and it is the spec of record. Do exactly what its numbered steps say (step 13 excepted, above).\n2. The same file, lines 418-432 - the Global constraints. Every one applies to you.\n3. The same file, lines 104-152 (D2 pointer docs - the `tracker:` key grammar your helper reads and the `agent:` label vocabulary it manipulates) and lines 308-346 (D8 helper budget, the four verb signatures, the `bash scripts/receipt.sh` invocation form, and the two mandatory line-savers).\n4. The same file, lines 37-65 - the Verified facts, so you do not re-derive or contradict them.\n5. The reference implementation you are porting FROM: /home/jjrdar/repos/loop-stack-session/scripts/tracker.sh, read-only. Port only the functions step 1 names and none of the ones it excludes.\n\nFILES YOU OWN. You may create or edit ONLY these paths, all relative to the worktree root:\n- skills/loop-drive/scripts/receipt.sh\n- tests/receipt-gates.sh\n- ci/reference-allowlist.txt - APPEND ONLY, one line per addition with a trailing `# why <justification>`, never a rewrite and never a deletion of any existing line. Read the file first: it already carries seeded lines, and a duplicate of an existing line is a defect. Appending is the LAST resort, taken only when a reference genuinely cannot be rewritten to resolve inside its own skill.\n- ./NOTES - your report, worktree root only; the check excludes it from the ownership audit.\nAny other file you change is a scope violation and fails this unit, even if the change is an improvement. If you believe a file outside this list must change, do not change it: write the reason into ./NOTES and carry on with what you do own. `skills/loop-drive/SKILL.md` and `skills/loop-drive/references/` are NOT yours; they belong to U09 in a later wave. You create only the `scripts/` subdirectory under `skills/loop-drive/`.\n\nWAVE COLLISION NOTE. Within this wave the units touch disjoint files by construction. `ci/reference-allowlist.txt` is the single sanctioned overlap, and the orchestrator resolves it by union-append at the gate. A collision in any other file is a scope violation.\n\nCHECK CUSTODY, AND THE AUDIT THAT IS COMING. You own `tests/receipt-gates.sh` and you are judged by it, which means you could pass this unit by writing a weak test. You will not get away with it, and you should not try. Beyond running your test, the orchestrator's check audits the test file itself by literal marker and FAILS the unit if any of these is missing or weakened:\n- the markers `Gate 0` through `Gate 6`, one per gate, in the shapes Task 4 step 9 names;\n- the exit codes 4, 5 and 7 as values actually compared against (Gate 1 asserts 7, Gate 2 asserts 4, Gate 5 asserts 5);\n- the literal string `RACE:` (Gate 2);\n- the exact gitlab skip line `skip: gitlab gates (GITLAB_TEST_PROJECT unset)`;\n- no assertion line ending in `|| true`. A `|| true` on a provisioning or cleanup line is expected and allowed - Gate 0's label block quotes loop-setup's `gh label create ... || true` verbatim, and the stale-repo sweep uses it too. A `|| true` that neuters an ASSERTION is the thing that fails you.\nA separate Opus validator then re-runs your test against a fresh scratch repo and reads it criterion by criterion against source-plan lines 762-774. A weakened, deleted, or vacuous assertion is reported as a fail.\n\nSPECIAL RULES, non-negotiable.\n- You must NOT run `gh repo delete` against anything except a repo its own run created and named `jrit-loop-scratch-<epoch>-<random>`. That is a hard rule. The stale-repo sweep in step 9's setup deletes only repos matching the `jrit-loop-scratch-*` name pattern under the `jroethel` account; nothing else is ever a delete target.\n- Your test creates real scratch repos under the `jroethel` GitHub account. Jeremy has given explicit consent for that, and the `delete_repo` scope has been verified present on the token. The name is `jrit-loop-scratch-$(date +%s)-$RANDOM`, never `$$`: a PID collides across hosts and across a reboot, and a collision means your trap deletes a repo your run did not create.\n- The helper is invoked as `bash scripts/receipt.sh ...` everywhere, per D8: in prose, in your test, and anywhere else it is named. The execute bit is a marker, never a mechanism.\n- If `bash ci/budget-check.sh` reports a helper SLOC of 200 or more, STOP. Do not trim semantics to fit, do not raise the threshold, do not delete a verb. Record the measured number in ./NOTES under a line beginning `C5 CONDITION:` and state which functions carry the weight. The escalation to human checkpoint C5 is the orchestrator's, not yours, and a truthful 214 is worth more to this run than a gamed 199.\n\nHOW TO RUN, exactly. From the worktree root:\nbash tests/receipt-gates.sh && bash ci/budget-check.sh && echo PASS\nRun it BEFORE you start any work and confirm it FAILS: `tests/receipt-gates.sh` does not exist yet, so it fails immediately, and that is the expected fail-first state. Run it again when you are done and confirm it exits 0. Record BOTH real exit codes in ./NOTES, the before and the after, as observed numbers and not as claims.\n\nSTEP 10 IS MANDATORY AND ITS EVIDENCE GOES IN ./NOTES. Run the test against the live scratch repo, confirm Gate 0 and all six gates pass, and then confirm the scratch repo is gone: `gh repo list jroethel --limit 200 | grep jrit-loop-scratch` must print nothing. Paste that command's output (or its emptiness) into ./NOTES. The check re-runs that same sweep after your test; a leaked scratch repo fails the unit.\n\n./NOTES CONTRACT. First line, exactly:\nAGENT STATUS unit=u04 branch=worktree verdict=self-pass repairs=0\nThen: what you read; every file you created or edited; the acceptance command with its before and after exit codes; the measured helper SLOC; every allow-list line you appended with its `# why`; the step-10 scratch-repo sweep output; and a section headed AMBIGUITIES TAKEN.\n\nHOUSE STYLE, non-negotiable, and CI enforces it: never the em dash character U+2014, plain \"-\" only; one full sentence per line in markdown; aligned pipe tables; scripts use `set -uo pipefail`, a `fail()` helper, and end with an explicit `PASS:` line; checks print WHY they fail, never a bare `exit 1`.\n\nIF SOMETHING IS AMBIGUOUS: you cannot ask. Record the question in ./NOTES under \"AMBIGUITIES TAKEN\", take the most conservative reading (the one that changes the least and claims the least), and continue. A conservative reading that is later corrected costs one relaunch; a confident guess that ships costs a criterion.\n\nWHEN THE ACCEPTANCE COMMAND PASSES AND ./NOTES IS WRITTEN, STOP IMMEDIATELY AND RETURN. Do not explore unrelated code, do not improve adjacent files, do not refactor anything that is not broken.\n",
      "check": "bash /home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/manifests/checks/u04.sh",
      "verified": "The receipt helper implements the four verbs under a measured 200-line budget, and tests/receipt-gates.sh still proves Gate 0 through Gate 6 with exit codes 4, 5 and 7 against a real throwaway GitHub repo it then deletes."
    },
    {
      "key": "u05-brainstorm-improve-molt",
      "engine": "claude-zai",
      "model": "glm-5.2",
      "task_type": "docs",
      "timeout_s": 2400,
      "expect_files": [
        "/home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/patches/u05.patch"
      ],
      "spec": "You are U05, an implementer on the jrit-loop plugin port. You work alone in an isolated git worktree of /home/jjrdar/repos/jrit/jrit-loop; treat the worktree root you start in as the repo root. Five sibling workers are running right now on other files; they do not touch yours.\n\nDO NOT git commit, do NOT git push, do NOT run git add yourself, and do NOT touch .git. The orchestrator commits your work on main from the patch your check exports. This overrides your task's step 21 (its step-final add-and-commit is performed by the orchestrator with that exact message); every other step is yours.\n\nREAD FIRST, read-only, in this order:\n1. /home/jjrdar/repos/loop-stack-session/docs/plans/2026-09-13.B32.jrit-loop-plugin-port-plan.md lines 780-816 - this is YOUR task (Task 5), in full, and it is the spec of record. Do exactly what its numbered steps say (step 21 excepted, above).\n2. The same file, lines 418-432 - the Global constraints. Every one applies to you.\n3. The same file, lines 104-152 (D2, for the pointer-doc key names `rubix-autorun`, `briefs-home`, `plans-home` and the `agent:` vocabulary), lines 154-168 (D3, the pre-plugin detection paragraph, verbatim and four physical lines), lines 203-234 (D5, the shared-reference table, the byte-equality pairs, and the two exact replacement sentences), lines 236-280 (D6, the stub template and the exact rubix-review filled instance).\n4. The same file, lines 37-65 - the Verified facts, so you do not re-derive or contradict them.\n5. The sources you are porting FROM, read-only: /home/jjrdar/repos/loop-stack-session/skills/loop-brainstorm/, .../loop-improve/, .../loop-molt/.\n\nFILES YOU OWN. You may create or edit ONLY these paths, all relative to the worktree root:\n- skills/loop-brainstorm/SKILL.md, skills/loop-brainstorm/references/brief-pipeline.md, skills/loop-brainstorm/references/one-minute-test.md, skills/loop-brainstorm/references/tracker-scan.md\n- skills/loop-improve/SKILL.md, skills/loop-improve/references/audit-playbook.md, skills/loop-improve/references/brief-pipeline.md, skills/loop-improve/references/tracker-scan.md\n- skills/loop-molt/SKILL.md, skills/loop-molt/references/protocol.md, skills/loop-molt/references/brief-pipeline.md\n- ci/reference-allowlist.txt - APPEND ONLY, one line per addition with a trailing `# why <justification>`, never a rewrite and never a deletion of any existing line. Read the file first: it already carries seeded lines, and a duplicate of an existing line is a defect.\n- ./NOTES - your report, worktree root only; the check excludes it from the ownership audit.\nEleven files, no twelfth. Any other file you change is a scope violation and fails this unit, even if the change is an improvement. If you believe a file outside this list must change, do not change it: write the reason into ./NOTES and carry on with what you do own.\n\nWAVE COLLISION NOTE. Within this wave the units touch disjoint files by construction. `ci/reference-allowlist.txt` is the single sanctioned overlap, and the orchestrator resolves it by union-append at the gate. A collision in any other file is a scope violation.\n\nCHECK CUSTODY. `ci/structure-check.sh` judges you and it is not yours. Do not create or edit any file under `ci/` except the append to `ci/reference-allowlist.txt` sanctioned above. A diff touching a check script is an automatic scope violation.\n\nKNOWN STRAGGLERS YOU MUST RESOLVE. A pre-run of the structure check's reference resolver against the source repo found three unresolved references. Two of them are yours:\n- `principles.md`, referenced in the source `skills/loop-brainstorm/SKILL.md` around line 31. There is no `principles.md` in this plugin and none is being created.\n- a bare `brief-pipeline.md`, referenced in the source `skills/loop-improve/SKILL.md` around line 77, with no `references/` prefix, so it does not resolve inside the skill.\nFor each, rewrite the PROSE: either to an intra-skill relative path that actually resolves (for example `references/brief-pipeline.md`), or to plan-sanctioned wording that names no file at all, in the shape D5 already uses for the loop-molt and loop-improve provenance rewrites at lines 228-234. Appending the filename to `ci/reference-allowlist.txt` is the LAST resort and needs a defensible `# why` saying why the reference cannot be made to resolve; a bare \"the check complained\" is not a justification and will be read as evasion.\nThe third straggler, `principles.md` at `skills/loop-drive/SKILL.md` line 15 in the source repo, is NOT yours: `skills/loop-drive/` belongs to U09 in a later wave. It is named here only so you do not reach outside your ownership list to fix it.\n\nHOW TO RUN, exactly. From the worktree root:\nbash ci/structure-check.sh && claude plugin validate --strict skills && echo PASS\nREAD THIS BEFORE YOU RUN IT. Against the empty destination tree, where `skills/` holds only `.gitkeep`, this command can legitimately exit 0, because there is nothing yet for it to find fault with. That is not the check being wrong and it is not a green light. Your fail-first proof is Task 5 step 1's ordering, and it is mandatory: copy the three unported skill directories in FIRST, then run the acceptance command and confirm it FAILS on the banned tokens and unresolved references the copies still carry. Record three real exit codes in ./NOTES: the run against the empty tree, the post-copy run that must fail, and the final run that must exit 0. If the post-copy run passes, stop and say so in ./NOTES; that means the check is wrong, and a wrong check is a finding, not a green light.\n\nBYTE-EQUALITY IS BY CONSTRUCTION, NOT BY HAND. Step 16's three `cp` commands are the mechanism. CI asserts brainstorm-versus-improve and brainstorm-versus-molt on `brief-pipeline.md`, and brainstorm-versus-improve on `tracker-scan.md`. Never hand-edit a duplicate: edit the canonical copy under `skills/loop-brainstorm/references/` and re-run the `cp`. Do the copies LAST, after every edit to the canonical files, or you will ship a stale duplicate.\n\nTWO THINGS THE CHECK ASSERTS THAT ARE EASY TO GET BACKWARDS.\n- The D3 pre-plugin detection paragraph goes verbatim into `loop-brainstorm/SKILL.md` and `loop-improve/SKILL.md`, and must NOT appear in `loop-molt/SKILL.md` (step 15). loop-molt audits prose artifacts, so a pointer doc is not a precondition for it.\n- The D6 rubix-review stub goes into `loop-brainstorm/SKILL.md` with its four bold markers intact and verbatim from lines 273-280.\n\n./NOTES CONTRACT. First line, exactly:\nAGENT STATUS unit=u05 branch=worktree verdict=self-pass repairs=0\nThen: what you read; every file you created or edited; the acceptance command with all three real exit codes; how you resolved each of the two stragglers, quoting the before and after wording; every allow-list line you appended with its `# why`; and a section headed AMBIGUITIES TAKEN.\n\nHOUSE STYLE, non-negotiable, and CI enforces it: never the em dash character U+2014, plain \"-\" only; one full sentence per line in markdown; aligned pipe tables.\n\nIF SOMETHING IS AMBIGUOUS: you cannot ask. Record the question in ./NOTES under \"AMBIGUITIES TAKEN\", take the most conservative reading (the one that changes the least and claims the least), and continue. A conservative reading that is later corrected costs one relaunch; a confident guess that ships costs a criterion.\n\nWHEN THE ACCEPTANCE COMMAND PASSES AND ./NOTES IS WRITTEN, STOP IMMEDIATELY AND RETURN. Do not explore unrelated code, do not improve adjacent files, do not refactor anything that is not broken.\n",
      "check": "bash /home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/manifests/checks/u05.sh",
      "verified": "All eleven loop-brainstorm, loop-improve and loop-molt files are ported with byte-identical shared references, the two known unresolved references are fixed, and the structure check plus strict plugin validation both pass."
    },
    {
      "key": "u06-review-plan",
      "engine": "claude-zai",
      "model": "glm-5.2",
      "task_type": "docs",
      "timeout_s": 2400,
      "expect_files": [
        "/home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/patches/u06.patch"
      ],
      "spec": "You are U06, an implementer on the jrit-loop plugin port. You work alone in an isolated git worktree of /home/jjrdar/repos/jrit/jrit-loop; treat the worktree root you start in as the repo root. Five sibling workers are running right now on other files; they do not touch yours.\n\nDO NOT git commit, do NOT git push, do NOT run git add yourself, and do NOT touch .git. The orchestrator commits your work on main from the patch your check exports. This overrides your task's step 13 (its step-final add-and-commit is performed by the orchestrator with that exact message); every other step is yours.\n\nREAD FIRST, read-only, in this order:\n1. /home/jjrdar/repos/loop-stack-session/docs/plans/2026-09-13.B32.jrit-loop-plugin-port-plan.md lines 818-850 - this is YOUR task (Task 6), in full, and it is the spec of record. Do exactly what its numbered steps say (step 13 excepted, above).\n2. The same file, lines 418-432 - the Global constraints. Every one applies to you.\n3. The same file, lines 104-152 (D2, for the pointer-doc keys `rubix-autorun` and `plans-home`), lines 154-168 (D3, the pre-plugin detection paragraph, verbatim and four physical lines), lines 203-234 (D5, the reviewer-contract fail-closed replacement at lines 222-226 and the house-style sentence at lines 232-234, both used verbatim), lines 236-280 (D6, the stub template and the exact rubix-review filled instance).\n4. The same file, lines 37-65 - the Verified facts, so you do not re-derive or contradict them.\n5. The sources you are porting FROM, read-only: /home/jjrdar/repos/loop-stack-session/skills/loop-review/SKILL.md, .../skills/loop-plan/SKILL.md, and /home/jjrdar/repos/loop-stack-session/config/reviewer-conduct-contract.md.\n\nFILES YOU OWN. You may create or edit ONLY these paths, all relative to the worktree root:\n- skills/loop-review/SKILL.md, skills/loop-review/references/reviewer-conduct-contract.md\n- skills/loop-plan/SKILL.md\n- ci/reference-allowlist.txt - APPEND ONLY, one line per addition with a trailing `# why <justification>`, never a rewrite and never a deletion of any existing line. Read the file first: it already carries seeded lines, and a duplicate of an existing line is a defect. Appending is the LAST resort, taken only when a reference genuinely cannot be rewritten to resolve inside its own skill.\n- ./NOTES - your report, worktree root only; the check excludes it from the ownership audit.\nThree shipped files, no fourth. Any other file you change is a scope violation and fails this unit, even if the change is an improvement. If you believe a file outside this list must change, do not change it: write the reason into ./NOTES and carry on with what you do own.\n\nWAVE COLLISION NOTE. Within this wave the units touch disjoint files by construction. `ci/reference-allowlist.txt` is the single sanctioned overlap, and the orchestrator resolves it by union-append at the gate. A collision in any other file is a scope violation.\n\nCHECK CUSTODY. `ci/structure-check.sh` judges you and it is not yours. Do not create or edit any file under `ci/` except the append to `ci/reference-allowlist.txt` sanctioned above. A diff touching a check script is an automatic scope violation.\n\nTHE REVIEWER CONTRACT IS A COPY, NOT A REWRITE. Step 2's `cp` is the mechanism, and the check verifies the shipped file is byte-identical to the source at /home/jjrdar/repos/loop-stack-session/config/reviewer-conduct-contract.md. Leave its six lines of policy intact, including the barred-installer examples that name `install.sh`, `setup.sh`, `~/.claude` and `~/.agents`. That is legitimate prose and it passes both structure checks by design: Check A's banned tokens are the `/skills/`-suffixed forms, and Check B skips `install.sh` and `setup.sh`. Do not \"fix\" it to please a check. This file is the canonical original that U09 later copies byte-for-byte into loop-drive, so a single edited character here breaks a byte-equality pair in a later wave.\n\nTWO THINGS THE CHECK ASSERTS THAT ARE EASY TO GET BACKWARDS.\n- `loop-plan/SKILL.md` GETS the D3 pre-plugin detection paragraph, verbatim, near the top and immediately after the first heading. `loop-review/SKILL.md` does NOT get it (step 5), because loop-review's stated contract is that it works in any repo with zero setup and a pointer-doc precondition would break that.\n- The D5 fail-closed replacement at lines 84-86 of `loop-review/SKILL.md` is pasted verbatim from plan lines 222-226, all three lines, including the sentence that names the jrit-loop plugin install as the thing to reinstall.\n\nHOW TO RUN, exactly. From the worktree root:\nbash ci/structure-check.sh && claude plugin validate --strict skills && echo PASS\nREAD THIS BEFORE YOU RUN IT. Against the empty destination tree, where `skills/` holds only `.gitkeep`, this command can legitimately exit 0, because there is nothing yet for it to find fault with. That is not the check being wrong and it is not a green light. Your fail-first proof is Task 6 step 1's ordering, and it is mandatory: copy the two unported SKILL.md files in FIRST, then run the acceptance command and confirm it FAILS on the banned tokens they still carry. Record three real exit codes in ./NOTES: the run against the empty tree, the post-copy run that must fail, and the final run that must exit 0. If the post-copy run passes, stop and say so in ./NOTES; that means the check is wrong, and a wrong check is a finding, not a green light.\n\n./NOTES CONTRACT. First line, exactly:\nAGENT STATUS unit=u06 branch=worktree verdict=self-pass repairs=0\nThen: what you read; every file you created or edited; the acceptance command with all three real exit codes; the `cmp` or `git hash-object` evidence that the shipped reviewer contract matches its source byte for byte; every allow-list line you appended with its `# why`; and a section headed AMBIGUITIES TAKEN.\n\nHOUSE STYLE, non-negotiable, and CI enforces it: never the em dash character U+2014, plain \"-\" only; one full sentence per line in markdown; aligned pipe tables.\n\nIF SOMETHING IS AMBIGUOUS: you cannot ask. Record the question in ./NOTES under \"AMBIGUITIES TAKEN\", take the most conservative reading (the one that changes the least and claims the least), and continue. A conservative reading that is later corrected costs one relaunch; a confident guess that ships costs a criterion.\n\nWHEN THE ACCEPTANCE COMMAND PASSES AND ./NOTES IS WRITTEN, STOP IMMEDIATELY AND RETURN. Do not explore unrelated code, do not improve adjacent files, do not refactor anything that is not broken.\n",
      "check": "bash /home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/manifests/checks/u06.sh",
      "verified": "loop-review ships the reviewer-conduct contract as a byte-identical real file with the fail-closed replacement wording, loop-plan carries the pre-plugin paragraph and the rubix stub, and both pass the structure check and strict plugin validation."
    },
    {
      "key": "u07-track-auto-handoff-wayfinder",
      "engine": "claude-zai",
      "model": "glm-5.2",
      "task_type": "docs",
      "timeout_s": 2400,
      "expect_files": [
        "/home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/patches/u07.patch"
      ],
      "spec": "You are U07, an implementer on the jrit-loop plugin port. You work alone in an isolated git worktree of /home/jjrdar/repos/jrit/jrit-loop; treat the worktree root you start in as the repo root. Five sibling workers are running right now on other files; they do not touch yours.\n\nDO NOT git commit, do NOT git push, do NOT run git add yourself, and do NOT touch .git. The orchestrator commits your work on main from the patch your check exports. This overrides your task's step 22 (its step-final add-and-commit is performed by the orchestrator with that exact message); every other step is yours. Step 15 tells you to record the two `--help` findings in the commit message: since you do not commit, write them into ./NOTES under a line beginning `GH/GLAB CHILD-ISSUE FINDINGS:`, verbatim enough to paste, and the orchestrator carries them into the commit subject.\n\nREAD FIRST, read-only, in this order:\n1. /home/jjrdar/repos/loop-stack-session/docs/plans/2026-09-13.B32.jrit-loop-plugin-port-plan.md lines 852-894 - this is YOUR task (Task 7), in full, and it is the spec of record. Do exactly what its numbered steps say (step 22 excepted, above).\n2. The same file, lines 418-432 - the Global constraints. Every one applies to you.\n3. The same file, lines 104-152 (D2, for the pointer-doc keys `tracker`, `autonomy-default`, `handoffs-home`, `reviews-home` and the `agent:` vocabulary), lines 154-168 (D3, the pre-plugin detection paragraph, verbatim and four physical lines), lines 282-306 (D7, the frontmatter table and the reasons the other recognized keys were rejected).\n4. The same file, lines 37-65 - the Verified facts, so you do not re-derive or contradict them.\n5. The sources you are porting FROM, read-only: /home/jjrdar/repos/loop-stack-session/skills/{loop-track,loop-auto,handoff,wayfinder}/.\n\nFILES YOU OWN. You may create or edit ONLY these paths, all relative to the worktree root:\n- skills/loop-track/SKILL.md\n- skills/loop-auto/SKILL.md\n- skills/handoff/SKILL.md, skills/handoff/agents/openai.yaml\n- skills/wayfinder/SKILL.md\n- ci/reference-allowlist.txt - APPEND ONLY, one line per addition with a trailing `# why <justification>`, never a rewrite and never a deletion of any existing line. Read the file first: it already carries seeded lines, and a duplicate of an existing line is a defect. Appending is the LAST resort, taken only when a reference genuinely cannot be rewritten to resolve inside its own skill.\n- ./NOTES - your report, worktree root only; the check excludes it from the ownership audit.\nFive shipped files, no sixth. Step 1 also DELETES `skills/loop-track/loop-track.sh` and `skills/loop-auto/loop-auto.sh` if your copy brings them in; deleting those two is required and is not a scope violation, and the final tree must show no trace of them. Any other file you change is a scope violation and fails this unit, even if the change is an improvement. If you believe a file outside this list must change, do not change it: write the reason into ./NOTES and carry on with what you do own.\n\nWAVE COLLISION NOTE. Within this wave the units touch disjoint files by construction. `ci/reference-allowlist.txt` is the single sanctioned overlap, and the orchestrator resolves it by union-append at the gate. A collision in any other file is a scope violation.\n\nCHECK CUSTODY. `ci/structure-check.sh` and `ci/budget-check.sh` judge you and neither is yours. Do not create or edit any file under `ci/` except the append to `ci/reference-allowlist.txt` sanctioned above. A diff touching a check script is an automatic scope violation.\n\nA SATISFIABILITY NOTE, SO YOU DO NOT OVERREACH. Your acceptance command runs `ci/budget-check.sh`, which measures `skills/loop-drive/scripts/receipt.sh`. That file is U04's, it is being written in parallel right now, and it does not exist in your worktree. budget-check prints a `skip:` line for a file that does not yet exist and does not fail on it. Do NOT create `skills/loop-drive/scripts/receipt.sh` to make a skip go away; creating it is a scope violation that fails this unit. What budget-check does need from you is `skills/handoff/agents/openai.yaml`, which you ship, and whose SLOC it prints as information only with no threshold attached.\n\nHOW TO RUN, exactly. From the worktree root:\nbash ci/structure-check.sh && bash ci/budget-check.sh && claude plugin validate --strict skills && echo PASS\nREAD THIS BEFORE YOU RUN IT. Against the empty destination tree, where `skills/` holds only `.gitkeep`, this command can legitimately exit 0, because there is nothing yet for it to find fault with. That is not the check being wrong and it is not a green light. Your fail-first proof is Task 7 step 1's ordering, and it is mandatory: copy the four unported skill directories in FIRST, then run the acceptance command and confirm it FAILS. Record three real exit codes in ./NOTES: the run against the empty tree, the post-copy run that must fail, and the final run that must exit 0. If the post-copy run passes, stop and say so in ./NOTES; that means the check is wrong, and a wrong check is a finding, not a green light.\n\nTHINGS THE CHECK ASSERTS THAT ARE EASY TO GET WRONG.\n- All four SKILL.md files carry the D3 pre-plugin detection paragraph, verbatim, four physical lines, near the top after the first heading. loop-track, loop-auto, handoff and wayfinder are four of D3's eight.\n- `loop-auto`'s frontmatter `description` is replaced wholesale with the exact block at plan lines 876-878. The shipped description names `docs/chain-state.md`, which is a banned token, and frontmatter lives inside the file Check A scans, so leaving it in place fails your acceptance check with an error that looks like a body problem. After your rewrite, `docs/chain-state.md` must not appear anywhere in `loop-auto/SKILL.md`, body or frontmatter.\n- Frontmatter keys, exactly, per D7: `handoff` carries `name`, `description`, `argument-hint`, `disable-model-invocation`; `wayfinder` carries `name`, `description`, `disable-model-invocation`; `loop-track` and `loop-auto` carry `name` and `description` and nothing else. Each non-default key also gets one sentence of prose in the body stating its behaviour.\n- Zero executables: `find skills/loop-track skills/loop-auto skills/handoff skills/wayfinder -type f -perm -u+x` must print nothing.\n- wayfinder's child-issue prose (step 15) must claim only what `gh --help` and `glab --help` on this host actually support, and where a CLI has no native sub-issue support the prose must name the `Blocked by: #N` body convention as the mechanism for that backend. Run both `--help` commands for real; do not recall them from training.\n\n./NOTES CONTRACT. First line, exactly:\nAGENT STATUS unit=u07 branch=worktree verdict=self-pass repairs=0\nThen: what you read; every file you created, edited or deleted; the acceptance command with all three real exit codes; the `GH/GLAB CHILD-ISSUE FINDINGS:` line with the two `--help` observations you actually made; every allow-list line you appended with its `# why`; and a section headed AMBIGUITIES TAKEN.\n\nHOUSE STYLE, non-negotiable, and CI enforces it: never the em dash character U+2014, plain \"-\" only; one full sentence per line in markdown; aligned pipe tables.\n\nIF SOMETHING IS AMBIGUOUS: you cannot ask. Record the question in ./NOTES under \"AMBIGUITIES TAKEN\", take the most conservative reading (the one that changes the least and claims the least), and continue. A conservative reading that is later corrected costs one relaunch; a confident guess that ships costs a criterion.\n\nWHEN THE ACCEPTANCE COMMAND PASSES AND ./NOTES IS WRITTEN, STOP IMMEDIATELY AND RETURN. Do not explore unrelated code, do not improve adjacent files, do not refactor anything that is not broken.\n",
      "check": "bash /home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/manifests/checks/u07.sh",
      "verified": "The two retired shell scripts are gone, loop-auto no longer names the chain-state file in body or frontmatter, wayfinder claims only the child-issue support gh and glab actually have, and all four skills carry the frontmatter D7 specifies with no executables."
    },
    {
      "key": "u08-loop-setup",
      "engine": "claude-zai",
      "model": "glm-5.2",
      "task_type": "docs",
      "timeout_s": 3600,
      "expect_files": [
        "/home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/patches/u08.patch"
      ],
      "spec": "You are U08, an implementer on the jrit-loop plugin port. You work alone in an isolated git worktree of /home/jjrdar/repos/jrit/jrit-loop; treat the worktree root you start in as the repo root. Five sibling workers are running right now on other files; they do not touch yours.\n\nDO NOT git commit, do NOT git push, do NOT run git add yourself, and do NOT touch .git. The orchestrator commits your work on main from the patch your check exports. This overrides your task's step 19 (its step-final add-and-commit is performed by the orchestrator with that exact message); every other step is yours.\n\nREAD FIRST, read-only, in this order:\n1. /home/jjrdar/repos/loop-stack-session/docs/plans/2026-09-13.B32.jrit-loop-plugin-port-plan.md lines 896-937 - this is YOUR task (Task 8), in full, and it is the spec of record. Do exactly what its numbered steps say (step 19 excepted, above).\n2. The same file, lines 418-432 - the Global constraints. Every one applies to you.\n3. The same file, lines 104-152 (D2, the `docs/loop/pointer.md` template you reproduce verbatim and the description of what `docs/loop/conventions.md` must carry), lines 154-168 (D3, the pre-plugin detection paragraph - you do NOT carry it, see below), lines 170-201 (D4, the local-issues grammar you reproduce verbatim and then prove with your own test).\n4. The same file, lines 37-65 - the Verified facts, especially the 2026-09-13 fact that Claude Code does not read `AGENTS.md` natively and the documented pattern is a `CLAUDE.md` that imports it.\n5. The sources you are porting FROM, read-only: /home/jjrdar/repos/loop-stack-session/skills/loop-setup/SKILL.md and .../skills/loop-setup/references/import-triage.md. Do NOT copy `setup.sh`; it is 556 lines and it dies with this port.\n\nFILES YOU OWN. You may create or edit ONLY these paths, all relative to the worktree root:\n- skills/loop-setup/SKILL.md\n- skills/loop-setup/references/import-triage.md\n- skills/loop-setup/references/pointer-templates.md\n- tests/fixtures/pre-plugin-repo/config/repo-state.md\n- tests/fixtures/pre-plugin-repo/README.md\n- tests/local-mode-grammar.sh\n- ci/reference-allowlist.txt - APPEND ONLY, one line per addition with a trailing `# why <justification>`, never a rewrite and never a deletion of any existing line. Read the file first: it already carries seeded lines, and a duplicate of an existing line is a defect. Appending is the LAST resort, taken only when a reference genuinely cannot be rewritten to resolve inside its own skill.\n- ./NOTES - your report, worktree root only; the check excludes it from the ownership audit.\nSix shipped files, no seventh. In particular `tests/fixtures/pre-plugin-repo/docs/loop/pointer.md` must NEVER exist: the fixture's whole point is a repo that has not been migrated, and your own acceptance command asserts its absence. Any other file you change is a scope violation and fails this unit, even if the change is an improvement. If you believe a file outside this list must change, do not change it: write the reason into ./NOTES and carry on with what you do own.\n\nWAVE COLLISION NOTE. Within this wave the units touch disjoint files by construction. `ci/reference-allowlist.txt` is the single sanctioned overlap, and the orchestrator resolves it by union-append at the gate. A collision in any other file is a scope violation.\n\nCHECK CUSTODY, AND THE AUDIT THAT IS COMING. You own `tests/local-mode-grammar.sh` and your acceptance command runs it, which means you could pass this unit by writing a test that asserts nothing. You will not get away with it, and you should not try. Beyond running your test, the orchestrator's check audits the test file itself and FAILS the unit if it is vacuous:\n- it must round-trip, not just render: create a `docs/issues.md` per the D4 grammar inside a `mktemp -d`, simulate a create by appending a new `## #<n>` section and bumping the `<!-- next-number: N -->` marker in the same write, simulate a receipt by appending one `> receipt <UTC ISO-8601>: <text>` line to a body, then PARSE THE RESULT BACK and assert all four grammar rules - section numbers ascend, `state:` and `labels:` are the first two lines after each heading in that order, `state:` is `open` or `closed`, and the marker equals the highest section number plus one;\n- it must be a pure-file test: no network, no tracker, no `gh`, no `glab`, no `curl`. The check asserts their absence;\n- it must carry at least four distinct failure paths, one per grammar rule, each printing WHY it failed;\n- no assertion line may end in `|| true`;\n- it prints `PASS: local-mode grammar round-trip`.\n`ci/structure-check.sh` also judges you and it is NOT yours; do not touch anything under `ci/` except the sanctioned allow-list append. A separate Opus validator then reads your test criterion by criterion against source-plan line 930 and reports any weakened or vacuous assertion as a fail.\n\nHOW TO RUN, exactly. From the worktree root, the compound command at plan line 913:\nbash ci/structure-check.sh && claude plugin validate --strict skills && bash tests/local-mode-grammar.sh && bash -c 'grep -q \"jrit-loop:begin\" skills/loop-setup/SKILL.md && grep -q \"@AGENTS.md\" skills/loop-setup/SKILL.md && grep -q \"gh label create\" skills/loop-setup/SKILL.md && grep -q \"next-number\" skills/loop-setup/references/pointer-templates.md && test -f tests/fixtures/pre-plugin-repo/config/repo-state.md && test ! -f tests/fixtures/pre-plugin-repo/docs/loop/pointer.md && test -z \"$(find skills/loop-setup -type f -perm -u+x)\"' && echo PASS\nRun it BEFORE you start any work and confirm it FAILS: `tests/local-mode-grammar.sh` does not exist yet, so it fails there, and that is the expected fail-first state. Run it again when you are done and confirm it exits 0. Record BOTH real exit codes in ./NOTES, the before and the after, as observed numbers and not as claims.\n\nTHINGS THE CHECK ASSERTS THAT ARE EASY TO GET WRONG.\n- The label-provisioning command block is given in full and in ONE place, because `tests/receipt-gates.sh` Gate 0 quotes it verbatim to simulate this skill. All seven labels: `idea`, `agent:todo`, `agent:working`, `agent:needs-input`, `agent:review`, `agent:done`, `wayfinder:map`. The `|| true` on each `gh label create` is required, because an already-existing label is a success condition here.\n- `loop-setup` does NOT carry the D3 pre-plugin detection paragraph (step 15), because it owns the migration itself.\n- The `## Migrating a pre-plugin repo` section NAMES `docs/chain-state.md`, `ISSUES.md`, `BACKLOG.md` and `WAYFINDER.md` in order to delete them. That is sanctioned: Check A's `docs/chain-state.md` ban excludes this one file for this one reason, and the allow-list already carries the three mirror filenames. Do not soften the prose to dodge a check that already exempts you, and do not re-append those filenames to the allow-list.\n- The deletion prose is preview-then-assent and never silent: the skill lists the mirror files it actually found with a count, states plainly that they are untracked and therefore unrecoverable once removed, and deletes only after explicit assent.\n- The frontmatter carries only `name` and `description`, and the new `description` must no longer mention `config/repo-state.md`, `config/conventions.md`, or a finalize step; it names the two pointer docs and the managed instructions section.\n- `find skills/loop-setup -type f -perm -u+x` prints nothing. This skill creates no executable files and no mirror files, and says so.\n\n./NOTES CONTRACT. First line, exactly:\nAGENT STATUS unit=u08 branch=worktree verdict=self-pass repairs=0\nThen: what you read; every file you created or edited; the acceptance command with its before and after exit codes; the output of `bash tests/local-mode-grammar.sh` run on its own; every allow-list line you appended with its `# why`; and a section headed AMBIGUITIES TAKEN.\n\nHOUSE STYLE, non-negotiable, and CI enforces it: never the em dash character U+2014, plain \"-\" only; one full sentence per line in markdown; aligned pipe tables; scripts use `set -uo pipefail`, a `fail()` helper, and end with an explicit `PASS:` line; checks print WHY they fail, never a bare `exit 1`.\n\nIF SOMETHING IS AMBIGUOUS: you cannot ask. Record the question in ./NOTES under \"AMBIGUITIES TAKEN\", take the most conservative reading (the one that changes the least and claims the least), and continue. A conservative reading that is later corrected costs one relaunch; a confident guess that ships costs a criterion.\n\nWHEN THE ACCEPTANCE COMMAND PASSES AND ./NOTES IS WRITTEN, STOP IMMEDIATELY AND RETURN. Do not explore unrelated code, do not improve adjacent files, do not refactor anything that is not broken.\n",
      "check": "bash /home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/manifests/checks/u08.sh",
      "verified": "loop-setup now writes the two pointer docs, the AGENTS.md managed section, the CLAUDE.md import and the seven-label set itself with zero executables, and tests/local-mode-grammar.sh proves the local grammar round-trips with no network."
    },
    {
      "key": "u11a-sweep-staging",
      "engine": "claude-zai",
      "model": "glm-5.2",
      "task_type": "docs",
      "timeout_s": 2400,
      "expect_files": [
        "/home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/patches/u11a.patch"
      ],
      "spec": "You are U11a, an implementer on the jrit-loop plugin port. You are the STAGING half of Task 11, and you are the one unit in this run that must change nothing at all in the outside world.\n\nREAD THIS FIRST AND TAKE IT LITERALLY. You execute ZERO tracker mutations. Running a single `gh issue transfer`, `gh issue close`, `gh issue edit`, `gh issue comment`, `gh label create`, or `gh label delete` against any repository is an IMMEDIATE FAIL of this unit, even if the command would have been correct, even if you would have run it later anyway, and even if it succeeds. Every `gh` call you make is a READ: `gh issue list`, `gh issue view`, `gh label list`, `gh --help`. Transferring sixteen issues across repos has no batch undo, which is exactly why the firing is human checkpoint C8 and not yours. Your product is the command list Jeremy reads before he fires it.\n\nYou work in an isolated git worktree of /home/jjrdar/repos/jrit/jrit-loop; treat the worktree root you start in as the repo root. Five sibling workers are running right now on other files; they do not touch yours.\n\nDO NOT git commit, do NOT git push, do NOT run git add yourself, and do NOT touch .git, in this worktree or in any other repo on this host. The orchestrator commits your work from the patch your check exports. Task 11 step 14's step-final add-and-commit belongs to U11b and is outside your range entirely; so are steps 4 through 13.\n\nREAD FIRST, read-only, in this order:\n1. /home/jjrdar/repos/loop-stack-session/docs/plans/2026-09-13.B32.jrit-loop-plugin-port-plan.md lines 1025-1056 - this is Task 11 in full. YOUR SCOPE IS STEPS 1, 2 AND 3 ONLY. Read steps 4 through 14 anyway, because the command list you stage is exactly what those steps would run, in their order, in full.\n2. The same file, lines 400-414 - D12, the disposition table for all twenty-four open issues, plus the #3 migration note and the #44 gate.\n3. The same file, lines 418-432 - the Global constraints. Every one applies to you.\n4. The same file, lines 37-65 - the Verified facts, including the fact that `loop-stack-session` had exactly 24 open issues on 2026-09-13 and that D12 disposes of all 24 with no remainder.\n\nWHAT YOU PRODUCE. Exactly three artifacts, and nothing else.\n1. /home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/u11a-lss-open-before.json - the live starting snapshot. Run Task 11 step 1's command verbatim, `gh issue list -R jroethel/loop-stack-session --state open --limit 200 --json number,title,labels > /tmp/lss-open-before.json`, then copy it to that drive-log path, because nothing load-bearing may live only in /tmp. Confirm it holds exactly 24 issues. If the count differs, DO NOT proceed on the plan's numbers: re-derive the dispositions against the live list, write both the plan's list and the live list into ./NOTES with the differences named, and flag it at the top of ./NOTES as the finding it is.\n2. /home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/u11a-sweep-commands.md - the C8 packet. This is the deliverable Jeremy reads. It carries, in this order: the live 24-issue snapshot rendered as a readable table (number, title, labels); the D12 disposition table beside it, so twenty-four dispositions can be compared against twenty-four live issues in one read; a line stating whether the two agree with no remainder; and then THE ORDERED COMMAND LIST, in execution order and in full, as a fenced bash block that can be run verbatim with no editing. The command list contains, in the order Task 11 gives them: the seven `gh label create ... -R jroethel/jrit-loop` lines from step 4; the single dry-run `gh issue transfer 7 -R jroethel/loop-stack-session jroethel/jrit-loop` plus its verification command from step 5; the fifteen-issue transfer loop from step 5, verbatim including its `-R jroethel/loop-stack-session` qualifier; the step 6 comment carrying #3's migration note verbatim; the three closes from steps 7 and 8 with their exact comment text and `--reason \"not planned\"`; the comments from steps 9, 10 and 11 on the five issues that stay open; and the step 12 mirror-file deletion in the `git rm --cached ISSUES.md BACKLOG.md WAYFINDER.md` form. Under the block, state the expected end state in one line: five issues open in loop-stack-session, sixteen in jrit-loop. Every transfer command MUST carry `-R jroethel/loop-stack-session`; it is load-bearing, because without it `gh` resolves the source repo from the working directory and an unqualified transfer run from the wrong directory targets the wrong repo.\n3. docs/issue-migration-map.md, in your worktree, and no other file in that working tree. Write it as the mapping skeleton U11b fills in at transfer time: an aligned pipe table with one row per migrated issue, columns old number, old title, new URL, with the new-URL cell reading `pending: filled at transfer time` for every row. Take the sixteen old numbers and titles from your live snapshot, not from memory. Say in one sentence above the table what the file is for: sixteen renumbered issues with no record is sixteen dead cross-references in every brief, plan and commit message that ever cited them.\n\nAMBIGUITY THE ORCHESTRATOR ALREADY RESOLVED FOR YOU, so you do not have to take it. Task 11 step 3 says to stage the command list but does not name a file for it, while the plan's ownership table says this task creates `docs/issue-migration-map.md` in jrit-loop and NO OTHER FILE in that working tree. Those two constraints together put the command list outside the worktree, which is why artifact 2 lives in the drive-log directory. Do not write the command list into the worktree, and do not invent a second jrit-loop file for it.\n\nFILES YOU OWN.\n- In the worktree: docs/issue-migration-map.md, and ./NOTES (your report, worktree root only; the check excludes it from the ownership audit). Nothing else in the worktree, at all.\n- Outside the worktree, write-allowed: the two drive-log artifacts named above, and /tmp/lss-open-before.json.\nAny other file you change is a scope violation and fails this unit. You have no business in `skills/`, `ci/`, or `tests/`, and you do not append to `ci/reference-allowlist.txt`.\n\nHOW TO RUN, exactly, AND WHY IT WILL STILL FAIL WHEN YOU ARE DONE. Task 11's acceptance check is U11b's, at plan line 1038. From the worktree root:\nbash -c 'set -e; open=$(gh issue list -R jroethel/loop-stack-session --state open --limit 200 --json number -q \"length\"); test \"$open\" -eq 5 || { echo \"expected 5 open in loop-stack-session, found $open\"; exit 1; }; for n in 39 47 32; do s=$(gh issue view $n -R jroethel/loop-stack-session --json state -q .state); test \"$s\" = CLOSED || { echo \"#$n is $s\"; exit 1; }; done; mig=$(gh issue list -R jroethel/jrit-loop --state all --limit 200 --json number -q \"length\"); test \"$mig\" -ge 16 || { echo \"only $mig in jrit-loop\"; exit 1; }; echo PASS'\nRun it ONCE, at the start, per Task 11 step 2, and record its real exit code and its message in ./NOTES. It will report 24 open rather than 5. That is correct. Unlike every other unit in this wave, your acceptance command MUST STILL FAIL when you finish, because passing it requires the twenty-four mutations you are forbidden to make. Do not run it again at the end hoping for green, and never make it pass. Your unit is judged on the three artifacts above and on the proof that the tracker is untouched: your check re-counts the open issues and re-reads the state of #39, #47 and #32, and a count that is no longer 24, or any of those three no longer OPEN, fails this unit outright.\n\n./NOTES CONTRACT. First line, exactly:\nAGENT STATUS unit=u11a branch=worktree verdict=self-pass repairs=0\nThen: what you read; every file you created, with its absolute path; the live open-issue count you observed and whether it matched 24; the acceptance command's real exit code and message from its one run; the exact list of `gh` commands you executed, which must be reads only; a line reading `MUTATIONS EXECUTED: 0`; and a section headed AMBIGUITIES TAKEN.\n\nHOUSE STYLE, non-negotiable: never the em dash character U+2014, plain \"-\" only; one full sentence per line in markdown; aligned pipe tables.\n\nIF SOMETHING IS AMBIGUOUS: you cannot ask. Record the question in ./NOTES under \"AMBIGUITIES TAKEN\", take the most conservative reading (the one that changes the least and claims the least), and continue. For this unit the most conservative reading is always the one that mutates nothing.\n\nWHEN THE THREE ARTIFACTS ARE WRITTEN AND ./NOTES IS WRITTEN, STOP IMMEDIATELY AND RETURN. Do not explore unrelated code, do not improve adjacent files, do not \"just check\" whether a transfer would work.\n",
      "check": "bash /home/jjrdar/repos/loop-stack-session/docs/drive-logs/B32/manifests/checks/u11a.sh",
      "verified": "The complete ordered sweep command list is staged for checkpoint C8 with all sixteen transfers and exactly three closes, the migration-map skeleton is written, and both live trackers are provably untouched."
    }
  ]
}
