# Handoff: after seam 1 - propagation beyond this Mac and the parked advisories (#52)

Date: 2026-09-08.
State when written: seam-1 waves 1-3 landed and validated on `i52-seam1-recording-convention`;
wave 4 (template roll v7 + this dogfood) in flight; wave 5 (propagation + live board) next.

## Next actions

- Roll template-version 7 on RIT-UADV2223's conforming clones (the WSL host), the same piloted way
  config v4 rolled there; class h stays dormant on any repo until its `lifecycle-lint-since:` is set.
- Work the parked board-side advisories from the 2026-09-07 handoff: `board.sh`'s runtime spike-doc
  grep to a config key, `_health.md` omitting zero-card conforming repos, `$HOME`-rooted resume `cd`
  paths, the reading-mode empty render.
- Close-out candidates recorded at the gates: the `is_date` guard in take-stock's
  authoritative-handoff gather; rename or caption the board resume block's `Entry:` / `Next:` lines
  (Jeremy's criterion-7 note); sys-prompts-cc's four pre-existing class-a plan-set findings;
  `handoff-log.sh state` deriving `consumed-unarchived` by not-in-archive exclusion instead of
  by in-docs/handoffs location (loop-review Spec finding 5, one-line tightening).
  (The 24h died-mid-work threshold stays - Jeremy called it signal, informed that it fires at 25h.)
- Decide #52's disposition once the board shows all eight lanes live (close, or re-scope what
  remains to new issues).

## Notes

- The recording convention is live in this repo and sys-prompts-cc; `scripts/take-stock.sh <root>`
  is the resume tool of record, `conventions.md` Session records is the doctrine.

## Transitions

- 2026-09-08T19:07:42Z action 2 executed - all four board advisories closed: CSS config key, zero-card health line, non-$HOME key resolution, reading-mode caption
