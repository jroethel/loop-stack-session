{
  "run_name": "i52-seam1-recording-convention",
  "repo": "/Users/jjrdar/create/loops/loop-stack-session",
  "workdir": "/Users/jjrdar/.ringer/work/i52-seam1-recording-convention",
  "worktrees": true,
  "max_parallel": 1,
  "tasks": [
    {
      "key": "t9-template-v7",
      "engine": "claude-zai",
      "model": "glm-5.2",
      "task_type": "code-feature",
      "timeout_s": 3600,
      "spec": "You are a code-feature worker (test-first) implementing ONE unit of the loop-stack seam 1 recording convention.\nYour current working directory IS a git worktree of the loop-stack-session repo. Edit files here directly.\nDo NOT push. Do NOT run git commit at any point: leave every change uncommitted in the worktree.\nThe harness exports your diff as a patch and the orchestrator applies and commits it. Any commit you make dies with this worktree.\nThe embedded task text below may end with steps that mention committing; those are stripped, and if any commit instruction survives anywhere in this spec, ignore it.\n\nYou own ONLY these paths. Do not create, edit, or delete anything else, anywhere:\n- config/repo-state.template.md\n- config/conventions.template.md\n- config/conventions.md\n- skills/loop-setup/setup.sh\n- skills/handoff/SKILL.md\n- config/context-map.md\n- tests/loop-setup/reconcile.sh\n\nNever touch docs/handoffs/, docs/archive/, docs/reviews/, docs/sessions/, or any repo outside this one. The check audits this mechanically and fails on any path outside the list above.\n\nPORTABILITY (hard constraint): target macOS stock /bin/bash 3.2. No associative arrays, no ${var,,}, no grep -P, no GNU-only flags. `set -uo pipefail` and `export LC_ALL=C` at the top of every new script, BSD `date` first with a GNU `date` fallback, tab-separated pipelines. New scripts get `chmod +x`.\n\nHOW TO RUN YOUR OWN CHECK: bash tests/loop-setup/reconcile.sh && bash tests/loop-setup/docs-gitlab.sh && bash tests/run.sh. Do NOT run the full tests/run.sh unless that command includes it; a broader run would trip over a parallel neighbour's in-flight changes. The full suite runs at the wave gate.\n\nACCEPTANCE TEST CUSTODY: The acceptance test tests/loop-setup/reconcile.sh is FIXED and AUTHORITATIVE in its assertions. Make exactly the replacement or append your task's steps specify, verbatim. The harness verifies every required assertion message is present in your file before running the suite, and the validator audits your test diff line by line. Deleting or weakening any assertion the steps did not authorize replacing is an automatic fail.\n\nWork test-first, in the order the task's numbered steps give. If anything is ambiguous, take the most conservative reading, record the question in your final output, and proceed; you cannot ask.\n\n--- SOURCE PLAN, SHARED CONTRACT (verbatim) ---\n# Seam 1: Recording Convention Implementation Plan\n\n> For executors: tasks use checkbox syntax; execute in dependency order; a task is done when its\n> acceptance check passes. No specific tooling, harness, or skills are assumed.\n\n**Goal:** every work session in a conforming repo leaves one durable, hand-editable session card, handoff consumption becomes an append-only per-action transition log that auto-archives, and the three empty board lanes populate from those records.\n\n**Approach:** doctrine plus enforcement, not a daemon.\nThree small shell scripts write the judgment facts (`session-card.sh`, `handoff-log.sh`) and derive everything else (`take-stock.sh`); `lifecycle-lint.sh` grows a class h behind an adoption-date gate, and `board-cards.sh` reads the derived rows into the empty lanes.\nRollout is piloted on this repo and `~/create/sys-prompts-cc`, proven against the sys-prompts-cc handoff case, then propagated by the template-version roll.\n\n**Tech stack:** bash, awk, plain markdown; no new dependencies.\n\n**Source brief:** `docs/briefs/2026-09-07.I52.seam1-recording-convention-brief.md`\n\n## Settled decisions (pinned by the brief, not reopened here)\n\n| Decision                      | Settled value                                                            |\n| ---                           | ---                                                                      |\n| Session terminal statuses     | closed set of 5: `done`, `blocked`, `handed-off`, `parked`, `aborted`    |\n| Died-mid-work                 | an open card with no close, by construction; no status for it            |\n| Write path                    | `scripts/session-card.sh open \\| log \\| close` plus conventions doctrine |\n| Card home                     | `docs/sessions/`, one card per session, updated in place                 |\n| Filename collision            | `-2` suffix on the slug                                                  |\n| Lint class f                  | `docs/sessions/` exempt (class f scans four lanes, sessions is not one)  |\n| Done-lane closed-issue window | 60 days, a visible constant in `board-cards.sh`                          |\n| Handoff dispositions          | closed set `executed \\| superseded`, appended to a transition log        |\n| Current state                 | always derived from the log, never a mutable status field                |\n| Lint class h                  | consumed-unarchived handoffs, exit non-zero                              |\n| Class h gate                  | `lifecycle-lint-since:` in `config/repo-state.md`, on-or-after only      |\n| Adoption date                 | the date this run ships the lint class, no template placeholder          |\n| Legacy backfill               | the 20 pre-adoption handoffs, human-confirmed table, then archived       |\n| Died-mid-work threshold       | 24 hours, a hardcoded visible constant                                   |\n| take-stock                    | a new script rebuilding recorded state from git, handoffs, the tracker   |\n| Template roll                 | `template-version` 6 to 7, carrying the rubix-autorun drift fix          |\n| Pilot pair                    | this repo and `~/create/sys-prompts-cc`                                  |\n\n## Plan-owned design calls (decided here, with reversal paths)\n\n**D9, went-stale as card vs flag: flag, carried on a 13th Card TSV field `marker`.**\nA went-stale repo has no work item to be a card of, and the shipped renderer has no non-card row concept, so a went-stale card would need a lane it does not have.\nThe Card TSV grows one field, `marker`, with values empty, `went-stale`, or `died-mid-work`; `went-stale` rides the repo's git card and `died-mid-work` rides the open session card.\nA went-stale repo always gets its git card, so the existing clean-conforming suppression rule gains one exception.\nReversal: emit a separate card row with `source=state` and drop field 13, changing `board-cards.sh` and the two board tests only.\n\n**D13, session card key set and `## Log` format.**\nFrontmatter, eight keys, each mapping onto a Card TSV field: `session_card`, `session`, `token`, `title`, `status`, `closed`, `why`, `next`.\n`## Log` is append-only, one entry per line, `- <ISO-8601 UTC timestamp> <free text>`.\nReversal: add a key to `session-card.sh`'s emitter and to `take-stock.sh`'s reader; existing cards keep parsing because every reader greps line-anchored keys.\n\n**D20, take-stock invocation surface: `scripts/take-stock.sh <repo-root> [--cards]`.**\nSingle positional argument matching `lifecycle-lint.sh`'s shape; default output is the human recorded-state report, `--cards` emits the 7-field intermediate TSV that `board-cards.sh` consumes.\nReversal: split `--cards` into a second script; the intermediate TSV contract is unchanged either way.\n\n**Went-stale comparison granularity: calendar day, not seconds.**\nA commit made seconds after recording is not stale; a repo is went-stale when its newest commit's `YYYY-MM-DD` is strictly later than its recorded-state date.\nReversal: switch to an hour threshold beside `DIED_MID_WORK_HOURS` in `take-stock.sh`.\n\n**Session status to board lane mapping.**\n`open` to `in-session`, `blocked` to `blocked-on-fact`, `handed-off` to `handed-off`, `done` to `done`, `aborted` to `done`, `parked` to `backlog`.\n`aborted` lands in `done` because it is terminal and not resumable; the card's `why` says it was aborted.\nReversal: one `case` arm in `board-cards.sh`.\n\n## Global constraints\n\nHost of record for all live verification: Jeremy's Mac; this repo is `/Users/jjrdar/create/loops/loop-stack-session`.\nSession terminal status vocabulary, closed and exact: `done`, `blocked`, `handed-off`, `parked`, `aborted`; anything else is rejected by `session-card.sh close`.\nHandoff disposition vocabulary, closed and exact: `executed`, `superseded`; anything else is rejected by `handoff-log.sh disposition`.\nDone-lane closed-issue lookback: 60 days, declared as `DONE_LOOKBACK_DAYS=60` at the top of `scripts/board-cards.sh`.\nDied-mid-work threshold: 24 hours, declared as `DIED_MID_WORK_HOURS=24` at the top of `scripts/take-stock.sh`.\nHarness-agnostic rule: no session-card or transition-log field names or assumes a particular agent harness; the cards are plain markdown any harness or human can write and read.\nVerbose-announce archive rule 5: every automatic handoff archive prints `moved <src> to <dest>` on stdout, one line per moved file.\nRecording tax cap: exactly one session card per session, updated in place; anything derivable comes from `take-stock.sh`, never from hand-recording.\nEvery handoff written on or after adoption carries a `## Next actions` section, one action per `- ` line; that list is the enumeration dispositions index into.\nLint baseline, measured 2026-09-07 before any task ran, so every \"lint is clean\" check below is baseline-relative rather than a bare exit 0.\n`scripts/lifecycle-lint.sh .` currently exits 1 with exactly four findings: class a on `docs/plans/2026-08-29.I28.import-gate-diagnosis-plan.md`, class a on the two `docs/plans/2026-09-02.I52.obsidian-board-mvp-plan{,_loop}.md` files, and class f on `docs/handoffs/2026-09-01.I56-architecture-research-home.md`.\nThe `#28` class a is a known false positive: issue #28 is open and covers that plan, but remote backends match issue titles only and #28's title does not contain the stem.\nTask 6 clears the class f finding by archiving that handoff; the class a findings are a lifecycle decision put to the human at Task 6's checkpoint, never auto-taken.\nPortability: macOS stock `/bin/bash` 3.2, no associative arrays, no `${var,,}`, no `grep -P`, no GNU-only flags; `set -uo pipefail` and `export LC_ALL=C` at the top of every new script, BSD `date` first with a GNU `date` fallback, tab-separated pipelines, and the repo's no-jq brace-depth JSON scan.\nTests are plain bash scripts under `tests/`, exit non-zero on failure, print one `PASS:` line on success, build fixtures under `mktemp -d`, and never touch the real Cortex vault, the real scan roots, or any repo outside their scratch directory.\nHouse style: plain dash never the em-dash character, one sentence per physical line in prose, aligned pipe tables no wider than 110 characters, plain commit messages with no co-author line.\n\n**Session card format** (the single canonical definition; Task 1 emits it, Task 3 reads it):\n\n```\n---\nsession_card: true\nsession: <ISO-8601 UTC, when the card was opened>\ntoken: <I52 | B7 | empty>\ntitle: <one line, what this session is>\nstatus: <open | done | blocked | handed-off | parked | aborted>\nclosed: <ISO-8601 UTC, empty while open>\nwhy: <one line, the reason for the terminal status, empty while open>\nnext: <one line, the next action>\n---\n\n# <title>\n\n## Log\n\n- <ISO-8601 UTC> opened\n- <ISO-8601 UTC> <free text, one line per meaningful step>\n```\n\nPath: `docs/sessions/<YYYY-MM-DD>.<tokens>.<slug>.md`, slug from the title lowercased with every run of non-alphanumerics collapsed to a dash; on collision the slug takes a `-2` suffix, then `-3`, and so on.\n`log` and `close` target the newest card whose `status:` is `open`, resolved by the `session:` timestamp; with no open card both exit non-zero (exit 3), and with two open cards tied to the second they exit non-zero (exit 7) naming the tied files, because a silent pick between same-second cards would target one at random (the doctrine is one card per session, so a tie is already an anomaly worth surfacing).\n\n**Handoff transition log** (the single canonical definition; Task 2 writes it, Task 2 and Task 3 read it):\n\n```\n## Transitions\n\n- <ISO-8601 UTC> action <n> <executed|superseded> - <one-line note>\n```\n\nAppended to the handoff file itself, append-only, never rewritten.\nThe actions section is any `## ` heading whose text starts with `Next actions` (prefix match, so `## Next actions, in recommended order` counts); enumerable actions are the lines beginning `- ` or `<n>. ` inside it, numbered 1..N by position.\nThis matches the real corpus: the three sys-prompts-cc handoffs all carry numbered `1.` items under prefix-variant headings.\nDerived state: `live` when the section is absent, when it parses to zero actions with no bulk line, or when any parsed action has no disposition line; `consumed` when every parsed action has one (or a bulk `action 0` line stands in for a zero-action file) and the file sits in `docs/archive/`; `consumed-unarchived` in the same condition with the file still in `docs/handoffs/`.\nThe bulk `action 0` line is the legacy-backfill shape for files whose actions cannot be enumerated; it is written by `handoff-log.sh disposition <handoff> all <d>` on a zero-action file, and on a file with parsed actions the same verb appends one line per action 1..N instead.\n\n**take-stock intermediate TSV** (`take-stock.sh <root> --cards`, tab-separated, no header, 7 fields):\n\n| # | Field           | Meaning                                                             |\n| - | ---             | ---                                                                 |\n| 1 | `kind`          | `repo`, `session`, or `handoff`                                     |\n| 2 | `ref`           | repo-relative path of the card or handoff, empty on the `repo` row  |\n| 3 | `status`        | session status, or handoff derived state, empty on the `repo` row   |\n| 4 | `token`         | doc-grammar token (`I52`), or empty                                 |\n| 5 | `title`         | one-line title, tabs and newlines collapsed to spaces               |\n| 6 | `last_activity` | `YYYY-MM-DD` of the newest recorded activity for that row           |\n| 7 | `marker`        | `went-stale` (repo row), `died-mid-work` (session row), or empty    |\n\nExactly one `repo` row is emitted, always first.\nOne `session` row per file in `docs/sessions/`, one `handoff` row per handoff whose derived state is `live`.\n\n**Card TSV field 13** (extends the shipped 12-field contract in\n`docs/plans/2026-09-02.I52.obsidian-board-mvp-plan.md`):\n\n| #  | Field    | Meaning                                            |\n| -- | ---      | ---                                                |\n| 13 | `marker` | empty, `went-stale`, or `died-mid-work`            |\n\n**Card sources and ids** (`board-cards.sh`, after Task 5):\n\n| Source    | card_id                        | Column                                    |\n| ---       | ---                            | ---                                       |\n| tracker   | `<repo_key>#<token>`           | from the `agent:` label, as shipped       |\n| git       | `<repo_key>#git`               | `next-up`                                 |\n| session   | `<repo_key>#s-<file stem>`     | from `status`, per the mapping above      |\n| handoff   | `<repo_key>#h-<file stem>`     | `handed-off`                              |\n| closed    | `<repo_key>#<token>`           | `done`                                    |\n\n## Dependency graph\n\n```\nWave 1:  Task 1 (session-card.sh)   Task 2 (handoff-log.sh + lint h)   Task 4 (Card TSV field 13)\nWave 2:  Task 3 (take-stock.sh)      depends on 1, 2\n         Task 6 (legacy backfill)    depends on 2\nWave 3:  Task 5 (board wiring)       depends on 3, 4\n         Task 8 (sys-prompts-cc pilot) depends on 2, 3\nWave 4:  Task 7 (dogfood this repo)  depends on 3, 5, 6\n         Task 9 (template roll v7)   depends on 1, 2, 3, 8\nWave 5:  Task 10 (propagation + live board) depends on 5, 7, 8, 9\n```\n\nWave 1's three tasks own disjoint files and parallelize.\nTask 4 and Task 5 both own `scripts/board-cards.sh` and `tests/board/cards.sh`, so they are strictly ordered; between them the Card TSV producer and consumer agree at 13 fields and nothing is broken.\nTask 7 and Task 9 own disjoint files (session cards in this repo versus the templates and `setup.sh`) and parallelize.\nIn-task regression gates are scoped to the suites the task can affect, so a parallel neighbor's in-flight changes never block a task on damage it did not cause; the full `tests/run.sh` runs once at each wave boundary before the next wave starts, and at Task 10 Step 1.\n\n## Human checkpoints\n\n- **After Task 6, before the archive moves.** The executor presents the pre-adoption handoffs as a table (file, date, proposed disposition, one-line reason) and stops. Jeremy confirms or edits the dispositions; only then do the archive moves run, verbose-announced. This is the brief's one-time bulk backfill under human confirmation. A second, separate prompt (Task 6 Step 3b) then asks the unrelated MVP plan-set archive question on its own; one decision per prompt.\n- **Before Task 10's propagation step.** Rolling template-version 7 into seven repos outside this one writes to repos the run does not own. The executor prints the target list with each repo's current template-version and working-tree state and stops for an explicit go naming which repos roll; a dirty tree blocks that repo's roll.\n- **After Task 10, brief criterion 7 `[judgment]`.** After one real work session under the convention, Jeremy answers two questions: did the recording tax feel acceptable, and does the card answer \"where did I leave off\" without reconstruction. Record the verdict in the run's session card `why:` line. Criteria 1 through 6 cover the mechanics; this call stays human.\n\n## How to run\n\nAll commands run from this repo's root, `/Users/jjrdar/create/loops/loop-stack-session`.\n\nFull test suite (globs `tests/*/*.sh`, so the new `tests/sessions/` directory is picked up with no wiring):\n\n```\ntests/run.sh\n```\n\nA single suite:\n\n```\nbash tests/sessions/session-card.sh\n```\n\nRecorded state and lint for any repo:\n\n```\nscripts/take-stock.sh .\nscripts/lifecycle-lint.sh .\n```\n\nBoard render (the environment the shipped MVP already takes):\n\n```\nexport LOOP_BOARD_CORTEX=/Users/jjrdar/Documents/_CS_DOCUMENTS_/Obsidian/Cortex\nexport LOOP_BOARD_HOME=\"$LOOP_BOARD_CORTEX/00_System/Board\"\nscripts/board.sh\n```\n\nThe two `export` lines are separate statements on purpose: a single-command environment prefix does not expand `LOOP_BOARD_CORTEX` for the assignment that follows it on the same line.\n\n---\n\n--- SOURCE PLAN, YOUR TASK (verbatim) ---\n### Task 9: Template roll, version 6 to 7\n\nDepends on: Task 1, Task 2, Task 3, Task 8.\n\n**Files (exclusive ownership):**\n- Modify: `config/repo-state.template.md` (`template-version:` at line 8; the Lanes table at lines 19 to 28)\n- Modify: `config/conventions.template.md` (the import-sweep exclusion sentence at line 31; two new doctrine sections; one new archive rule)\n- Modify: `config/conventions.md` (refreshed as a verbatim copy of the template)\n- Modify: `skills/loop-setup/setup.sh` (`is_excluded()` at lines 33 to 35; the vendored-script install block at lines 152 to 185; the refresh loop `for pair in ...` at line 189; `reconcile_config()`'s carry-forward at lines 318 to 321)\n- Modify: `skills/handoff/SKILL.md` (one added line)\n- Modify: `config/context-map.md` (one added index line; note it already carries one uncommitted user edit, which rides along - do not revert it)\n- Test: `tests/loop-setup/reconcile.sh` (extend)\n\n**Interfaces:**\n- Consumes: the three scripts from Tasks 1, 2, and 3, which become vendored artifacts.\n- Produces: `template-version: 7` in `config/repo-state.template.md`, doctrine for session records and the handoff lifecycle in `config/conventions.template.md`, `setup.sh` vendoring and refreshing `session-card.sh`, `handoff-log.sh`, and `take-stock.sh` alongside the five it already carries, and a reconcile that preserves `rubix-autorun:` and `lifecycle-lint-since:` across a re-render.\n\nBehavior notes, each edge named:\n- `rubix-autorun` drift fix (D25): the renderers emit the template's literal `rubix-autorun: ask` line, and `reconcile_config`'s carry-forward loop does not cover it, so a repo that set `off` or `on` silently resets to `ask`. The fix is a substitution on the rendered candidate, not an append, because appending would duplicate the key the renderer already emitted:\n\n```\n  ra=\"$(grep -E '^rubix-autorun:' config/repo-state.md | head -1 | sed -E 's/^rubix-autorun:[[:space:]]*//; s/[[:space:]]*$//')\"\n  case \"$ra\" in\n    ask|off|on) cand=\"$(printf '%s\\n' \"$cand\" | sed -E \"s|^rubix-autorun:.*|rubix-autorun: $ra|\")\" ;;\n    *) : ;;   # unrecognized or absent: leave the rendered default; never interpolate free text into sed\n  esac\n```\n\n  The substitution runs on the candidate before the `diff -u` the human reviews, so the approved diff is the file that gets written.\n\n- `lifecycle-lint-since` (D16, no placeholder): the template carries no `lifecycle-lint-since:` line at all, so the renderers emit none and there is nothing to duplicate. The key is added to the carry-forward loop, which becomes `for k in autonomy-default tracker-remote-ack lifecycle-lint-since; do`. A repo without the key stays without it, and class h stays dormant there.\n- Lanes table gains one row: `| Sessions      | docs/sessions/                | One card per session; see conventions.md.      |` with the pipes padded to match the existing rows.\n- The convention must sit on the files a fresh agent is actually told to read (`CLAUDE.md` points at `config/repo-state.md` and `config/context-map.md`; nothing points at `conventions.md`): `config/repo-state.template.md` gains one line directly under its Lanes table - `Every session opens a session card before its first substantive action: scripts/session-card.sh open (doctrine: conventions.md, Session records).` - so the rendered repo-state in every conforming repo carries the pointer, and this repo's `config/context-map.md` gains one index line pointing at the Session records section of `conventions.md`.\n- `is_excluded()` gains `docs/spikes/*` and `docs/sessions/*` to its first `case` arm, so neither a spike finding nor a session card is ever offered as a tracker-issue import candidate.\n- The conventions template's exclusion sentence gains `docs/spikes/`, and `docs/sessions/` immediately before `docs/reviews/`, keeping the prose and the code in agreement (this is the brief's second folded board-side advisory finding).\n- `skills/handoff/SKILL.md` gains one line after its line 15: `Include a \"## Next actions\" section listing one next action per \"- \" line; that list is what handoff consumption dispositions index into.`\n- Two new doctrine sections in `config/conventions.template.md`, placed after `## Where I left off`:\n\n```\n## Session records\n\nEvery work session in a conforming repo opens a session card before its first substantive action and closes it before it ends.\n`scripts/session-card.sh open \"<title>\" [<token>]` at the start, `scripts/session-card.sh log \"<one line>\"` after each meaningful step - at minimum once per commit, so the card's floor is the commit history's grain - and `scripts/session-card.sh close <status> \"<why>\" \"<next>\"` at the end.\nCards live in `docs/sessions/`, one per session, updated in place, and are committed with the work they record.\nThe terminal status vocabulary is closed: `done`, `blocked`, `handed-off`, `parked`, `aborted`.\nA card opened and never closed is, by construction, a session that died mid-work; nothing is recorded at the moment of death.\nThe recording tax is capped at that one card: everything derivable comes from `scripts/take-stock.sh`, never from hand-recording.\nCards are plain markdown and harness-agnostic - no field names or assumes a particular agent harness, so any harness or a human can write and read them.\n`docs/sessions/` is outside the filename-grammar lanes, so lint class f never scans it and the `-2` collision suffix is legal there.\n\n## Handoff lifecycle\n\nEvery handoff carries a `## Next actions` section, one action per `- ` line; that list is the enumeration dispositions index into.\nConsumption is per action: `scripts/handoff-log.sh disposition <handoff> <n> <executed|superseded> [note]` appends one line to the handoff's own `## Transitions` section.\nThe log is append-only and current state is always derived from it, never held in a mutable status field; \"was this action already done\" is answered from history.\n`scripts/handoff-log.sh consume <handoff>` refuses while any action lacks a disposition, and the disposition that completes the set archives the handoff automatically, announced under archive rule 5.\n`docs/handoffs/` therefore holds only live handoffs.\n`scripts/lifecycle-lint.sh` class h enforces this for handoffs dated on or after the `lifecycle-lint-since:` key in `config/repo-state.md`; with the key absent the class does not run, and handoffs dated earlier are grandfathered.\n```\n\n- One new archive rule in the same file's `## Archive and graduation rules` list: `7. A handoff archives automatically when every action in its ## Next actions list carries a disposition; the move is announced under rule 5.`\n- The live `config/conventions.md` carries a 5-line `rubix-autorun` doctrine block (after its line 27) that the template lacks - the doctrine half of the D25 drift.\n  Fold that block verbatim into `config/conventions.template.md` at the same position before any copy; a bare `cp` without the fold would delete shipped doctrine.\n- After editing the template, refresh `config/conventions.md` with `cp config/conventions.template.md config/conventions.md`, which is exactly what `reconcile_config` does, so this repo and a reconciled repo stay byte-identical.\n\n**Acceptance check:** `bash tests/loop-setup/reconcile.sh` exits 0 and `bash tests/loop-setup/docs-gitlab.sh` exits 0 `[executed-check]`.\n\n- [ ] Step 1: Extend `tests/loop-setup/reconcile.sh`. Append, before its final `echo \"PASS: ...` line, exactly:\n\n```\n# --- v7: rubix-autorun and lifecycle-lint-since survive a re-render, unduplicated ---\nW=\"$(mktemp -d)\"; trap 'rm -rf \"$REF\" \"$S\" \"$G\" \"$V\" \"$W\"' EXIT\n( cd \"$W\" && git init -q )\nmkdir -p \"$W/config\"\ncat > \"$W/config/repo-state.md\" <<'EOS'\n# Repo State Map\n\ntemplate-version: 6\nfilename-grammar-since: 2026-02-02\nlifecycle-lint-since: 2026-09-08\n\nRemote: none (local tracker; see the Local tracker section)\ntracker: local\nrubix-autorun: off\nEOS\n( cd \"$W\" && LOOP_ASSUME_YES=1 \"$SETUP\" </dev/null >/dev/null ) || fail \"v7 accept re-render exited non-zero\"\ngrep -q \"^template-version: $TV\\$\" \"$W/config/repo-state.md\" || fail \"re-render did not bump the version to $TV\"\ngrep -q '^rubix-autorun: off$' \"$W/config/repo-state.md\" || fail \"re-render reset rubix-autorun to ask (the drift bug)\"\n[ \"$(grep -c '^rubix-autorun:' \"$W/config/repo-state.md\")\" -eq 1 ] || fail \"re-render duplicated rubix-autorun\"\ngrep -q '^lifecycle-lint-since: 2026-09-08$' \"$W/config/repo-state.md\" \\\n  || fail \"re-render dropped lifecycle-lint-since\"\n[ \"$(grep -c '^lifecycle-lint-since:' \"$W/config/repo-state.md\")\" -eq 1 ] \\\n  || fail \"re-render duplicated lifecycle-lint-since\"\ngrep -q '{{LIFECYCLE_LINT_SINCE}}' \"$TPL\" && fail \"the template must carry no LIFECYCLE_LINT_SINCE placeholder\"\ngrep -q 'docs/sessions' \"$W/config/repo-state.md\" || fail \"the v7 Lanes table has no Sessions row\"\nfor s in session-card.sh handoff-log.sh take-stock.sh; do\n  [ -f \"$W/scripts/$s\" ] || fail \"the v7 roll did not vendor scripts/$s\"\n  [ -x \"$W/scripts/$s\" ] || fail \"vendored scripts/$s is not executable\"\ndone\ngrep -q 'docs/spikes/' \"$REPO/config/conventions.template.md\" \\\n  || fail \"the conventions template does not exclude docs/spikes/ from the import sweep\"\ngrep -q 'docs/sessions/' \"$REPO/config/conventions.template.md\" \\\n  || fail \"the conventions template does not exclude docs/sessions/ from the import sweep\"\ndiff \"$REPO/config/conventions.template.md\" \"$REPO/config/conventions.md\" \\\n  || fail \"this repo's conventions.md has drifted from the v7 template\"\n```\n\n- [ ] Step 2: Run it - `bash tests/loop-setup/reconcile.sh`, expected FAIL on `re-render reset rubix-autorun to ask (the drift bug)`.\n- [ ] Step 3: Make the `config/repo-state.template.md` edits: bump `template-version:` to `7`, add the Sessions row to the Lanes table, and add the session-card pointer line under the table per the behavior note.\n- [ ] Step 4: Make the `config/conventions.template.md` edits: fold in the live file's 5-line `rubix-autorun` doctrine block first, then the two doctrine sections, archive rule 7, and the exclusion sentence gaining `docs/spikes/` and `docs/sessions/`. Then `cp config/conventions.template.md config/conventions.md`, and confirm the copy still contains the `rubix-autorun` doctrine block.\n- [ ] Step 5: Make the `skills/loop-setup/setup.sh` edits: `is_excluded()` exclusions, three new vendored scripts in the install block and the refresh loop, the `rubix-autorun` substitution, and `lifecycle-lint-since` in the carry-forward loop.\n- [ ] Step 6: Add the one line to `skills/handoff/SKILL.md`, and the one index line to `config/context-map.md` pointing at the Session records doctrine.\n- [ ] Step 7: Run it - `bash tests/loop-setup/reconcile.sh`, expected PASS. Then `tests/run.sh`, all suites green.\n\n--- OUTPUT CONTRACT ---\nEnd your run by printing exactly one JSON object on its own line:\n{\"unit\": \"t9-template-v7\", \"files_changed\": [...], \"tests_passed\": <n>, \"tests_failed\": <n>, \"deviations\": [...], \"open_questions\": [...], \"deferred_items\": [...]}\nLeave everything uncommitted. The harness exports the patch.",
      "check": "set -uo pipefail\nexport LC_ALL=C\nPATCH=/Users/jjrdar/.ringer/work/i52-seam1-recording-convention/patches/t9-template-v7.patch\nmkdir -p \"$(dirname \"$PATCH\")\"\n\n# acceptance-test custody: every required assertion message must be present\nmissing=0\nwhile IFS= read -r line; do\n  [ -n \"$line\" ] || continue\n  grep -Fq -- \"$line\" tests/loop-setup/reconcile.sh || { echo \"CHECK FAIL: required assertion missing from tests/loop-setup/reconcile.sh: $line\"; missing=1; }\ndone <<'REQUIRED_EOF'\nv7 accept re-render exited non-zero\nre-render did not bump the version to\nre-render reset rubix-autorun to ask (the drift bug)\nre-render duplicated rubix-autorun\nre-render dropped lifecycle-lint-since\nre-render duplicated lifecycle-lint-since\nthe template must carry no LIFECYCLE_LINT_SINCE placeholder\nthe v7 Lanes table has no Sessions row\nthe v7 roll did not vendor scripts/\nvendored scripts/\nthe conventions template does not exclude docs/spikes/ from the import sweep\nthe conventions template does not exclude docs/sessions/ from the import sweep\nthis repo's conventions.md has drifted from the v7 template\nREQUIRED_EOF\n[ \"$missing\" -eq 0 ] || exit 1\n\n# run the acceptance test, printing everything\nbash tests/loop-setup/reconcile.sh; rc=$?\n[ \"$rc\" -eq 0 ] || { echo \"CHECK FAIL: acceptance test tests/loop-setup/reconcile.sh exited $rc; its output above names the failed assertion\"; exit 1; }\n\n# scoped regression gate (this task's suites only)\nbash tests/loop-setup/reconcile.sh && bash tests/loop-setup/docs-gitlab.sh && bash tests/run.sh\nrc=$?\n[ \"$rc\" -eq 0 ] || { echo \"CHECK FAIL: the scoped regression gate exited $rc; a suite this task can affect regressed\"; exit 1; }\n\n# ownership audit\ngit add -A\nextra=\"$(git diff --cached --name-only | grep -vE '^(config/repo\\-state\\.template\\.md|config/conventions\\.template\\.md|config/conventions\\.md|skills/loop\\-setup/setup\\.sh|skills/handoff/SKILL\\.md|config/context\\-map\\.md|tests/loop\\-setup/reconcile\\.sh)$' || true)\"\n[ -z \"$extra\" ] || { echo \"CHECK FAIL: files outside the ownership list were changed:\"; printf '%s\\n' \"$extra\"; exit 1; }\n\n# portability audit on changed shell files\nbad=\"$(git diff --cached --name-only | grep -E '\\.sh$' | xargs grep -nE 'declare -A|\\$\\{[A-Za-z_]+,,\\}|grep -P' 2>/dev/null || true)\"\n[ -z \"$bad\" ] || { echo \"CHECK FAIL: bash 3.2 portability violation (associative array, lowercase expansion, or grep -P):\"; printf '%s\\n' \"$bad\"; exit 1; }\n\n# export the patch outside the worktree\ngit diff --cached > \"$PATCH\"\n[ -s \"$PATCH\" ] || { echo \"CHECK FAIL: exported patch $PATCH is empty; the worker produced no applicable work\"; exit 1; }\necho \"CHECK PASS: The reconcile suite passes at template-version 7: rubix-autorun and lifecycle-lint-since both survive a re-render unduplicated, no placeholder is introduced, the Sessions lane row exists, the three new scripts are vendored executable, and conventions.md is byte-identical to the template.\"",
      "expect_files": [
        "/Users/jjrdar/.ringer/work/i52-seam1-recording-convention/patches/t9-template-v7.patch"
      ],
      "verified": "The reconcile suite passes at template-version 7: rubix-autorun and lifecycle-lint-since both survive a re-render unduplicated, no placeholder is introduced, the Sessions lane row exists, the three new scripts are vendored executable, and conventions.md is byte-identical to the template."
    }
  ]
}
