{
  "run_name": "i52-seam1-recording-convention",
  "repo": "/Users/jjrdar/create/loops/loop-stack-session",
  "workdir": "/Users/jjrdar/.ringer/work/i52-seam1-recording-convention",
  "worktrees": true,
  "max_parallel": 3,
  "tasks": [
    {
      "key": "t1-session-card",
      "engine": "claude-zai",
      "model": "glm-5.2",
      "task_type": "code-feature",
      "timeout_s": 3600,
      "spec": "You are a code-feature worker (test-first) implementing ONE unit of the loop-stack seam 1 recording convention.\nYour current working directory IS a git worktree of the loop-stack-session repo. Edit files here directly.\nDo NOT push. Do NOT run git commit at any point: leave every change uncommitted in the worktree.\nThe harness exports your diff as a patch and the orchestrator applies and commits it. Any commit you make dies with this worktree.\nThe embedded task text below may end with steps that mention committing; those are stripped, and if any commit instruction survives anywhere in this spec, ignore it.\n\nYou own ONLY these paths. Do not create, edit, or delete anything else, anywhere:\n- scripts/session-card.sh\n- tests/sessions/session-card.sh\n- tests/hardcodes/sweep.sh\n\nNever touch docs/handoffs/, docs/archive/, docs/reviews/, docs/sessions/, or any repo outside this one. The check audits this mechanically and fails on any path outside the list above.\n\nPORTABILITY (hard constraint): target macOS stock /bin/bash 3.2. No associative arrays, no ${var,,}, no grep -P, no GNU-only flags. `set -uo pipefail` and `export LC_ALL=C` at the top of every new script, BSD `date` first with a GNU `date` fallback, tab-separated pipelines. New scripts get `chmod +x`.\n\nHOW TO RUN YOUR OWN CHECK: bash tests/sessions/session-card.sh && bash tests/hardcodes/sweep.sh. Do NOT run the full tests/run.sh unless that command includes it; a broader run would trip over a parallel neighbour's in-flight changes. The full suite runs at the wave gate.\n\nACCEPTANCE TEST CUSTODY: The acceptance test tests/sessions/session-card.sh is FIXED and AUTHORITATIVE. Write it first, verbatim from your task's Step 1 block, exactly as the steps say. The harness then OVERWRITES your copy with the canonical text and runs that. You cannot pass by editing the test.\n\nWork test-first, in the order the task's numbered steps give. If anything is ambiguous, take the most conservative reading, record the question in your final output, and proceed; you cannot ask.\n\n--- SOURCE PLAN, SHARED CONTRACT (verbatim) ---\n# Seam 1: Recording Convention Implementation Plan\n\n> For executors: tasks use checkbox syntax; execute in dependency order; a task is done when its\n> acceptance check passes. No specific tooling, harness, or skills are assumed.\n\n**Goal:** every work session in a conforming repo leaves one durable, hand-editable session card, handoff consumption becomes an append-only per-action transition log that auto-archives, and the three empty board lanes populate from those records.\n\n**Approach:** doctrine plus enforcement, not a daemon.\nThree small shell scripts write the judgment facts (`session-card.sh`, `handoff-log.sh`) and derive everything else (`take-stock.sh`); `lifecycle-lint.sh` grows a class h behind an adoption-date gate, and `board-cards.sh` reads the derived rows into the empty lanes.\nRollout is piloted on this repo and `~/create/sys-prompts-cc`, proven against the sys-prompts-cc handoff case, then propagated by the template-version roll.\n\n**Tech stack:** bash, awk, plain markdown; no new dependencies.\n\n**Source brief:** `docs/briefs/2026-09-07.I52.seam1-recording-convention-brief.md`\n\n## Settled decisions (pinned by the brief, not reopened here)\n\n| Decision                      | Settled value                                                            |\n| ---                           | ---                                                                      |\n| Session terminal statuses     | closed set of 5: `done`, `blocked`, `handed-off`, `parked`, `aborted`    |\n| Died-mid-work                 | an open card with no close, by construction; no status for it            |\n| Write path                    | `scripts/session-card.sh open \\| log \\| close` plus conventions doctrine |\n| Card home                     | `docs/sessions/`, one card per session, updated in place                 |\n| Filename collision            | `-2` suffix on the slug                                                  |\n| Lint class f                  | `docs/sessions/` exempt (class f scans four lanes, sessions is not one)  |\n| Done-lane closed-issue window | 60 days, a visible constant in `board-cards.sh`                          |\n| Handoff dispositions          | closed set `executed \\| superseded`, appended to a transition log        |\n| Current state                 | always derived from the log, never a mutable status field                |\n| Lint class h                  | consumed-unarchived handoffs, exit non-zero                              |\n| Class h gate                  | `lifecycle-lint-since:` in `config/repo-state.md`, on-or-after only      |\n| Adoption date                 | the date this run ships the lint class, no template placeholder          |\n| Legacy backfill               | the 20 pre-adoption handoffs, human-confirmed table, then archived       |\n| Died-mid-work threshold       | 24 hours, a hardcoded visible constant                                   |\n| take-stock                    | a new script rebuilding recorded state from git, handoffs, the tracker   |\n| Template roll                 | `template-version` 6 to 7, carrying the rubix-autorun drift fix          |\n| Pilot pair                    | this repo and `~/create/sys-prompts-cc`                                  |\n\n## Plan-owned design calls (decided here, with reversal paths)\n\n**D9, went-stale as card vs flag: flag, carried on a 13th Card TSV field `marker`.**\nA went-stale repo has no work item to be a card of, and the shipped renderer has no non-card row concept, so a went-stale card would need a lane it does not have.\nThe Card TSV grows one field, `marker`, with values empty, `went-stale`, or `died-mid-work`; `went-stale` rides the repo's git card and `died-mid-work` rides the open session card.\nA went-stale repo always gets its git card, so the existing clean-conforming suppression rule gains one exception.\nReversal: emit a separate card row with `source=state` and drop field 13, changing `board-cards.sh` and the two board tests only.\n\n**D13, session card key set and `## Log` format.**\nFrontmatter, eight keys, each mapping onto a Card TSV field: `session_card`, `session`, `token`, `title`, `status`, `closed`, `why`, `next`.\n`## Log` is append-only, one entry per line, `- <ISO-8601 UTC timestamp> <free text>`.\nReversal: add a key to `session-card.sh`'s emitter and to `take-stock.sh`'s reader; existing cards keep parsing because every reader greps line-anchored keys.\n\n**D20, take-stock invocation surface: `scripts/take-stock.sh <repo-root> [--cards]`.**\nSingle positional argument matching `lifecycle-lint.sh`'s shape; default output is the human recorded-state report, `--cards` emits the 7-field intermediate TSV that `board-cards.sh` consumes.\nReversal: split `--cards` into a second script; the intermediate TSV contract is unchanged either way.\n\n**Went-stale comparison granularity: calendar day, not seconds.**\nA commit made seconds after recording is not stale; a repo is went-stale when its newest commit's `YYYY-MM-DD` is strictly later than its recorded-state date.\nReversal: switch to an hour threshold beside `DIED_MID_WORK_HOURS` in `take-stock.sh`.\n\n**Session status to board lane mapping.**\n`open` to `in-session`, `blocked` to `blocked-on-fact`, `handed-off` to `handed-off`, `done` to `done`, `aborted` to `done`, `parked` to `backlog`.\n`aborted` lands in `done` because it is terminal and not resumable; the card's `why` says it was aborted.\nReversal: one `case` arm in `board-cards.sh`.\n\n## Global constraints\n\nHost of record for all live verification: Jeremy's Mac; this repo is `/Users/jjrdar/create/loops/loop-stack-session`.\nSession terminal status vocabulary, closed and exact: `done`, `blocked`, `handed-off`, `parked`, `aborted`; anything else is rejected by `session-card.sh close`.\nHandoff disposition vocabulary, closed and exact: `executed`, `superseded`; anything else is rejected by `handoff-log.sh disposition`.\nDone-lane closed-issue lookback: 60 days, declared as `DONE_LOOKBACK_DAYS=60` at the top of `scripts/board-cards.sh`.\nDied-mid-work threshold: 24 hours, declared as `DIED_MID_WORK_HOURS=24` at the top of `scripts/take-stock.sh`.\nHarness-agnostic rule: no session-card or transition-log field names or assumes a particular agent harness; the cards are plain markdown any harness or human can write and read.\nVerbose-announce archive rule 5: every automatic handoff archive prints `moved <src> to <dest>` on stdout, one line per moved file.\nRecording tax cap: exactly one session card per session, updated in place; anything derivable comes from `take-stock.sh`, never from hand-recording.\nEvery handoff written on or after adoption carries a `## Next actions` section, one action per `- ` line; that list is the enumeration dispositions index into.\nLint baseline, measured 2026-09-07 before any task ran, so every \"lint is clean\" check below is baseline-relative rather than a bare exit 0.\n`scripts/lifecycle-lint.sh .` currently exits 1 with exactly four findings: class a on `docs/plans/2026-08-29.I28.import-gate-diagnosis-plan.md`, class a on the two `docs/plans/2026-09-02.I52.obsidian-board-mvp-plan{,_loop}.md` files, and class f on `docs/handoffs/2026-09-01.I56-architecture-research-home.md`.\nThe `#28` class a is a known false positive: issue #28 is open and covers that plan, but remote backends match issue titles only and #28's title does not contain the stem.\nTask 6 clears the class f finding by archiving that handoff; the class a findings are a lifecycle decision put to the human at Task 6's checkpoint, never auto-taken.\nPortability: macOS stock `/bin/bash` 3.2, no associative arrays, no `${var,,}`, no `grep -P`, no GNU-only flags; `set -uo pipefail` and `export LC_ALL=C` at the top of every new script, BSD `date` first with a GNU `date` fallback, tab-separated pipelines, and the repo's no-jq brace-depth JSON scan.\nTests are plain bash scripts under `tests/`, exit non-zero on failure, print one `PASS:` line on success, build fixtures under `mktemp -d`, and never touch the real Cortex vault, the real scan roots, or any repo outside their scratch directory.\nHouse style: plain dash never the em-dash character, one sentence per physical line in prose, aligned pipe tables no wider than 110 characters, plain commit messages with no co-author line.\n\n**Session card format** (the single canonical definition; Task 1 emits it, Task 3 reads it):\n\n```\n---\nsession_card: true\nsession: <ISO-8601 UTC, when the card was opened>\ntoken: <I52 | B7 | empty>\ntitle: <one line, what this session is>\nstatus: <open | done | blocked | handed-off | parked | aborted>\nclosed: <ISO-8601 UTC, empty while open>\nwhy: <one line, the reason for the terminal status, empty while open>\nnext: <one line, the next action>\n---\n\n# <title>\n\n## Log\n\n- <ISO-8601 UTC> opened\n- <ISO-8601 UTC> <free text, one line per meaningful step>\n```\n\nPath: `docs/sessions/<YYYY-MM-DD>.<tokens>.<slug>.md`, slug from the title lowercased with every run of non-alphanumerics collapsed to a dash; on collision the slug takes a `-2` suffix, then `-3`, and so on.\n`log` and `close` target the newest card whose `status:` is `open`, resolved by the `session:` timestamp; with no open card both exit non-zero (exit 3), and with two open cards tied to the second they exit non-zero (exit 7) naming the tied files, because a silent pick between same-second cards would target one at random (the doctrine is one card per session, so a tie is already an anomaly worth surfacing).\n\n**Handoff transition log** (the single canonical definition; Task 2 writes it, Task 2 and Task 3 read it):\n\n```\n## Transitions\n\n- <ISO-8601 UTC> action <n> <executed|superseded> - <one-line note>\n```\n\nAppended to the handoff file itself, append-only, never rewritten.\nThe actions section is any `## ` heading whose text starts with `Next actions` (prefix match, so `## Next actions, in recommended order` counts); enumerable actions are the lines beginning `- ` or `<n>. ` inside it, numbered 1..N by position.\nThis matches the real corpus: the three sys-prompts-cc handoffs all carry numbered `1.` items under prefix-variant headings.\nDerived state: `live` when the section is absent, when it parses to zero actions with no bulk line, or when any parsed action has no disposition line; `consumed` when every parsed action has one (or a bulk `action 0` line stands in for a zero-action file) and the file sits in `docs/archive/`; `consumed-unarchived` in the same condition with the file still in `docs/handoffs/`.\nThe bulk `action 0` line is the legacy-backfill shape for files whose actions cannot be enumerated; it is written by `handoff-log.sh disposition <handoff> all <d>` on a zero-action file, and on a file with parsed actions the same verb appends one line per action 1..N instead.\n\n**take-stock intermediate TSV** (`take-stock.sh <root> --cards`, tab-separated, no header, 7 fields):\n\n| # | Field           | Meaning                                                             |\n| - | ---             | ---                                                                 |\n| 1 | `kind`          | `repo`, `session`, or `handoff`                                     |\n| 2 | `ref`           | repo-relative path of the card or handoff, empty on the `repo` row  |\n| 3 | `status`        | session status, or handoff derived state, empty on the `repo` row   |\n| 4 | `token`         | doc-grammar token (`I52`), or empty                                 |\n| 5 | `title`         | one-line title, tabs and newlines collapsed to spaces               |\n| 6 | `last_activity` | `YYYY-MM-DD` of the newest recorded activity for that row           |\n| 7 | `marker`        | `went-stale` (repo row), `died-mid-work` (session row), or empty    |\n\nExactly one `repo` row is emitted, always first.\nOne `session` row per file in `docs/sessions/`, one `handoff` row per handoff whose derived state is `live`.\n\n**Card TSV field 13** (extends the shipped 12-field contract in\n`docs/plans/2026-09-02.I52.obsidian-board-mvp-plan.md`):\n\n| #  | Field    | Meaning                                            |\n| -- | ---      | ---                                                |\n| 13 | `marker` | empty, `went-stale`, or `died-mid-work`            |\n\n**Card sources and ids** (`board-cards.sh`, after Task 5):\n\n| Source    | card_id                        | Column                                    |\n| ---       | ---                            | ---                                       |\n| tracker   | `<repo_key>#<token>`           | from the `agent:` label, as shipped       |\n| git       | `<repo_key>#git`               | `next-up`                                 |\n| session   | `<repo_key>#s-<file stem>`     | from `status`, per the mapping above      |\n| handoff   | `<repo_key>#h-<file stem>`     | `handed-off`                              |\n| closed    | `<repo_key>#<token>`           | `done`                                    |\n\n## Dependency graph\n\n```\nWave 1:  Task 1 (session-card.sh)   Task 2 (handoff-log.sh + lint h)   Task 4 (Card TSV field 13)\nWave 2:  Task 3 (take-stock.sh)      depends on 1, 2\n         Task 6 (legacy backfill)    depends on 2\nWave 3:  Task 5 (board wiring)       depends on 3, 4\n         Task 8 (sys-prompts-cc pilot) depends on 2, 3\nWave 4:  Task 7 (dogfood this repo)  depends on 3, 5, 6\n         Task 9 (template roll v7)   depends on 1, 2, 3, 8\nWave 5:  Task 10 (propagation + live board) depends on 5, 7, 8, 9\n```\n\nWave 1's three tasks own disjoint files and parallelize.\nTask 4 and Task 5 both own `scripts/board-cards.sh` and `tests/board/cards.sh`, so they are strictly ordered; between them the Card TSV producer and consumer agree at 13 fields and nothing is broken.\nTask 7 and Task 9 own disjoint files (session cards in this repo versus the templates and `setup.sh`) and parallelize.\nIn-task regression gates are scoped to the suites the task can affect, so a parallel neighbor's in-flight changes never block a task on damage it did not cause; the full `tests/run.sh` runs once at each wave boundary before the next wave starts, and at Task 10 Step 1.\n\n## Human checkpoints\n\n- **After Task 6, before the archive moves.** The executor presents the pre-adoption handoffs as a table (file, date, proposed disposition, one-line reason) and stops. Jeremy confirms or edits the dispositions; only then do the archive moves run, verbose-announced. This is the brief's one-time bulk backfill under human confirmation. A second, separate prompt (Task 6 Step 3b) then asks the unrelated MVP plan-set archive question on its own; one decision per prompt.\n- **Before Task 10's propagation step.** Rolling template-version 7 into seven repos outside this one writes to repos the run does not own. The executor prints the target list with each repo's current template-version and working-tree state and stops for an explicit go naming which repos roll; a dirty tree blocks that repo's roll.\n- **After Task 10, brief criterion 7 `[judgment]`.** After one real work session under the convention, Jeremy answers two questions: did the recording tax feel acceptable, and does the card answer \"where did I leave off\" without reconstruction. Record the verdict in the run's session card `why:` line. Criteria 1 through 6 cover the mechanics; this call stays human.\n\n## How to run\n\nAll commands run from this repo's root, `/Users/jjrdar/create/loops/loop-stack-session`.\n\nFull test suite (globs `tests/*/*.sh`, so the new `tests/sessions/` directory is picked up with no wiring):\n\n```\ntests/run.sh\n```\n\nA single suite:\n\n```\nbash tests/sessions/session-card.sh\n```\n\nRecorded state and lint for any repo:\n\n```\nscripts/take-stock.sh .\nscripts/lifecycle-lint.sh .\n```\n\nBoard render (the environment the shipped MVP already takes):\n\n```\nexport LOOP_BOARD_CORTEX=/Users/jjrdar/Documents/_CS_DOCUMENTS_/Obsidian/Cortex\nexport LOOP_BOARD_HOME=\"$LOOP_BOARD_CORTEX/00_System/Board\"\nscripts/board.sh\n```\n\nThe two `export` lines are separate statements on purpose: a single-command environment prefix does not expand `LOOP_BOARD_CORTEX` for the assignment that follows it on the same line.\n\n---\n\n--- SOURCE PLAN, YOUR TASK (verbatim) ---\n### Task 1: `scripts/session-card.sh` - open, log, close\n\nDepends on: none.\n\n**Files (exclusive ownership):**\n- Create: `scripts/session-card.sh`\n- Modify: `tests/hardcodes/sweep.sh` (add `docs/sessions/` to the `ALLOW` array: session-card log lines quote real commands, which on this host contain `/Users/jjrdar` paths, and a committed card must not turn the sweep red)\n- Test: `tests/sessions/session-card.sh`\n\n**Interfaces:**\n- Consumes: nothing from other tasks. Operates on the current working directory's `docs/sessions/`, creating it when absent.\n- Produces: three subcommands, all writing plain markdown in the Global-constraints session card format.\n  - `session-card.sh open \"<title>\" [<token>]` creates a card and prints its repo-relative path on stdout; exit 0.\n  - `session-card.sh log \"<text>\"` appends `- <ISO-8601 UTC> <text>` to the newest open card's `## Log`; exit 0.\n  - `session-card.sh close <status> \"<why>\" \"<next>\"` sets `status:`, stamps `closed:`, writes `why:` and `next:`, and appends a `closed <status>` log line; exit 0.\n- Exit codes: 2 on a usage error, 3 when `log` or `close` finds no open card, 4 when `close` is given a status outside the closed vocabulary, 7 when two open cards tie on `session:` to the second (the message names the tied files).\n\nBehavior notes, each edge named:\n- Status vocabulary is the exact five-word list; validation is a `case` statement, not a regex.\n- Slug: title lowercased, every run of characters outside `[a-z0-9]` collapsed to a single dash, leading and trailing dashes stripped.\n- Collision: if `docs/sessions/<date>.<tokens>.<slug>.md` exists, try `-2`, then `-3`, and so on until a free name is found; an existing card is never overwritten.\n- Token argument absent means the `token:` frontmatter value is empty; the filename then carries no token segment.\n- Free text in `<title>`, `<why>`, `<next>`, and `<text>` passes through `tr '\\t\\n' '  '` so no value can break the single-line frontmatter shape or split a log line.\n- `log` on a card whose `status:` is not `open` exits 3; a closed card is never appended to.\n- Key rewrites use `awk` over the frontmatter block and a `mv` of a temp file, never `sed -i` (BSD and GNU disagree on `-i`), and rewrite each key in place so a key is never duplicated.\n- The card is committed with the work it records; the script itself never runs git.\n\n**Acceptance check:** `bash tests/sessions/session-card.sh` exits 0 `[executed-check]`.\n\n- [ ] Step 1: Write the failing test `tests/sessions/session-card.sh` verbatim:\n\n```\n#!/usr/bin/env bash\n# session-card.sh open creates one card carrying the frontmatter key set, log appends timestamped\n# ## Log lines, close writes a terminal status from the closed vocabulary and rejects anything else,\n# a closed card refuses further logs, a same-day same-slug open takes the -2 suffix, and no field\n# names a specific agent harness.\nset -uo pipefail\nHERE=\"$(cd \"$(dirname \"$0\")\" && pwd)\"\nREPO=\"$(cd \"$HERE/../..\" && pwd)\"\nSC=\"$REPO/scripts/session-card.sh\"\nfail() { echo \"FAIL: $1\" >&2; exit 1; }\ntmp=\"$(mktemp -d)\"; trap 'rm -rf \"$tmp\"' EXIT\ncd \"$tmp\" || fail \"cannot cd into the scratch repo\"\n\ncard=\"$(bash \"$SC\" open \"Seam 1 recording convention\" I52)\" || fail \"open exited non-zero\"\n[ -f \"$card\" ] || fail \"open did not print the path of a card it created\"\ncase \"$card\" in docs/sessions/*.md) ;; *) fail \"card is not under docs/sessions/: $card\" ;; esac\nfor k in session_card session token title status closed why next; do\n  grep -qE \"^$k:\" \"$card\" || fail \"frontmatter key '$k' missing from a fresh card\"\ndone\n[ \"$(grep -c '^status: open$' \"$card\")\" -eq 1 ] || fail \"a fresh card is not status: open\"\ngrep -qE '^token: I52$' \"$card\" || fail \"the token argument did not land in the frontmatter\"\ncase \"$card\" in *.I52.*) ;; *) fail \"the token did not land in the filename: $card\" ;; esac\ngrep -q '^## Log$' \"$card\" || fail \"a fresh card has no ## Log section\"\n\nbash \"$SC\" log \"ran tests/run.sh, 56 suites green\" || fail \"log exited non-zero\"\nbash \"$SC\" log \"second step\" || fail \"second log exited non-zero\"\n[ \"$(grep -cE '^- [0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z ' \"$card\")\" -eq 3 ] \\\n  || fail \"expected 3 log lines (open plus two logs), each '- <ISO ts> <text>'\"\ngrep -q 'ran tests/run.sh, 56 suites green' \"$card\" || fail \"log text did not land in the card\"\n\nbash \"$SC\" close bogus \"why\" \"next\" 2>/dev/null && fail \"close accepted a status outside the closed vocabulary\"\ngrep -qE '^status: open$' \"$card\" || fail \"a rejected close still mutated the card\"\nbash \"$SC\" close handed-off \"quota ended the session\" \"run task 4\" || fail \"close exited non-zero\"\ngrep -qE '^status: handed-off$' \"$card\" || fail \"close did not write the terminal status\"\ngrep -qE '^closed: [0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$' \"$card\" \\\n  || fail \"close did not stamp closed: with an ISO-8601 UTC timestamp\"\ngrep -qE '^why: quota ended the session$' \"$card\" || fail \"close did not record why\"\ngrep -qE '^next: run task 4$' \"$card\" || fail \"close did not record next\"\n[ \"$(grep -c '^status:' \"$card\")\" -eq 1 ] || fail \"close duplicated the status key\"\nbash \"$SC\" log \"after close\" 2>/dev/null && fail \"log accepted an entry on a closed card\"\n\n# collision: a second open the same day with the same slug takes the -2 suffix, never overwrites\ncard2=\"$(bash \"$SC\" open \"Seam 1 recording convention\" I52)\" || fail \"second open exited non-zero\"\n[ \"$card2\" != \"$card\" ] || fail \"second open reused the first card's path\"\ncase \"$card2\" in *-2.md) ;; *) fail \"collision did not take the -2 suffix: $card2\" ;; esac\ngrep -qE '^status: handed-off$' \"$card\" || fail \"second open mutated the first card\"\nbash \"$SC\" close parked \"collision probe done\" \"none\" || fail \"close of the collision card exited non-zero\"\ngrep -qE '^status: parked$' \"$card2\" || fail \"close did not hit the collision card\"\n\n# every remaining terminal status is accepted, one open card at a time so each close is unambiguous\nfor s in done blocked aborted; do\n  c=\"$(bash \"$SC\" open \"status probe $s\")\" || fail \"open for status $s exited non-zero\"\n  bash \"$SC\" close \"$s\" \"probe\" \"none\" || fail \"close rejected the valid status '$s'\"\n  grep -qE \"^status: $s\\$\" \"$c\" || fail \"status '$s' did not land in the card\"\ndone\n\n# same-second tie: two open cards with identical session: timestamps make log/close refuse, never guess\nca=\"$(bash \"$SC\" open \"tie probe\")\" || fail \"tie open a exited non-zero\"\ncb=\"$(bash \"$SC\" open \"tie probe\")\" || fail \"tie open b exited non-zero\"\ncase \"$cb\" in *-2.md) ;; *) fail \"tie probe b did not collide into -2: $cb\" ;; esac\nts_a=\"$(grep -E '^session:' \"$ca\")\"\nawk -v k=\"$ts_a\" '{sub(/^session:.*/, k)} 1' \"$cb\" > \"$cb.tmp\" && mv \"$cb.tmp\" \"$cb\"   # force the exact tie\nbash \"$SC\" close done \"tie\" \"none\" 2>/dev/null\n[ $? -eq 7 ] || fail \"close must exit 7 on a same-second open-card tie instead of picking one at random\"\ngrep -qE '^status: open$' \"$ca\" && grep -qE '^status: open$' \"$cb\" || fail \"a refused close still mutated a card\"\n\ngrep -qiE 'claude|anthropic|opencode|ringer' \"$card2\" && fail \"a card field names a specific agent harness\"\necho \"PASS: open/log/close lifecycle, closed status vocabulary, -2 collision suffix, harness-agnostic\"\n```\n\n- [ ] Step 2: Run it - `bash tests/sessions/session-card.sh`, expected FAIL with a missing-script error on `scripts/session-card.sh`.\n- [ ] Step 3: Implement `scripts/session-card.sh` to the Interfaces contract and the Global-constraints card format; `set -uo pipefail`, `export LC_ALL=C`, bash 3.2 constructs only, `chmod +x`.\n- [ ] Step 4: Add one line to the `ALLOW` array in `tests/hardcodes/sweep.sh`: `docs/sessions/` with a short trailing comment matching the array's existing style.\n- [ ] Step 5: Run it - `bash tests/sessions/session-card.sh`, expected PASS; then `bash tests/hardcodes/sweep.sh`, still PASS.\n\n--- OUTPUT CONTRACT ---\nEnd your run by printing exactly one JSON object on its own line:\n{\"unit\": \"t1-session-card\", \"files_changed\": [...], \"tests_passed\": <n>, \"tests_failed\": <n>, \"deviations\": [...], \"open_questions\": [...], \"deferred_items\": [...]}\nLeave everything uncommitted. The harness exports the patch.",
      "check": "set -uo pipefail\nexport LC_ALL=C\nPATCH=/Users/jjrdar/.ringer/work/i52-seam1-recording-convention/patches/t1-session-card.patch\nmkdir -p \"$(dirname \"$PATCH\")\"\n\n# acceptance-test custody: canonical overwrite\nmkdir -p \"$(dirname tests/sessions/session-card.sh)\"\ncat > tests/sessions/session-card.sh <<'CANONICAL_TEST_EOF'\n#!/usr/bin/env bash\n# session-card.sh open creates one card carrying the frontmatter key set, log appends timestamped\n# ## Log lines, close writes a terminal status from the closed vocabulary and rejects anything else,\n# a closed card refuses further logs, a same-day same-slug open takes the -2 suffix, and no field\n# names a specific agent harness.\nset -uo pipefail\nHERE=\"$(cd \"$(dirname \"$0\")\" && pwd)\"\nREPO=\"$(cd \"$HERE/../..\" && pwd)\"\nSC=\"$REPO/scripts/session-card.sh\"\nfail() { echo \"FAIL: $1\" >&2; exit 1; }\ntmp=\"$(mktemp -d)\"; trap 'rm -rf \"$tmp\"' EXIT\ncd \"$tmp\" || fail \"cannot cd into the scratch repo\"\n\ncard=\"$(bash \"$SC\" open \"Seam 1 recording convention\" I52)\" || fail \"open exited non-zero\"\n[ -f \"$card\" ] || fail \"open did not print the path of a card it created\"\ncase \"$card\" in docs/sessions/*.md) ;; *) fail \"card is not under docs/sessions/: $card\" ;; esac\nfor k in session_card session token title status closed why next; do\n  grep -qE \"^$k:\" \"$card\" || fail \"frontmatter key '$k' missing from a fresh card\"\ndone\n[ \"$(grep -c '^status: open$' \"$card\")\" -eq 1 ] || fail \"a fresh card is not status: open\"\ngrep -qE '^token: I52$' \"$card\" || fail \"the token argument did not land in the frontmatter\"\ncase \"$card\" in *.I52.*) ;; *) fail \"the token did not land in the filename: $card\" ;; esac\ngrep -q '^## Log$' \"$card\" || fail \"a fresh card has no ## Log section\"\n\nbash \"$SC\" log \"ran tests/run.sh, 56 suites green\" || fail \"log exited non-zero\"\nbash \"$SC\" log \"second step\" || fail \"second log exited non-zero\"\n[ \"$(grep -cE '^- [0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z ' \"$card\")\" -eq 3 ] \\\n  || fail \"expected 3 log lines (open plus two logs), each '- <ISO ts> <text>'\"\ngrep -q 'ran tests/run.sh, 56 suites green' \"$card\" || fail \"log text did not land in the card\"\n\nbash \"$SC\" close bogus \"why\" \"next\" 2>/dev/null && fail \"close accepted a status outside the closed vocabulary\"\ngrep -qE '^status: open$' \"$card\" || fail \"a rejected close still mutated the card\"\nbash \"$SC\" close handed-off \"quota ended the session\" \"run task 4\" || fail \"close exited non-zero\"\ngrep -qE '^status: handed-off$' \"$card\" || fail \"close did not write the terminal status\"\ngrep -qE '^closed: [0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$' \"$card\" \\\n  || fail \"close did not stamp closed: with an ISO-8601 UTC timestamp\"\ngrep -qE '^why: quota ended the session$' \"$card\" || fail \"close did not record why\"\ngrep -qE '^next: run task 4$' \"$card\" || fail \"close did not record next\"\n[ \"$(grep -c '^status:' \"$card\")\" -eq 1 ] || fail \"close duplicated the status key\"\nbash \"$SC\" log \"after close\" 2>/dev/null && fail \"log accepted an entry on a closed card\"\n\n# collision: a second open the same day with the same slug takes the -2 suffix, never overwrites\ncard2=\"$(bash \"$SC\" open \"Seam 1 recording convention\" I52)\" || fail \"second open exited non-zero\"\n[ \"$card2\" != \"$card\" ] || fail \"second open reused the first card's path\"\ncase \"$card2\" in *-2.md) ;; *) fail \"collision did not take the -2 suffix: $card2\" ;; esac\ngrep -qE '^status: handed-off$' \"$card\" || fail \"second open mutated the first card\"\nbash \"$SC\" close parked \"collision probe done\" \"none\" || fail \"close of the collision card exited non-zero\"\ngrep -qE '^status: parked$' \"$card2\" || fail \"close did not hit the collision card\"\n\n# every remaining terminal status is accepted, one open card at a time so each close is unambiguous\nfor s in done blocked aborted; do\n  c=\"$(bash \"$SC\" open \"status probe $s\")\" || fail \"open for status $s exited non-zero\"\n  bash \"$SC\" close \"$s\" \"probe\" \"none\" || fail \"close rejected the valid status '$s'\"\n  grep -qE \"^status: $s\\$\" \"$c\" || fail \"status '$s' did not land in the card\"\ndone\n\n# same-second tie: two open cards with identical session: timestamps make log/close refuse, never guess\nca=\"$(bash \"$SC\" open \"tie probe\")\" || fail \"tie open a exited non-zero\"\ncb=\"$(bash \"$SC\" open \"tie probe\")\" || fail \"tie open b exited non-zero\"\ncase \"$cb\" in *-2.md) ;; *) fail \"tie probe b did not collide into -2: $cb\" ;; esac\nts_a=\"$(grep -E '^session:' \"$ca\")\"\nawk -v k=\"$ts_a\" '{sub(/^session:.*/, k)} 1' \"$cb\" > \"$cb.tmp\" && mv \"$cb.tmp\" \"$cb\"   # force the exact tie\nbash \"$SC\" close done \"tie\" \"none\" 2>/dev/null\n[ $? -eq 7 ] || fail \"close must exit 7 on a same-second open-card tie instead of picking one at random\"\ngrep -qE '^status: open$' \"$ca\" && grep -qE '^status: open$' \"$cb\" || fail \"a refused close still mutated a card\"\n\ngrep -qiE 'claude|anthropic|opencode|ringer' \"$card2\" && fail \"a card field names a specific agent harness\"\necho \"PASS: open/log/close lifecycle, closed status vocabulary, -2 collision suffix, harness-agnostic\"\nCANONICAL_TEST_EOF\n\n# run the acceptance test suite(s), printing everything\nbash tests/sessions/session-card.sh; rc=$?\n[ \"$rc\" -eq 0 ] || { echo \"CHECK FAIL: acceptance test tests/sessions/session-card.sh exited $rc; its output above names the failed assertion\"; exit 1; }\n\n# scoped regression gate (this task's suites only)\nbash tests/sessions/session-card.sh && bash tests/hardcodes/sweep.sh\nrc=$?\n[ \"$rc\" -eq 0 ] || { echo \"CHECK FAIL: the scoped regression gate exited $rc; a suite this task can affect regressed\"; exit 1; }\n\n# ownership audit\ngit add -A\nextra=\"$(git diff --cached --name-only | grep -vE '^(scripts/session\\-card\\.sh|tests/sessions/session\\-card\\.sh|tests/hardcodes/sweep\\.sh)$' || true)\"\n[ -z \"$extra\" ] || { echo \"CHECK FAIL: files outside the ownership list were changed:\"; printf '%s\\n' \"$extra\"; exit 1; }\n\n# portability audit on changed shell files (comment lines excluded: header comments\n# legitimately NAME the banned constructs, as board-render-obsidian.sh line 9 does)\nbad=\"$(git diff --cached --name-only | grep -E '\\.sh$' | xargs grep -nE 'declare -A|\\$\\{[A-Za-z_]+,,\\}|grep -P' 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*#' || true)\"\n[ -z \"$bad\" ] || { echo \"CHECK FAIL: bash 3.2 portability violation (associative array, lowercase expansion, or grep -P):\"; printf '%s\\n' \"$bad\"; exit 1; }\n\n# export the patch outside the worktree\ngit diff --cached > \"$PATCH\"\n[ -s \"$PATCH\" ] || { echo \"CHECK FAIL: exported patch $PATCH is empty; the worker produced no applicable work\"; exit 1; }\necho \"CHECK PASS: The canonical session-card lifecycle suite passes: open/log/close, the closed status vocabulary, the -2 collision suffix, the same-second tie exit 7, and harness-agnostic fields; the hardcodes sweep still passes with docs/sessions/ allowed.\"",
      "expect_files": [
        "/Users/jjrdar/.ringer/work/i52-seam1-recording-convention/patches/t1-session-card.patch"
      ],
      "verified": "The canonical session-card lifecycle suite passes: open/log/close, the closed status vocabulary, the -2 collision suffix, the same-second tie exit 7, and harness-agnostic fields; the hardcodes sweep still passes with docs/sessions/ allowed."
    },
    {
      "key": "t2-handoff-log",
      "engine": "claude-zai",
      "model": "glm-5.2",
      "task_type": "code-feature",
      "timeout_s": 3600,
      "spec": "You are a code-feature worker (test-first) implementing ONE unit of the loop-stack seam 1 recording convention.\nYour current working directory IS a git worktree of the loop-stack-session repo. Edit files here directly.\nDo NOT push. Do NOT run git commit at any point: leave every change uncommitted in the worktree.\nThe harness exports your diff as a patch and the orchestrator applies and commits it. Any commit you make dies with this worktree.\nThe embedded task text below may end with steps that mention committing; those are stripped, and if any commit instruction survives anywhere in this spec, ignore it.\n\nYou own ONLY these paths. Do not create, edit, or delete anything else, anywhere:\n- scripts/handoff-log.sh\n- scripts/lifecycle-lint.sh\n- config/repo-state.md\n- tests/sessions/handoff-lifecycle.sh\n\nNever touch docs/handoffs/, docs/archive/, docs/reviews/, docs/sessions/, or any repo outside this one. The check audits this mechanically and fails on any path outside the list above.\n\nPORTABILITY (hard constraint): target macOS stock /bin/bash 3.2. No associative arrays, no ${var,,}, no grep -P, no GNU-only flags. `set -uo pipefail` and `export LC_ALL=C` at the top of every new script, BSD `date` first with a GNU `date` fallback, tab-separated pipelines. New scripts get `chmod +x`.\n\nHOW TO RUN YOUR OWN CHECK: for t in tests/sessions/*.sh tests/loop-setup/*.sh; do bash \"$t\" || exit 1; done. Do NOT run the full tests/run.sh unless that command includes it; a broader run would trip over a parallel neighbour's in-flight changes. The full suite runs at the wave gate.\n\nACCEPTANCE TEST CUSTODY: The acceptance test tests/sessions/handoff-lifecycle.sh is FIXED and AUTHORITATIVE. Write it first, verbatim from your task's Step 1 block, exactly as the steps say. The harness then OVERWRITES your copy with the canonical text and runs that. You cannot pass by editing the test.\n\nWork test-first, in the order the task's numbered steps give. If anything is ambiguous, take the most conservative reading, record the question in your final output, and proceed; you cannot ask.\n\n--- SOURCE PLAN, SHARED CONTRACT (verbatim) ---\n# Seam 1: Recording Convention Implementation Plan\n\n> For executors: tasks use checkbox syntax; execute in dependency order; a task is done when its\n> acceptance check passes. No specific tooling, harness, or skills are assumed.\n\n**Goal:** every work session in a conforming repo leaves one durable, hand-editable session card, handoff consumption becomes an append-only per-action transition log that auto-archives, and the three empty board lanes populate from those records.\n\n**Approach:** doctrine plus enforcement, not a daemon.\nThree small shell scripts write the judgment facts (`session-card.sh`, `handoff-log.sh`) and derive everything else (`take-stock.sh`); `lifecycle-lint.sh` grows a class h behind an adoption-date gate, and `board-cards.sh` reads the derived rows into the empty lanes.\nRollout is piloted on this repo and `~/create/sys-prompts-cc`, proven against the sys-prompts-cc handoff case, then propagated by the template-version roll.\n\n**Tech stack:** bash, awk, plain markdown; no new dependencies.\n\n**Source brief:** `docs/briefs/2026-09-07.I52.seam1-recording-convention-brief.md`\n\n## Settled decisions (pinned by the brief, not reopened here)\n\n| Decision                      | Settled value                                                            |\n| ---                           | ---                                                                      |\n| Session terminal statuses     | closed set of 5: `done`, `blocked`, `handed-off`, `parked`, `aborted`    |\n| Died-mid-work                 | an open card with no close, by construction; no status for it            |\n| Write path                    | `scripts/session-card.sh open \\| log \\| close` plus conventions doctrine |\n| Card home                     | `docs/sessions/`, one card per session, updated in place                 |\n| Filename collision            | `-2` suffix on the slug                                                  |\n| Lint class f                  | `docs/sessions/` exempt (class f scans four lanes, sessions is not one)  |\n| Done-lane closed-issue window | 60 days, a visible constant in `board-cards.sh`                          |\n| Handoff dispositions          | closed set `executed \\| superseded`, appended to a transition log        |\n| Current state                 | always derived from the log, never a mutable status field                |\n| Lint class h                  | consumed-unarchived handoffs, exit non-zero                              |\n| Class h gate                  | `lifecycle-lint-since:` in `config/repo-state.md`, on-or-after only      |\n| Adoption date                 | the date this run ships the lint class, no template placeholder          |\n| Legacy backfill               | the 20 pre-adoption handoffs, human-confirmed table, then archived       |\n| Died-mid-work threshold       | 24 hours, a hardcoded visible constant                                   |\n| take-stock                    | a new script rebuilding recorded state from git, handoffs, the tracker   |\n| Template roll                 | `template-version` 6 to 7, carrying the rubix-autorun drift fix          |\n| Pilot pair                    | this repo and `~/create/sys-prompts-cc`                                  |\n\n## Plan-owned design calls (decided here, with reversal paths)\n\n**D9, went-stale as card vs flag: flag, carried on a 13th Card TSV field `marker`.**\nA went-stale repo has no work item to be a card of, and the shipped renderer has no non-card row concept, so a went-stale card would need a lane it does not have.\nThe Card TSV grows one field, `marker`, with values empty, `went-stale`, or `died-mid-work`; `went-stale` rides the repo's git card and `died-mid-work` rides the open session card.\nA went-stale repo always gets its git card, so the existing clean-conforming suppression rule gains one exception.\nReversal: emit a separate card row with `source=state` and drop field 13, changing `board-cards.sh` and the two board tests only.\n\n**D13, session card key set and `## Log` format.**\nFrontmatter, eight keys, each mapping onto a Card TSV field: `session_card`, `session`, `token`, `title`, `status`, `closed`, `why`, `next`.\n`## Log` is append-only, one entry per line, `- <ISO-8601 UTC timestamp> <free text>`.\nReversal: add a key to `session-card.sh`'s emitter and to `take-stock.sh`'s reader; existing cards keep parsing because every reader greps line-anchored keys.\n\n**D20, take-stock invocation surface: `scripts/take-stock.sh <repo-root> [--cards]`.**\nSingle positional argument matching `lifecycle-lint.sh`'s shape; default output is the human recorded-state report, `--cards` emits the 7-field intermediate TSV that `board-cards.sh` consumes.\nReversal: split `--cards` into a second script; the intermediate TSV contract is unchanged either way.\n\n**Went-stale comparison granularity: calendar day, not seconds.**\nA commit made seconds after recording is not stale; a repo is went-stale when its newest commit's `YYYY-MM-DD` is strictly later than its recorded-state date.\nReversal: switch to an hour threshold beside `DIED_MID_WORK_HOURS` in `take-stock.sh`.\n\n**Session status to board lane mapping.**\n`open` to `in-session`, `blocked` to `blocked-on-fact`, `handed-off` to `handed-off`, `done` to `done`, `aborted` to `done`, `parked` to `backlog`.\n`aborted` lands in `done` because it is terminal and not resumable; the card's `why` says it was aborted.\nReversal: one `case` arm in `board-cards.sh`.\n\n## Global constraints\n\nHost of record for all live verification: Jeremy's Mac; this repo is `/Users/jjrdar/create/loops/loop-stack-session`.\nSession terminal status vocabulary, closed and exact: `done`, `blocked`, `handed-off`, `parked`, `aborted`; anything else is rejected by `session-card.sh close`.\nHandoff disposition vocabulary, closed and exact: `executed`, `superseded`; anything else is rejected by `handoff-log.sh disposition`.\nDone-lane closed-issue lookback: 60 days, declared as `DONE_LOOKBACK_DAYS=60` at the top of `scripts/board-cards.sh`.\nDied-mid-work threshold: 24 hours, declared as `DIED_MID_WORK_HOURS=24` at the top of `scripts/take-stock.sh`.\nHarness-agnostic rule: no session-card or transition-log field names or assumes a particular agent harness; the cards are plain markdown any harness or human can write and read.\nVerbose-announce archive rule 5: every automatic handoff archive prints `moved <src> to <dest>` on stdout, one line per moved file.\nRecording tax cap: exactly one session card per session, updated in place; anything derivable comes from `take-stock.sh`, never from hand-recording.\nEvery handoff written on or after adoption carries a `## Next actions` section, one action per `- ` line; that list is the enumeration dispositions index into.\nLint baseline, measured 2026-09-07 before any task ran, so every \"lint is clean\" check below is baseline-relative rather than a bare exit 0.\n`scripts/lifecycle-lint.sh .` currently exits 1 with exactly four findings: class a on `docs/plans/2026-08-29.I28.import-gate-diagnosis-plan.md`, class a on the two `docs/plans/2026-09-02.I52.obsidian-board-mvp-plan{,_loop}.md` files, and class f on `docs/handoffs/2026-09-01.I56-architecture-research-home.md`.\nThe `#28` class a is a known false positive: issue #28 is open and covers that plan, but remote backends match issue titles only and #28's title does not contain the stem.\nTask 6 clears the class f finding by archiving that handoff; the class a findings are a lifecycle decision put to the human at Task 6's checkpoint, never auto-taken.\nPortability: macOS stock `/bin/bash` 3.2, no associative arrays, no `${var,,}`, no `grep -P`, no GNU-only flags; `set -uo pipefail` and `export LC_ALL=C` at the top of every new script, BSD `date` first with a GNU `date` fallback, tab-separated pipelines, and the repo's no-jq brace-depth JSON scan.\nTests are plain bash scripts under `tests/`, exit non-zero on failure, print one `PASS:` line on success, build fixtures under `mktemp -d`, and never touch the real Cortex vault, the real scan roots, or any repo outside their scratch directory.\nHouse style: plain dash never the em-dash character, one sentence per physical line in prose, aligned pipe tables no wider than 110 characters, plain commit messages with no co-author line.\n\n**Session card format** (the single canonical definition; Task 1 emits it, Task 3 reads it):\n\n```\n---\nsession_card: true\nsession: <ISO-8601 UTC, when the card was opened>\ntoken: <I52 | B7 | empty>\ntitle: <one line, what this session is>\nstatus: <open | done | blocked | handed-off | parked | aborted>\nclosed: <ISO-8601 UTC, empty while open>\nwhy: <one line, the reason for the terminal status, empty while open>\nnext: <one line, the next action>\n---\n\n# <title>\n\n## Log\n\n- <ISO-8601 UTC> opened\n- <ISO-8601 UTC> <free text, one line per meaningful step>\n```\n\nPath: `docs/sessions/<YYYY-MM-DD>.<tokens>.<slug>.md`, slug from the title lowercased with every run of non-alphanumerics collapsed to a dash; on collision the slug takes a `-2` suffix, then `-3`, and so on.\n`log` and `close` target the newest card whose `status:` is `open`, resolved by the `session:` timestamp; with no open card both exit non-zero (exit 3), and with two open cards tied to the second they exit non-zero (exit 7) naming the tied files, because a silent pick between same-second cards would target one at random (the doctrine is one card per session, so a tie is already an anomaly worth surfacing).\n\n**Handoff transition log** (the single canonical definition; Task 2 writes it, Task 2 and Task 3 read it):\n\n```\n## Transitions\n\n- <ISO-8601 UTC> action <n> <executed|superseded> - <one-line note>\n```\n\nAppended to the handoff file itself, append-only, never rewritten.\nThe actions section is any `## ` heading whose text starts with `Next actions` (prefix match, so `## Next actions, in recommended order` counts); enumerable actions are the lines beginning `- ` or `<n>. ` inside it, numbered 1..N by position.\nThis matches the real corpus: the three sys-prompts-cc handoffs all carry numbered `1.` items under prefix-variant headings.\nDerived state: `live` when the section is absent, when it parses to zero actions with no bulk line, or when any parsed action has no disposition line; `consumed` when every parsed action has one (or a bulk `action 0` line stands in for a zero-action file) and the file sits in `docs/archive/`; `consumed-unarchived` in the same condition with the file still in `docs/handoffs/`.\nThe bulk `action 0` line is the legacy-backfill shape for files whose actions cannot be enumerated; it is written by `handoff-log.sh disposition <handoff> all <d>` on a zero-action file, and on a file with parsed actions the same verb appends one line per action 1..N instead.\n\n**take-stock intermediate TSV** (`take-stock.sh <root> --cards`, tab-separated, no header, 7 fields):\n\n| # | Field           | Meaning                                                             |\n| - | ---             | ---                                                                 |\n| 1 | `kind`          | `repo`, `session`, or `handoff`                                     |\n| 2 | `ref`           | repo-relative path of the card or handoff, empty on the `repo` row  |\n| 3 | `status`        | session status, or handoff derived state, empty on the `repo` row   |\n| 4 | `token`         | doc-grammar token (`I52`), or empty                                 |\n| 5 | `title`         | one-line title, tabs and newlines collapsed to spaces               |\n| 6 | `last_activity` | `YYYY-MM-DD` of the newest recorded activity for that row           |\n| 7 | `marker`        | `went-stale` (repo row), `died-mid-work` (session row), or empty    |\n\nExactly one `repo` row is emitted, always first.\nOne `session` row per file in `docs/sessions/`, one `handoff` row per handoff whose derived state is `live`.\n\n**Card TSV field 13** (extends the shipped 12-field contract in\n`docs/plans/2026-09-02.I52.obsidian-board-mvp-plan.md`):\n\n| #  | Field    | Meaning                                            |\n| -- | ---      | ---                                                |\n| 13 | `marker` | empty, `went-stale`, or `died-mid-work`            |\n\n**Card sources and ids** (`board-cards.sh`, after Task 5):\n\n| Source    | card_id                        | Column                                    |\n| ---       | ---                            | ---                                       |\n| tracker   | `<repo_key>#<token>`           | from the `agent:` label, as shipped       |\n| git       | `<repo_key>#git`               | `next-up`                                 |\n| session   | `<repo_key>#s-<file stem>`     | from `status`, per the mapping above      |\n| handoff   | `<repo_key>#h-<file stem>`     | `handed-off`                              |\n| closed    | `<repo_key>#<token>`           | `done`                                    |\n\n## Dependency graph\n\n```\nWave 1:  Task 1 (session-card.sh)   Task 2 (handoff-log.sh + lint h)   Task 4 (Card TSV field 13)\nWave 2:  Task 3 (take-stock.sh)      depends on 1, 2\n         Task 6 (legacy backfill)    depends on 2\nWave 3:  Task 5 (board wiring)       depends on 3, 4\n         Task 8 (sys-prompts-cc pilot) depends on 2, 3\nWave 4:  Task 7 (dogfood this repo)  depends on 3, 5, 6\n         Task 9 (template roll v7)   depends on 1, 2, 3, 8\nWave 5:  Task 10 (propagation + live board) depends on 5, 7, 8, 9\n```\n\nWave 1's three tasks own disjoint files and parallelize.\nTask 4 and Task 5 both own `scripts/board-cards.sh` and `tests/board/cards.sh`, so they are strictly ordered; between them the Card TSV producer and consumer agree at 13 fields and nothing is broken.\nTask 7 and Task 9 own disjoint files (session cards in this repo versus the templates and `setup.sh`) and parallelize.\nIn-task regression gates are scoped to the suites the task can affect, so a parallel neighbor's in-flight changes never block a task on damage it did not cause; the full `tests/run.sh` runs once at each wave boundary before the next wave starts, and at Task 10 Step 1.\n\n## Human checkpoints\n\n- **After Task 6, before the archive moves.** The executor presents the pre-adoption handoffs as a table (file, date, proposed disposition, one-line reason) and stops. Jeremy confirms or edits the dispositions; only then do the archive moves run, verbose-announced. This is the brief's one-time bulk backfill under human confirmation. A second, separate prompt (Task 6 Step 3b) then asks the unrelated MVP plan-set archive question on its own; one decision per prompt.\n- **Before Task 10's propagation step.** Rolling template-version 7 into seven repos outside this one writes to repos the run does not own. The executor prints the target list with each repo's current template-version and working-tree state and stops for an explicit go naming which repos roll; a dirty tree blocks that repo's roll.\n- **After Task 10, brief criterion 7 `[judgment]`.** After one real work session under the convention, Jeremy answers two questions: did the recording tax feel acceptable, and does the card answer \"where did I leave off\" without reconstruction. Record the verdict in the run's session card `why:` line. Criteria 1 through 6 cover the mechanics; this call stays human.\n\n## How to run\n\nAll commands run from this repo's root, `/Users/jjrdar/create/loops/loop-stack-session`.\n\nFull test suite (globs `tests/*/*.sh`, so the new `tests/sessions/` directory is picked up with no wiring):\n\n```\ntests/run.sh\n```\n\nA single suite:\n\n```\nbash tests/sessions/session-card.sh\n```\n\nRecorded state and lint for any repo:\n\n```\nscripts/take-stock.sh .\nscripts/lifecycle-lint.sh .\n```\n\nBoard render (the environment the shipped MVP already takes):\n\n```\nexport LOOP_BOARD_CORTEX=/Users/jjrdar/Documents/_CS_DOCUMENTS_/Obsidian/Cortex\nexport LOOP_BOARD_HOME=\"$LOOP_BOARD_CORTEX/00_System/Board\"\nscripts/board.sh\n```\n\nThe two `export` lines are separate statements on purpose: a single-command environment prefix does not expand `LOOP_BOARD_CORTEX` for the assignment that follows it on the same line.\n\n---\n\n--- SOURCE PLAN, YOUR TASK (verbatim) ---\n### Task 2: `scripts/handoff-log.sh` and `lifecycle-lint` class h\n\nDepends on: none.\n\n**Files (exclusive ownership):**\n- Create: `scripts/handoff-log.sh`\n- Modify: `scripts/lifecycle-lint.sh` (header class list at lines 3 to 24; new class h block inserted after the class g block that ends at line 242, before `exit \"$found\"`)\n- Modify: `config/repo-state.md` (add one `lifecycle-lint-since:` line under `filename-grammar-since:` at line 8)\n- Test: `tests/sessions/handoff-lifecycle.sh`\n\n**Interfaces:**\n- Consumes: a handoff file path as its argument; `config/repo-state.md`'s `lifecycle-lint-since:` key, read by `lifecycle-lint.sh` with its existing `fm()` helper.\n- Produces: four subcommands plus one lint class.\n  - `handoff-log.sh actions <handoff>` prints one row per action, `<n>\\t<action text>\\t<disposition or ->`; exit 0, exit 0 with no output when the file has no actions section or it parses to zero actions. The section heading matches by prefix (`## Next actions`), and both `- ` and `<n>. ` items enumerate, per the Global-constraints definition.\n  - `handoff-log.sh disposition <handoff> <n> <executed|superseded> [note]` appends a transition line; when that disposition completes the set, it archives the file to `docs/archive/` and prints `moved <src> to <dest>`; exit 0. Paths in the announcement print exactly as given on the command line, never relativized or resolved.\n  - `handoff-log.sh disposition <handoff> all <executed|superseded> [note]` is the bulk form: one transition line per action 1..N with the same disposition and note, or the single `action 0` bulk line when the file parses to zero actions; archival and announcement as above; exit 0.\n  - `handoff-log.sh consume <handoff>` archives and announces when every parsed action has a disposition; otherwise prints `handoff-log: cannot consume <path>: <n> action(s) have no disposition` on stderr and exits 5. A file parsing to zero actions is also refused (exit 5) - the vacuous case never consumes; only `disposition <handoff> all` handles legacy zero-action files, via the `action 0` bulk line.\n  - `handoff-log.sh state <handoff>` prints exactly one of `live`, `consumed`, `consumed-unarchived`; exit 0.\n- `lifecycle-lint.sh` gains class h: for each `docs/handoffs/*.md` dated on or after `lifecycle-lint-since:`, a derived state of `consumed-unarchived` emits `LINT h <path>: <why>` and forces exit 1. With the key absent, class h does not run at all.\n\nBehavior notes, each edge named:\n- Action enumeration: the lines beginning `- ` or `<n>. ` inside the actions section (heading prefix-matched on `## Next actions`), numbered 1..N by position; the section ends at the next `## ` heading or end of file.\n- A handoff with no actions section, or one parsing to zero actions and carrying no bulk line, derives `live`; `disposition <handoff> all <d>` is the only path that consumes a zero-action file, via the `action 0` bulk line.\n- Disposition vocabulary is the exact two-word list, validated by a `case` statement; anything else exits 4.\n- `<n>` outside 1..N (and not the literal `all`) exits 4.\n- Re-dispositioning an action appends another line rather than editing the earlier one; the latest line for an action wins when state is derived, and the log is never rewritten.\n- The `## Transitions` section is created on first disposition, appended at end of file, and is the only section the script writes; no `status:` key is ever added to a handoff.\n- Archive destination is the given handoff path with its `docs/handoffs/` segment replaced by `docs/archive/` (so an absolute path in one repo archives inside that same repo, and a path without a `docs/handoffs/` segment exits 2); the destination directory is created when absent. If the destination file already exists it takes a `-2` suffix before the `.md` (then `-3`, and so on), matching the session-card collision convention, so a fully dispositioned handoff can never strand in the lint-failing `consumed-unarchived` state; nothing is ever overwritten.\n- Archiving uses `git mv` when the file is tracked, plain `mv` otherwise, matching `setup.sh`'s `archive_offer`.\n- The note argument is optional; free text passes through `tr '\\t\\n' '  '` so a transition line is always one line.\n- `lifecycle-lint.sh` calls `\"$SCRIPT_DIR/handoff-log.sh\" state \"$f\"`, the same script-relative pattern it already uses for `TRACKER`, so the derivation has exactly one home.\n- Class h reuses the existing `date_of` helper and the lexical ISO comparison class f already uses (`[[ \"$d\" < \"$SINCE\" ]] && continue`).\n\n**Acceptance check:** `bash tests/sessions/handoff-lifecycle.sh` exits 0 `[executed-check]`.\n\n- [ ] Step 1: Write the failing test `tests/sessions/handoff-lifecycle.sh` verbatim:\n\n```\n#!/usr/bin/env bash\n# handoff-log.sh enumerates a handoff's actions, refuses to consume while any action lacks a\n# disposition, appends dispositions to an append-only ## Transitions log rather than mutating a\n# status field, archives automatically and announces when the last disposition lands, and\n# lifecycle-lint class h flags a consumed-unarchived handoff only on or after lifecycle-lint-since.\nset -uo pipefail\nHERE=\"$(cd \"$(dirname \"$0\")\" && pwd)\"\nREPO=\"$(cd \"$HERE/../..\" && pwd)\"\nHL=\"$REPO/scripts/handoff-log.sh\"\nLINT=\"$REPO/scripts/lifecycle-lint.sh\"\nfail() { echo \"FAIL: $1\" >&2; exit 1; }\ntmp=\"$(mktemp -d)\"; trap 'rm -rf \"$tmp\"' EXIT\nmkdir -p \"$tmp/docs/handoffs\" \"$tmp/docs/archive\" \"$tmp/config\"\nprintf 'template-version: 7\\nlifecycle-lint-since: 2026-09-08\\n' > \"$tmp/config/repo-state.md\"\nh=\"$tmp/docs/handoffs/2026-09-10.I52.example-handoff.md\"\ncat > \"$h\" <<'EOF'\n# Handoff: example (#52)\n\n## Next actions\n\n- Run the seam 1 plan.\n- Roll template-version 7.\n\n## Notes\n\n- Not an action, a different section.\nEOF\n\n[ \"$(bash \"$HL\" actions \"$h\" | grep -c .)\" -eq 2 ] \\\n  || fail \"actions must enumerate exactly the two ## Next actions entries, not the Notes bullet\"\n[ \"$(bash \"$HL\" state \"$h\")\" = live ] || fail \"a handoff with no dispositions is not live\"\nbash \"$HL\" consume \"$h\" 2>/dev/null && fail \"consume succeeded with no dispositions present\"\nbash \"$HL\" disposition \"$h\" 1 bogus \"x\" 2>/dev/null && fail \"disposition accepted a value outside executed|superseded\"\nbash \"$HL\" disposition \"$h\" 9 executed \"x\" 2>/dev/null && fail \"disposition accepted an out-of-range action number\"\n\nbash \"$HL\" disposition \"$h\" 1 executed \"seam 1 shipped\" || fail \"disposition 1 exited non-zero\"\ngrep -q '^## Transitions$' \"$h\" || fail \"disposition did not create a ## Transitions section\"\ngrep -qE '^- [0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z action 1 executed - seam 1 shipped$' \"$h\" \\\n  || fail \"the transition line is not '- <ISO ts> action <n> <disposition> - <note>'\"\ngrep -qE '^status:' \"$h\" && fail \"disposition mutated a status field instead of appending to the log\"\nbash \"$HL\" consume \"$h\" 2>/dev/null && fail \"consume succeeded with action 2 still undispositioned\"\n[ \"$(bash \"$HL\" state \"$h\")\" = live ] || fail \"a partially dispositioned handoff is not live\"\n[ \"$(bash \"$HL\" actions \"$h\" | awk -F'\\t' '$1==1{print $3}')\" = executed ] \\\n  || fail \"actions does not report the disposition it just recorded\"\n\nout=\"$(bash \"$HL\" disposition \"$h\" 2 superseded \"the seam 1 brief replaces it\")\" || fail \"disposition 2 exited non-zero\"\n[ -f \"$h\" ] && fail \"the last disposition did not archive the handoff automatically\"\na=\"$tmp/docs/archive/2026-09-10.I52.example-handoff.md\"\n[ -f \"$a\" ] || fail \"the archived handoff is not in docs/archive/\"\nprintf '%s\\n' \"$out\" | grep -q \"moved $tmp/docs/handoffs/2026-09-10.I52.example-handoff.md to $tmp/docs/archive/\" \\\n  || fail \"the automatic archive was not verbose-announced with the paths exactly as given (archive rule 5)\"\n[ \"$(bash \"$HL\" state \"$a\")\" = consumed ] || fail \"an archived, fully dispositioned handoff is not consumed\"\n[ \"$(grep -c '^- .* action ' \"$a\")\" -eq 2 ] || fail \"the transition log is not append-only with both entries\"\n\n# class h: a consumed-unarchived handoff dated on/after lifecycle-lint-since is flagged\ncp \"$a\" \"$tmp/docs/handoffs/2026-09-10.I52.example-handoff.md\"\n[ \"$(bash \"$HL\" state \"$tmp/docs/handoffs/2026-09-10.I52.example-handoff.md\")\" = consumed-unarchived ] \\\n  || fail \"a fully dispositioned handoff still in docs/handoffs/ is not consumed-unarchived\"\nbash \"$LINT\" \"$tmp\" > \"$tmp/lint.out\" 2>/dev/null && fail \"lint exited 0 with a consumed-unarchived handoff\"\ngrep -q '^LINT h docs/handoffs/2026-09-10.I52.example-handoff.md:' \"$tmp/lint.out\" \\\n  || fail \"class h did not flag the consumed-unarchived handoff\"\n\n# grandfathering: the same defect dated before lifecycle-lint-since is ignored\nmv \"$tmp/docs/handoffs/2026-09-10.I52.example-handoff.md\" \"$tmp/docs/handoffs/2026-09-01.I52.old-handoff.md\"\nbash \"$LINT\" \"$tmp\" > \"$tmp/lint2.out\" 2>/dev/null\ngrep -q '^LINT h ' \"$tmp/lint2.out\" && fail \"class h flagged a handoff dated before lifecycle-lint-since\"\n\n# absence of the key skips class h entirely (the same absence-skips pattern as class f)\nprintf 'template-version: 7\\n' > \"$tmp/config/repo-state.md\"\ncp \"$a\" \"$tmp/docs/handoffs/2026-09-10.I52.example-handoff.md\"\nbash \"$LINT\" \"$tmp\" > \"$tmp/lint3.out\" 2>/dev/null\ngrep -q '^LINT h ' \"$tmp/lint3.out\" && fail \"class h ran with no lifecycle-lint-since key present\"\n\n# a live handoff is never flagged\nrm -f \"$tmp/docs/handoffs\"/*.md\nprintf 'template-version: 7\\nlifecycle-lint-since: 2026-09-08\\n' > \"$tmp/config/repo-state.md\"\nprintf '# H\\n\\n## Next actions\\n\\n- one\\n' > \"$tmp/docs/handoffs/2026-09-11.I52.live-handoff.md\"\nbash \"$LINT\" \"$tmp\" > \"$tmp/lint4.out\" 2>/dev/null\ngrep -q '^LINT h ' \"$tmp/lint4.out\" && fail \"class h flagged a live handoff\"\n\n# the real corpus shape: a prefix-variant heading with numbered items enumerates, and `all` bulk-dispositions it\nh2=\"$tmp/docs/handoffs/2026-09-12.I52.numbered.md\"\nprintf '# H2\\n\\n## Next actions, in recommended order\\n\\n1. First numbered action.\\n2. Second numbered action.\\n\\n## Notes\\n\\n- not an action\\n' > \"$h2\"\n[ \"$(bash \"$HL\" actions \"$h2\" | grep -c .)\" -eq 2 ] \\\n  || fail \"a prefix-variant heading with numbered items must enumerate 2 actions\"\nbash \"$HL\" disposition \"$h2\" all superseded \"replaced by the next thread\" || fail \"bulk disposition exited non-zero\"\na2=\"$tmp/docs/archive/2026-09-12.I52.numbered.md\"\n[ -f \"$a2\" ] || fail \"bulk disposition of every action did not auto-archive\"\n[ \"$(grep -c '^- .* action [12] superseded' \"$a2\")\" -eq 2 ] || fail \"bulk form must write one line per action 1..N\"\n\n# zero parsed actions: live until the action 0 bulk line, never vacuously consumed\nh3=\"$tmp/docs/handoffs/2026-09-13.I52.proseonly.md\"\nprintf '# H3\\n\\n## Next actions\\n\\nProse only, nothing enumerable.\\n' > \"$h3\"\n[ \"$(bash \"$HL\" state \"$h3\")\" = live ] || fail \"a section parsing to zero actions must derive live, not vacuously consumed\"\nbash \"$HL\" consume \"$h3\" 2>/dev/null && fail \"consume must refuse a zero-action file instead of vacuously succeeding\"\nbash \"$LINT\" \"$tmp\" > \"$tmp/lint5.out\" 2>/dev/null\ngrep -q '^LINT h docs/handoffs/2026-09-13' \"$tmp/lint5.out\" && fail \"class h flagged a zero-action live handoff\"\nbash \"$HL\" disposition \"$h3\" all executed \"legacy shape\" || fail \"bulk disposition of a zero-action file exited non-zero\"\na3=\"$tmp/docs/archive/2026-09-13.I52.proseonly.md\"\n[ -f \"$a3\" ] || fail \"the zero-action bulk path did not archive\"\ngrep -qE '^- .* action 0 executed - legacy shape$' \"$a3\" || fail \"the zero-action bulk line is not the action 0 shape\"\n[ \"$(bash \"$HL\" state \"$a3\")\" = consumed ] || fail \"an archived action-0 file must derive consumed\"\n\n# archive-destination collision takes a -2 suffix instead of stranding consumed-unarchived\nh4=\"$tmp/docs/handoffs/2026-09-13.I52.proseonly.md\"\nprintf '# H4\\n\\n## Next actions\\n\\nAlso prose only.\\n' > \"$h4\"\nbash \"$HL\" disposition \"$h4\" all superseded \"collision probe\" || fail \"disposition into a colliding archive name exited non-zero\"\n[ -f \"$tmp/docs/archive/2026-09-13.I52.proseonly-2.md\" ] || fail \"archive collision did not take the -2 suffix\"\necho \"PASS: enumeration incl. numbered, consume rejected, append-only log, all-bulk, zero-action live, archive announced, -2 collision, class h -since gate\"\n```\n\n- [ ] Step 2: Run it - `bash tests/sessions/handoff-lifecycle.sh`, expected FAIL with a missing-script error on `scripts/handoff-log.sh`.\n- [ ] Step 3: Implement `scripts/handoff-log.sh` to the Interfaces contract, `chmod +x`.\n- [ ] Step 4: Add class h to `scripts/lifecycle-lint.sh`: extend the header comment class list with `h  consumed-unarchived handoff: every action in ## Next actions carries a disposition but the file still sits in docs/handoffs/; gated by lifecycle-lint-since:`, and insert the class block after class g and before `exit \"$found\"`:\n\n```\n# (h) consumed but unarchived handoff - runs only when the adopting key is present (same\n# absence-skips pattern as class f). Handoffs dated before lifecycle-lint-since: are grandfathered.\n# The derivation has one home: handoff-log.sh state, called script-relative like TRACKER above.\nHSINCE=\"\"\n[ -f config/repo-state.md ] && HSINCE=\"$(fm config/repo-state.md lifecycle-lint-since)\"\nif [ -n \"$HSINCE\" ] && [ -x \"$SCRIPT_DIR/handoff-log.sh\" ]; then\n  for f in docs/handoffs/*.md; do\n    [ -e \"$f\" ] || continue\n    d=\"$(date_of \"$(basename \"$f\")\")\"\n    case \"$d\" in\n      [0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]) ;;\n      *) continue ;;\n    esac\n    [[ \"$d\" < \"$HSINCE\" ]] && continue\n    [ \"$(\"$SCRIPT_DIR/handoff-log.sh\" state \"$f\")\" = consumed-unarchived ] || continue\n    lint h \"$f\" \"every action carries a disposition but the handoff is still live (archive it; lifecycle since $HSINCE)\"\n  done\nfi\n```\n\n- [ ] Step 5: Add the adoption key to `config/repo-state.md`, immediately after the `filename-grammar-since:` line, using the date this task runs: `printf 'lifecycle-lint-since: %s\\n' \"$(date +%Y-%m-%d)\"` and insert it by hand so the key sits on its own line. Confirm with `grep -c '^lifecycle-lint-since:' config/repo-state.md` returning exactly `1`.\n- [ ] Step 6: Run it - `bash tests/sessions/handoff-lifecycle.sh`, expected PASS. Then run only the suites this task can affect - `for t in tests/sessions/*.sh tests/loop-setup/*.sh; do bash \"$t\" || exit 1; done` - not the full `tests/run.sh`, which would trip over Task 4's in-flight board changes when the wave runs in parallel; the full suite runs at the wave boundary.\n\n--- OUTPUT CONTRACT ---\nEnd your run by printing exactly one JSON object on its own line:\n{\"unit\": \"t2-handoff-log\", \"files_changed\": [...], \"tests_passed\": <n>, \"tests_failed\": <n>, \"deviations\": [...], \"open_questions\": [...], \"deferred_items\": [...]}\nLeave everything uncommitted. The harness exports the patch.",
      "check": "set -uo pipefail\nexport LC_ALL=C\nPATCH=/Users/jjrdar/.ringer/work/i52-seam1-recording-convention/patches/t2-handoff-log.patch\nmkdir -p \"$(dirname \"$PATCH\")\"\n\n# acceptance-test custody: canonical overwrite\nmkdir -p \"$(dirname tests/sessions/handoff-lifecycle.sh)\"\ncat > tests/sessions/handoff-lifecycle.sh <<'CANONICAL_TEST_EOF'\n#!/usr/bin/env bash\n# handoff-log.sh enumerates a handoff's actions, refuses to consume while any action lacks a\n# disposition, appends dispositions to an append-only ## Transitions log rather than mutating a\n# status field, archives automatically and announces when the last disposition lands, and\n# lifecycle-lint class h flags a consumed-unarchived handoff only on or after lifecycle-lint-since.\nset -uo pipefail\nHERE=\"$(cd \"$(dirname \"$0\")\" && pwd)\"\nREPO=\"$(cd \"$HERE/../..\" && pwd)\"\nHL=\"$REPO/scripts/handoff-log.sh\"\nLINT=\"$REPO/scripts/lifecycle-lint.sh\"\nfail() { echo \"FAIL: $1\" >&2; exit 1; }\ntmp=\"$(mktemp -d)\"; trap 'rm -rf \"$tmp\"' EXIT\nmkdir -p \"$tmp/docs/handoffs\" \"$tmp/docs/archive\" \"$tmp/config\"\nprintf 'template-version: 7\\nlifecycle-lint-since: 2026-09-08\\n' > \"$tmp/config/repo-state.md\"\nh=\"$tmp/docs/handoffs/2026-09-10.I52.example-handoff.md\"\ncat > \"$h\" <<'EOF'\n# Handoff: example (#52)\n\n## Next actions\n\n- Run the seam 1 plan.\n- Roll template-version 7.\n\n## Notes\n\n- Not an action, a different section.\nEOF\n\n[ \"$(bash \"$HL\" actions \"$h\" | grep -c .)\" -eq 2 ] \\\n  || fail \"actions must enumerate exactly the two ## Next actions entries, not the Notes bullet\"\n[ \"$(bash \"$HL\" state \"$h\")\" = live ] || fail \"a handoff with no dispositions is not live\"\nbash \"$HL\" consume \"$h\" 2>/dev/null && fail \"consume succeeded with no dispositions present\"\nbash \"$HL\" disposition \"$h\" 1 bogus \"x\" 2>/dev/null && fail \"disposition accepted a value outside executed|superseded\"\nbash \"$HL\" disposition \"$h\" 9 executed \"x\" 2>/dev/null && fail \"disposition accepted an out-of-range action number\"\n\nbash \"$HL\" disposition \"$h\" 1 executed \"seam 1 shipped\" || fail \"disposition 1 exited non-zero\"\ngrep -q '^## Transitions$' \"$h\" || fail \"disposition did not create a ## Transitions section\"\ngrep -qE '^- [0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z action 1 executed - seam 1 shipped$' \"$h\" \\\n  || fail \"the transition line is not '- <ISO ts> action <n> <disposition> - <note>'\"\ngrep -qE '^status:' \"$h\" && fail \"disposition mutated a status field instead of appending to the log\"\nbash \"$HL\" consume \"$h\" 2>/dev/null && fail \"consume succeeded with action 2 still undispositioned\"\n[ \"$(bash \"$HL\" state \"$h\")\" = live ] || fail \"a partially dispositioned handoff is not live\"\n[ \"$(bash \"$HL\" actions \"$h\" | awk -F'\\t' '$1==1{print $3}')\" = executed ] \\\n  || fail \"actions does not report the disposition it just recorded\"\n\nout=\"$(bash \"$HL\" disposition \"$h\" 2 superseded \"the seam 1 brief replaces it\")\" || fail \"disposition 2 exited non-zero\"\n[ -f \"$h\" ] && fail \"the last disposition did not archive the handoff automatically\"\na=\"$tmp/docs/archive/2026-09-10.I52.example-handoff.md\"\n[ -f \"$a\" ] || fail \"the archived handoff is not in docs/archive/\"\nprintf '%s\\n' \"$out\" | grep -q \"moved $tmp/docs/handoffs/2026-09-10.I52.example-handoff.md to $tmp/docs/archive/\" \\\n  || fail \"the automatic archive was not verbose-announced with the paths exactly as given (archive rule 5)\"\n[ \"$(bash \"$HL\" state \"$a\")\" = consumed ] || fail \"an archived, fully dispositioned handoff is not consumed\"\n[ \"$(grep -c '^- .* action ' \"$a\")\" -eq 2 ] || fail \"the transition log is not append-only with both entries\"\n\n# class h: a consumed-unarchived handoff dated on/after lifecycle-lint-since is flagged\ncp \"$a\" \"$tmp/docs/handoffs/2026-09-10.I52.example-handoff.md\"\n[ \"$(bash \"$HL\" state \"$tmp/docs/handoffs/2026-09-10.I52.example-handoff.md\")\" = consumed-unarchived ] \\\n  || fail \"a fully dispositioned handoff still in docs/handoffs/ is not consumed-unarchived\"\nbash \"$LINT\" \"$tmp\" > \"$tmp/lint.out\" 2>/dev/null && fail \"lint exited 0 with a consumed-unarchived handoff\"\ngrep -q '^LINT h docs/handoffs/2026-09-10.I52.example-handoff.md:' \"$tmp/lint.out\" \\\n  || fail \"class h did not flag the consumed-unarchived handoff\"\n\n# grandfathering: the same defect dated before lifecycle-lint-since is ignored\nmv \"$tmp/docs/handoffs/2026-09-10.I52.example-handoff.md\" \"$tmp/docs/handoffs/2026-09-01.I52.old-handoff.md\"\nbash \"$LINT\" \"$tmp\" > \"$tmp/lint2.out\" 2>/dev/null\ngrep -q '^LINT h ' \"$tmp/lint2.out\" && fail \"class h flagged a handoff dated before lifecycle-lint-since\"\n\n# absence of the key skips class h entirely (the same absence-skips pattern as class f)\nprintf 'template-version: 7\\n' > \"$tmp/config/repo-state.md\"\ncp \"$a\" \"$tmp/docs/handoffs/2026-09-10.I52.example-handoff.md\"\nbash \"$LINT\" \"$tmp\" > \"$tmp/lint3.out\" 2>/dev/null\ngrep -q '^LINT h ' \"$tmp/lint3.out\" && fail \"class h ran with no lifecycle-lint-since key present\"\n\n# a live handoff is never flagged\nrm -f \"$tmp/docs/handoffs\"/*.md\nprintf 'template-version: 7\\nlifecycle-lint-since: 2026-09-08\\n' > \"$tmp/config/repo-state.md\"\nprintf '# H\\n\\n## Next actions\\n\\n- one\\n' > \"$tmp/docs/handoffs/2026-09-11.I52.live-handoff.md\"\nbash \"$LINT\" \"$tmp\" > \"$tmp/lint4.out\" 2>/dev/null\ngrep -q '^LINT h ' \"$tmp/lint4.out\" && fail \"class h flagged a live handoff\"\n\n# the real corpus shape: a prefix-variant heading with numbered items enumerates, and `all` bulk-dispositions it\nh2=\"$tmp/docs/handoffs/2026-09-12.I52.numbered.md\"\nprintf '# H2\\n\\n## Next actions, in recommended order\\n\\n1. First numbered action.\\n2. Second numbered action.\\n\\n## Notes\\n\\n- not an action\\n' > \"$h2\"\n[ \"$(bash \"$HL\" actions \"$h2\" | grep -c .)\" -eq 2 ] \\\n  || fail \"a prefix-variant heading with numbered items must enumerate 2 actions\"\nbash \"$HL\" disposition \"$h2\" all superseded \"replaced by the next thread\" || fail \"bulk disposition exited non-zero\"\na2=\"$tmp/docs/archive/2026-09-12.I52.numbered.md\"\n[ -f \"$a2\" ] || fail \"bulk disposition of every action did not auto-archive\"\n[ \"$(grep -c '^- .* action [12] superseded' \"$a2\")\" -eq 2 ] || fail \"bulk form must write one line per action 1..N\"\n\n# zero parsed actions: live until the action 0 bulk line, never vacuously consumed\nh3=\"$tmp/docs/handoffs/2026-09-13.I52.proseonly.md\"\nprintf '# H3\\n\\n## Next actions\\n\\nProse only, nothing enumerable.\\n' > \"$h3\"\n[ \"$(bash \"$HL\" state \"$h3\")\" = live ] || fail \"a section parsing to zero actions must derive live, not vacuously consumed\"\nbash \"$HL\" consume \"$h3\" 2>/dev/null && fail \"consume must refuse a zero-action file instead of vacuously succeeding\"\nbash \"$LINT\" \"$tmp\" > \"$tmp/lint5.out\" 2>/dev/null\ngrep -q '^LINT h docs/handoffs/2026-09-13' \"$tmp/lint5.out\" && fail \"class h flagged a zero-action live handoff\"\nbash \"$HL\" disposition \"$h3\" all executed \"legacy shape\" || fail \"bulk disposition of a zero-action file exited non-zero\"\na3=\"$tmp/docs/archive/2026-09-13.I52.proseonly.md\"\n[ -f \"$a3\" ] || fail \"the zero-action bulk path did not archive\"\ngrep -qE '^- .* action 0 executed - legacy shape$' \"$a3\" || fail \"the zero-action bulk line is not the action 0 shape\"\n[ \"$(bash \"$HL\" state \"$a3\")\" = consumed ] || fail \"an archived action-0 file must derive consumed\"\n\n# archive-destination collision takes a -2 suffix instead of stranding consumed-unarchived\nh4=\"$tmp/docs/handoffs/2026-09-13.I52.proseonly.md\"\nprintf '# H4\\n\\n## Next actions\\n\\nAlso prose only.\\n' > \"$h4\"\nbash \"$HL\" disposition \"$h4\" all superseded \"collision probe\" || fail \"disposition into a colliding archive name exited non-zero\"\n[ -f \"$tmp/docs/archive/2026-09-13.I52.proseonly-2.md\" ] || fail \"archive collision did not take the -2 suffix\"\necho \"PASS: enumeration incl. numbered, consume rejected, append-only log, all-bulk, zero-action live, archive announced, -2 collision, class h -since gate\"\nCANONICAL_TEST_EOF\n\n# run the acceptance test suite(s), printing everything\nbash tests/sessions/handoff-lifecycle.sh; rc=$?\n[ \"$rc\" -eq 0 ] || { echo \"CHECK FAIL: acceptance test tests/sessions/handoff-lifecycle.sh exited $rc; its output above names the failed assertion\"; exit 1; }\n\n# scoped regression gate (this task's suites only)\nfor t in tests/sessions/*.sh tests/loop-setup/*.sh; do bash \"$t\" || exit 1; done\nrc=$?\n[ \"$rc\" -eq 0 ] || { echo \"CHECK FAIL: the scoped regression gate exited $rc; a suite this task can affect regressed\"; exit 1; }\n\n# ownership audit\ngit add -A\nextra=\"$(git diff --cached --name-only | grep -vE '^(scripts/handoff\\-log\\.sh|scripts/lifecycle\\-lint\\.sh|config/repo\\-state\\.md|tests/sessions/handoff\\-lifecycle\\.sh)$' || true)\"\n[ -z \"$extra\" ] || { echo \"CHECK FAIL: files outside the ownership list were changed:\"; printf '%s\\n' \"$extra\"; exit 1; }\n\n# portability audit on changed shell files (comment lines excluded: header comments\n# legitimately NAME the banned constructs, as board-render-obsidian.sh line 9 does)\nbad=\"$(git diff --cached --name-only | grep -E '\\.sh$' | xargs grep -nE 'declare -A|\\$\\{[A-Za-z_]+,,\\}|grep -P' 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*#' || true)\"\n[ -z \"$bad\" ] || { echo \"CHECK FAIL: bash 3.2 portability violation (associative array, lowercase expansion, or grep -P):\"; printf '%s\\n' \"$bad\"; exit 1; }\n\n# export the patch outside the worktree\ngit diff --cached > \"$PATCH\"\n[ -s \"$PATCH\" ] || { echo \"CHECK FAIL: exported patch $PATCH is empty; the worker produced no applicable work\"; exit 1; }\necho \"CHECK PASS: The canonical handoff-lifecycle suite passes: action enumeration including numbered items, consume refused while any action lacks a disposition, an append-only Transitions log, the all-bulk and zero-action paths, the announced auto-archive with its -2 collision, and lint class h gated on lifecycle-lint-since.\"",
      "expect_files": [
        "/Users/jjrdar/.ringer/work/i52-seam1-recording-convention/patches/t2-handoff-log.patch"
      ],
      "verified": "The canonical handoff-lifecycle suite passes: action enumeration including numbered items, consume refused while any action lacks a disposition, an append-only Transitions log, the all-bulk and zero-action paths, the announced auto-archive with its -2 collision, and lint class h gated on lifecycle-lint-since."
    },
    {
      "key": "t4-card-marker",
      "engine": "claude-zai",
      "model": "glm-5.2",
      "task_type": "code-feature",
      "timeout_s": 3600,
      "spec": "You are a code-feature worker (test-first) implementing ONE unit of the loop-stack seam 1 recording convention.\nYour current working directory IS a git worktree of the loop-stack-session repo. Edit files here directly.\nDo NOT push. Do NOT run git commit at any point: leave every change uncommitted in the worktree.\nThe harness exports your diff as a patch and the orchestrator applies and commits it. Any commit you make dies with this worktree.\nThe embedded task text below may end with steps that mention committing; those are stripped, and if any commit instruction survives anywhere in this spec, ignore it.\n\nYou own ONLY these paths. Do not create, edit, or delete anything else, anywhere:\n- scripts/board-cards.sh\n- scripts/board-render-obsidian.sh\n- tests/board/cards.sh\n- tests/board/render.sh\n\nNever touch docs/handoffs/, docs/archive/, docs/reviews/, docs/sessions/, or any repo outside this one. The check audits this mechanically and fails on any path outside the list above.\n\nPORTABILITY (hard constraint): target macOS stock /bin/bash 3.2. No associative arrays, no ${var,,}, no grep -P, no GNU-only flags. `set -uo pipefail` and `export LC_ALL=C` at the top of every new script, BSD `date` first with a GNU `date` fallback, tab-separated pipelines. New scripts get `chmod +x`.\n\nHOW TO RUN YOUR OWN CHECK: for t in tests/board/*.sh; do bash \"$t\" || exit 1; done. Do NOT run the full tests/run.sh unless that command includes it; a broader run would trip over a parallel neighbour's in-flight changes. The full suite runs at the wave gate.\n\nACCEPTANCE TEST CUSTODY: The acceptance test tests/board/cards.sh and tests/board/render.sh is FIXED and AUTHORITATIVE in its assertions. Make exactly the replacement or append your task's steps specify, verbatim. The harness verifies every required assertion message is present in your file before running the suite, and the validator audits your test diff line by line. Deleting or weakening any assertion the steps did not authorize replacing is an automatic fail.\n\nWork test-first, in the order the task's numbered steps give. If anything is ambiguous, take the most conservative reading, record the question in your final output, and proceed; you cannot ask.\n\n--- SOURCE PLAN, SHARED CONTRACT (verbatim) ---\n# Seam 1: Recording Convention Implementation Plan\n\n> For executors: tasks use checkbox syntax; execute in dependency order; a task is done when its\n> acceptance check passes. No specific tooling, harness, or skills are assumed.\n\n**Goal:** every work session in a conforming repo leaves one durable, hand-editable session card, handoff consumption becomes an append-only per-action transition log that auto-archives, and the three empty board lanes populate from those records.\n\n**Approach:** doctrine plus enforcement, not a daemon.\nThree small shell scripts write the judgment facts (`session-card.sh`, `handoff-log.sh`) and derive everything else (`take-stock.sh`); `lifecycle-lint.sh` grows a class h behind an adoption-date gate, and `board-cards.sh` reads the derived rows into the empty lanes.\nRollout is piloted on this repo and `~/create/sys-prompts-cc`, proven against the sys-prompts-cc handoff case, then propagated by the template-version roll.\n\n**Tech stack:** bash, awk, plain markdown; no new dependencies.\n\n**Source brief:** `docs/briefs/2026-09-07.I52.seam1-recording-convention-brief.md`\n\n## Settled decisions (pinned by the brief, not reopened here)\n\n| Decision                      | Settled value                                                            |\n| ---                           | ---                                                                      |\n| Session terminal statuses     | closed set of 5: `done`, `blocked`, `handed-off`, `parked`, `aborted`    |\n| Died-mid-work                 | an open card with no close, by construction; no status for it            |\n| Write path                    | `scripts/session-card.sh open \\| log \\| close` plus conventions doctrine |\n| Card home                     | `docs/sessions/`, one card per session, updated in place                 |\n| Filename collision            | `-2` suffix on the slug                                                  |\n| Lint class f                  | `docs/sessions/` exempt (class f scans four lanes, sessions is not one)  |\n| Done-lane closed-issue window | 60 days, a visible constant in `board-cards.sh`                          |\n| Handoff dispositions          | closed set `executed \\| superseded`, appended to a transition log        |\n| Current state                 | always derived from the log, never a mutable status field                |\n| Lint class h                  | consumed-unarchived handoffs, exit non-zero                              |\n| Class h gate                  | `lifecycle-lint-since:` in `config/repo-state.md`, on-or-after only      |\n| Adoption date                 | the date this run ships the lint class, no template placeholder          |\n| Legacy backfill               | the 20 pre-adoption handoffs, human-confirmed table, then archived       |\n| Died-mid-work threshold       | 24 hours, a hardcoded visible constant                                   |\n| take-stock                    | a new script rebuilding recorded state from git, handoffs, the tracker   |\n| Template roll                 | `template-version` 6 to 7, carrying the rubix-autorun drift fix          |\n| Pilot pair                    | this repo and `~/create/sys-prompts-cc`                                  |\n\n## Plan-owned design calls (decided here, with reversal paths)\n\n**D9, went-stale as card vs flag: flag, carried on a 13th Card TSV field `marker`.**\nA went-stale repo has no work item to be a card of, and the shipped renderer has no non-card row concept, so a went-stale card would need a lane it does not have.\nThe Card TSV grows one field, `marker`, with values empty, `went-stale`, or `died-mid-work`; `went-stale` rides the repo's git card and `died-mid-work` rides the open session card.\nA went-stale repo always gets its git card, so the existing clean-conforming suppression rule gains one exception.\nReversal: emit a separate card row with `source=state` and drop field 13, changing `board-cards.sh` and the two board tests only.\n\n**D13, session card key set and `## Log` format.**\nFrontmatter, eight keys, each mapping onto a Card TSV field: `session_card`, `session`, `token`, `title`, `status`, `closed`, `why`, `next`.\n`## Log` is append-only, one entry per line, `- <ISO-8601 UTC timestamp> <free text>`.\nReversal: add a key to `session-card.sh`'s emitter and to `take-stock.sh`'s reader; existing cards keep parsing because every reader greps line-anchored keys.\n\n**D20, take-stock invocation surface: `scripts/take-stock.sh <repo-root> [--cards]`.**\nSingle positional argument matching `lifecycle-lint.sh`'s shape; default output is the human recorded-state report, `--cards` emits the 7-field intermediate TSV that `board-cards.sh` consumes.\nReversal: split `--cards` into a second script; the intermediate TSV contract is unchanged either way.\n\n**Went-stale comparison granularity: calendar day, not seconds.**\nA commit made seconds after recording is not stale; a repo is went-stale when its newest commit's `YYYY-MM-DD` is strictly later than its recorded-state date.\nReversal: switch to an hour threshold beside `DIED_MID_WORK_HOURS` in `take-stock.sh`.\n\n**Session status to board lane mapping.**\n`open` to `in-session`, `blocked` to `blocked-on-fact`, `handed-off` to `handed-off`, `done` to `done`, `aborted` to `done`, `parked` to `backlog`.\n`aborted` lands in `done` because it is terminal and not resumable; the card's `why` says it was aborted.\nReversal: one `case` arm in `board-cards.sh`.\n\n## Global constraints\n\nHost of record for all live verification: Jeremy's Mac; this repo is `/Users/jjrdar/create/loops/loop-stack-session`.\nSession terminal status vocabulary, closed and exact: `done`, `blocked`, `handed-off`, `parked`, `aborted`; anything else is rejected by `session-card.sh close`.\nHandoff disposition vocabulary, closed and exact: `executed`, `superseded`; anything else is rejected by `handoff-log.sh disposition`.\nDone-lane closed-issue lookback: 60 days, declared as `DONE_LOOKBACK_DAYS=60` at the top of `scripts/board-cards.sh`.\nDied-mid-work threshold: 24 hours, declared as `DIED_MID_WORK_HOURS=24` at the top of `scripts/take-stock.sh`.\nHarness-agnostic rule: no session-card or transition-log field names or assumes a particular agent harness; the cards are plain markdown any harness or human can write and read.\nVerbose-announce archive rule 5: every automatic handoff archive prints `moved <src> to <dest>` on stdout, one line per moved file.\nRecording tax cap: exactly one session card per session, updated in place; anything derivable comes from `take-stock.sh`, never from hand-recording.\nEvery handoff written on or after adoption carries a `## Next actions` section, one action per `- ` line; that list is the enumeration dispositions index into.\nLint baseline, measured 2026-09-07 before any task ran, so every \"lint is clean\" check below is baseline-relative rather than a bare exit 0.\n`scripts/lifecycle-lint.sh .` currently exits 1 with exactly four findings: class a on `docs/plans/2026-08-29.I28.import-gate-diagnosis-plan.md`, class a on the two `docs/plans/2026-09-02.I52.obsidian-board-mvp-plan{,_loop}.md` files, and class f on `docs/handoffs/2026-09-01.I56-architecture-research-home.md`.\nThe `#28` class a is a known false positive: issue #28 is open and covers that plan, but remote backends match issue titles only and #28's title does not contain the stem.\nTask 6 clears the class f finding by archiving that handoff; the class a findings are a lifecycle decision put to the human at Task 6's checkpoint, never auto-taken.\nPortability: macOS stock `/bin/bash` 3.2, no associative arrays, no `${var,,}`, no `grep -P`, no GNU-only flags; `set -uo pipefail` and `export LC_ALL=C` at the top of every new script, BSD `date` first with a GNU `date` fallback, tab-separated pipelines, and the repo's no-jq brace-depth JSON scan.\nTests are plain bash scripts under `tests/`, exit non-zero on failure, print one `PASS:` line on success, build fixtures under `mktemp -d`, and never touch the real Cortex vault, the real scan roots, or any repo outside their scratch directory.\nHouse style: plain dash never the em-dash character, one sentence per physical line in prose, aligned pipe tables no wider than 110 characters, plain commit messages with no co-author line.\n\n**Session card format** (the single canonical definition; Task 1 emits it, Task 3 reads it):\n\n```\n---\nsession_card: true\nsession: <ISO-8601 UTC, when the card was opened>\ntoken: <I52 | B7 | empty>\ntitle: <one line, what this session is>\nstatus: <open | done | blocked | handed-off | parked | aborted>\nclosed: <ISO-8601 UTC, empty while open>\nwhy: <one line, the reason for the terminal status, empty while open>\nnext: <one line, the next action>\n---\n\n# <title>\n\n## Log\n\n- <ISO-8601 UTC> opened\n- <ISO-8601 UTC> <free text, one line per meaningful step>\n```\n\nPath: `docs/sessions/<YYYY-MM-DD>.<tokens>.<slug>.md`, slug from the title lowercased with every run of non-alphanumerics collapsed to a dash; on collision the slug takes a `-2` suffix, then `-3`, and so on.\n`log` and `close` target the newest card whose `status:` is `open`, resolved by the `session:` timestamp; with no open card both exit non-zero (exit 3), and with two open cards tied to the second they exit non-zero (exit 7) naming the tied files, because a silent pick between same-second cards would target one at random (the doctrine is one card per session, so a tie is already an anomaly worth surfacing).\n\n**Handoff transition log** (the single canonical definition; Task 2 writes it, Task 2 and Task 3 read it):\n\n```\n## Transitions\n\n- <ISO-8601 UTC> action <n> <executed|superseded> - <one-line note>\n```\n\nAppended to the handoff file itself, append-only, never rewritten.\nThe actions section is any `## ` heading whose text starts with `Next actions` (prefix match, so `## Next actions, in recommended order` counts); enumerable actions are the lines beginning `- ` or `<n>. ` inside it, numbered 1..N by position.\nThis matches the real corpus: the three sys-prompts-cc handoffs all carry numbered `1.` items under prefix-variant headings.\nDerived state: `live` when the section is absent, when it parses to zero actions with no bulk line, or when any parsed action has no disposition line; `consumed` when every parsed action has one (or a bulk `action 0` line stands in for a zero-action file) and the file sits in `docs/archive/`; `consumed-unarchived` in the same condition with the file still in `docs/handoffs/`.\nThe bulk `action 0` line is the legacy-backfill shape for files whose actions cannot be enumerated; it is written by `handoff-log.sh disposition <handoff> all <d>` on a zero-action file, and on a file with parsed actions the same verb appends one line per action 1..N instead.\n\n**take-stock intermediate TSV** (`take-stock.sh <root> --cards`, tab-separated, no header, 7 fields):\n\n| # | Field           | Meaning                                                             |\n| - | ---             | ---                                                                 |\n| 1 | `kind`          | `repo`, `session`, or `handoff`                                     |\n| 2 | `ref`           | repo-relative path of the card or handoff, empty on the `repo` row  |\n| 3 | `status`        | session status, or handoff derived state, empty on the `repo` row   |\n| 4 | `token`         | doc-grammar token (`I52`), or empty                                 |\n| 5 | `title`         | one-line title, tabs and newlines collapsed to spaces               |\n| 6 | `last_activity` | `YYYY-MM-DD` of the newest recorded activity for that row           |\n| 7 | `marker`        | `went-stale` (repo row), `died-mid-work` (session row), or empty    |\n\nExactly one `repo` row is emitted, always first.\nOne `session` row per file in `docs/sessions/`, one `handoff` row per handoff whose derived state is `live`.\n\n**Card TSV field 13** (extends the shipped 12-field contract in\n`docs/plans/2026-09-02.I52.obsidian-board-mvp-plan.md`):\n\n| #  | Field    | Meaning                                            |\n| -- | ---      | ---                                                |\n| 13 | `marker` | empty, `went-stale`, or `died-mid-work`            |\n\n**Card sources and ids** (`board-cards.sh`, after Task 5):\n\n| Source    | card_id                        | Column                                    |\n| ---       | ---                            | ---                                       |\n| tracker   | `<repo_key>#<token>`           | from the `agent:` label, as shipped       |\n| git       | `<repo_key>#git`               | `next-up`                                 |\n| session   | `<repo_key>#s-<file stem>`     | from `status`, per the mapping above      |\n| handoff   | `<repo_key>#h-<file stem>`     | `handed-off`                              |\n| closed    | `<repo_key>#<token>`           | `done`                                    |\n\n## Dependency graph\n\n```\nWave 1:  Task 1 (session-card.sh)   Task 2 (handoff-log.sh + lint h)   Task 4 (Card TSV field 13)\nWave 2:  Task 3 (take-stock.sh)      depends on 1, 2\n         Task 6 (legacy backfill)    depends on 2\nWave 3:  Task 5 (board wiring)       depends on 3, 4\n         Task 8 (sys-prompts-cc pilot) depends on 2, 3\nWave 4:  Task 7 (dogfood this repo)  depends on 3, 5, 6\n         Task 9 (template roll v7)   depends on 1, 2, 3, 8\nWave 5:  Task 10 (propagation + live board) depends on 5, 7, 8, 9\n```\n\nWave 1's three tasks own disjoint files and parallelize.\nTask 4 and Task 5 both own `scripts/board-cards.sh` and `tests/board/cards.sh`, so they are strictly ordered; between them the Card TSV producer and consumer agree at 13 fields and nothing is broken.\nTask 7 and Task 9 own disjoint files (session cards in this repo versus the templates and `setup.sh`) and parallelize.\nIn-task regression gates are scoped to the suites the task can affect, so a parallel neighbor's in-flight changes never block a task on damage it did not cause; the full `tests/run.sh` runs once at each wave boundary before the next wave starts, and at Task 10 Step 1.\n\n## Human checkpoints\n\n- **After Task 6, before the archive moves.** The executor presents the pre-adoption handoffs as a table (file, date, proposed disposition, one-line reason) and stops. Jeremy confirms or edits the dispositions; only then do the archive moves run, verbose-announced. This is the brief's one-time bulk backfill under human confirmation. A second, separate prompt (Task 6 Step 3b) then asks the unrelated MVP plan-set archive question on its own; one decision per prompt.\n- **Before Task 10's propagation step.** Rolling template-version 7 into seven repos outside this one writes to repos the run does not own. The executor prints the target list with each repo's current template-version and working-tree state and stops for an explicit go naming which repos roll; a dirty tree blocks that repo's roll.\n- **After Task 10, brief criterion 7 `[judgment]`.** After one real work session under the convention, Jeremy answers two questions: did the recording tax feel acceptable, and does the card answer \"where did I leave off\" without reconstruction. Record the verdict in the run's session card `why:` line. Criteria 1 through 6 cover the mechanics; this call stays human.\n\n## How to run\n\nAll commands run from this repo's root, `/Users/jjrdar/create/loops/loop-stack-session`.\n\nFull test suite (globs `tests/*/*.sh`, so the new `tests/sessions/` directory is picked up with no wiring):\n\n```\ntests/run.sh\n```\n\nA single suite:\n\n```\nbash tests/sessions/session-card.sh\n```\n\nRecorded state and lint for any repo:\n\n```\nscripts/take-stock.sh .\nscripts/lifecycle-lint.sh .\n```\n\nBoard render (the environment the shipped MVP already takes):\n\n```\nexport LOOP_BOARD_CORTEX=/Users/jjrdar/Documents/_CS_DOCUMENTS_/Obsidian/Cortex\nexport LOOP_BOARD_HOME=\"$LOOP_BOARD_CORTEX/00_System/Board\"\nscripts/board.sh\n```\n\nThe two `export` lines are separate statements on purpose: a single-command environment prefix does not expand `LOOP_BOARD_CORTEX` for the assignment that follows it on the same line.\n\n---\n\n--- SOURCE PLAN, YOUR TASK (verbatim) ---\n### Task 4: Card TSV grows field 13, `marker`\n\nDepends on: none.\n\n**Files (exclusive ownership):**\n- Modify: `scripts/board-cards.sh` (the `card()` emitter at lines 31 to 35, and its three call sites at lines 116, 123, and 146)\n- Modify: `scripts/board-render-obsidian.sh` (the field-count guard at lines 89 to 93, the field unpacking at lines 106 to 108, the frontmatter block at lines 124 to 138, and the body block at lines 139 to 152)\n- Test: `tests/board/cards.sh` (extend), `tests/board/render.sh` (extend)\n\n**Interfaces:**\n- Consumes: the shipped 12-field Card TSV contract.\n- Produces: a 13-field Card TSV. Field 13 is `marker`, values empty, `went-stale`, or `died-mid-work`. Every existing producer call site passes an empty marker in this task; Task 5 populates it.\n  `board-render-obsidian.sh` rejects any row whose field count is not 13 before any write, emits `marker: <value>` as a frontmatter key, and prefixes the rendered title with `[<marker>] ` when the marker is non-empty, so the marker shows on the Bases card face without any `.base` change.\n\nBehavior notes, each edge named:\n- The `card()` helper takes a 13th positional argument and prints it as the last tab-separated field; the free-text `clean()` guard is not applied to it because the value comes from a fixed vocabulary.\n- The renderer's awk guard changes `NF != 12` to `NF != 13` and its message to `expected 13`; a 12-field row is now malformed and rejected, which is what the extended render test asserts.\n- The renderer unpacks `marker=\"${f[12]:-}\"` and emits it with `printf 'marker: %s\\n' \"$marker\"`, not through `fm_free`, because the value comes from a fixed vocabulary and must stay unquoted so a Bases filter can match it literally.\n- Title prefixing happens once, before the band emoji is prepended, so the rendered `title:` property reads `<emoji> [went-stale] <title>`.\n- `_health.md`'s column-mapping table (lines 171 to 183) is left alone in this task; Task 5 rewrites it.\n\n**Acceptance check:** `bash tests/board/cards.sh` and `bash tests/board/render.sh` both exit 0 `[executed-check]`.\n\n- [ ] Step 1: Extend `tests/board/cards.sh`. Replace its last two assertion lines and its `PASS:` line with exactly:\n\n```\nawk -F'\\t' 'NF!=13{exit 3}' <<<\"$out\" || fail \"a row does not have 13 fields\"\n[ \"$(awk -F'\\t' '$1==\"create/repoA#git\"{print $13}' <<<\"$out\")\" = \"\" ] \\\n  || fail \"field 13 (marker) must be empty for an ordinary git card\"\necho \"PASS: json parse, columns, B token, negative join, git card, sanitized title, failed source, 13 fields\"\n```\n\n- [ ] Step 2: Extend `tests/board/render.sh`. Replace its `card()` helper, its malformed-row block, and its `PASS:` line with exactly:\n\n```\ncard() { printf '%s\\tcreate/%s\\t%s\\t%s\\t%s\\t%s\\t1\\t2026-09-01\\t%s\\t%s\\tok\\t%s\\t%s\\n' \\\n  \"$1\" \"$2\" \"$3\" \"$4\" \"$5\" \"$6\" \"$7\" \"$8\" \"$now\" \"$9\"; }\n```\n\nand, in the fixture block, exactly:\n\n```\n{\n  card 'create/repoA#I52' repoA tracker in-session 'Board MVP' I52 '' '' ''\n  card 'create/repoA#git' repoA git next-up 'repoA working tree' '' '2 uncommitted, +0 ahead' '' went-stale\n} > \"$tmp/cards.tsv\"\n```\n\nand, replacing the malformed-row block and the `PASS:` line, exactly:\n\n```\ngrep -q '^marker: went-stale$' \"$home/create-repoA-git.md\" || fail \"the marker did not reach the card frontmatter\"\ngrep -q '\\[went-stale\\]' \"$home/create-repoA-git.md\" || fail \"the marker did not prefix the rendered title\"\ngrep -q '^marker:' \"$home/create-repoA-I52.md\" || fail \"an unmarked card still needs an explicit marker key\"\ngrep -q '^title: .*\\[' \"$home/create-repoA-I52.md\" && fail \"an unmarked card must carry no marker prefix in its title\"\n\n# atomicity: a render fed a malformed (12-field, the pre-marker shape) row must exit non-zero and\n# leave the prior board intact, never a wiped or half-built board.\nprintf 'a\\tb\\tc\\td\\te\\tf\\tg\\th\\ti\\tj\\tk\\tl\\n' > \"$tmp/bad.tsv\"\nrun < \"$tmp/bad.tsv\" && fail \"render should reject a 12-field row now that the contract is 13\"\n[ \"$(grep -rl 'board_card: true' \"$home\" | grep -c .)\" -eq 2 ] || fail \"failed render wiped the prior board\"\necho \"PASS: notes, frontmatter, marker, resume block, health note, .base preserved, atomic, outside untouched\"\n```\n\n- [ ] Step 3: Run both - `bash tests/board/cards.sh` and `bash tests/board/render.sh`, expected FAIL: cards on `a row does not have 13 fields`, render on `the marker did not reach the card frontmatter`.\n- [ ] Step 4: Implement the two script changes to the Interfaces contract.\n- [ ] Step 5: Run both - expected PASS. Then run the four board suites only - `for t in tests/board/*.sh; do bash \"$t\" || exit 1; done` - not the full `tests/run.sh`; the full suite runs at the wave boundary.\n\n--- OUTPUT CONTRACT ---\nEnd your run by printing exactly one JSON object on its own line:\n{\"unit\": \"t4-card-marker\", \"files_changed\": [...], \"tests_passed\": <n>, \"tests_failed\": <n>, \"deviations\": [...], \"open_questions\": [...], \"deferred_items\": [...]}\nLeave everything uncommitted. The harness exports the patch.",
      "check": "set -uo pipefail\nexport LC_ALL=C\nPATCH=/Users/jjrdar/.ringer/work/i52-seam1-recording-convention/patches/t4-card-marker.patch\nmkdir -p \"$(dirname \"$PATCH\")\"\n\n# acceptance-test custody: every required assertion message must be present,\n# audited in the file that actually carries it\nmissing=0\nwhile IFS= read -r line; do\n  [ -n \"$line\" ] || continue\n  grep -Fq -- \"$line\" tests/board/cards.sh || { echo \"CHECK FAIL: required assertion missing from tests/board/cards.sh: $line\"; missing=1; }\ndone <<'REQUIRED_EOF'\na row does not have 13 fields\nfield 13 (marker) must be empty for an ordinary git card\nREQUIRED_EOF\nwhile IFS= read -r line; do\n  [ -n \"$line\" ] || continue\n  grep -Fq -- \"$line\" tests/board/render.sh || { echo \"CHECK FAIL: required assertion missing from tests/board/render.sh: $line\"; missing=1; }\ndone <<'REQUIRED_EOF'\nthe marker did not reach the card frontmatter\nthe marker did not prefix the rendered title\nan unmarked card still needs an explicit marker key\nan unmarked card must carry no marker prefix in its title\nrender should reject a 12-field row now that the contract is 13\nfailed render wiped the prior board\nREQUIRED_EOF\n[ \"$missing\" -eq 0 ] || exit 1\n\n# run the acceptance test suite(s), printing everything\nbash tests/board/cards.sh; rc=$?\n[ \"$rc\" -eq 0 ] || { echo \"CHECK FAIL: acceptance test tests/board/cards.sh exited $rc; its output above names the failed assertion\"; exit 1; }\nbash tests/board/render.sh; rc=$?\n[ \"$rc\" -eq 0 ] || { echo \"CHECK FAIL: acceptance test tests/board/render.sh exited $rc; its output above names the failed assertion\"; exit 1; }\n\n# scoped regression gate (this task's suites only)\nfor t in tests/board/*.sh; do bash \"$t\" || exit 1; done\nrc=$?\n[ \"$rc\" -eq 0 ] || { echo \"CHECK FAIL: the scoped regression gate exited $rc; a suite this task can affect regressed\"; exit 1; }\n\n# ownership audit\ngit add -A\nextra=\"$(git diff --cached --name-only | grep -vE '^(scripts/board\\-cards\\.sh|scripts/board\\-render\\-obsidian\\.sh|tests/board/cards\\.sh|tests/board/render\\.sh)$' || true)\"\n[ -z \"$extra\" ] || { echo \"CHECK FAIL: files outside the ownership list were changed:\"; printf '%s\\n' \"$extra\"; exit 1; }\n\n# portability audit on changed shell files (comment lines excluded: header comments\n# legitimately NAME the banned constructs, as board-render-obsidian.sh line 9 does)\nbad=\"$(git diff --cached --name-only | grep -E '\\.sh$' | xargs grep -nE 'declare -A|\\$\\{[A-Za-z_]+,,\\}|grep -P' 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*#' || true)\"\n[ -z \"$bad\" ] || { echo \"CHECK FAIL: bash 3.2 portability violation (associative array, lowercase expansion, or grep -P):\"; printf '%s\\n' \"$bad\"; exit 1; }\n\n# export the patch outside the worktree\ngit diff --cached > \"$PATCH\"\n[ -s \"$PATCH\" ] || { echo \"CHECK FAIL: exported patch $PATCH is empty; the worker produced no applicable work\"; exit 1; }\necho \"CHECK PASS: The board cards and render suites pass at 13 fields, with marker emitted as frontmatter and as a title prefix, and a 12-field row now rejected atomically.\"",
      "expect_files": [
        "/Users/jjrdar/.ringer/work/i52-seam1-recording-convention/patches/t4-card-marker.patch"
      ],
      "verified": "The board cards and render suites pass at 13 fields, with marker emitted as frontmatter and as a title prefix, and a 12-field row now rejected atomically."
    }
  ]
}
