# Batch review: Obsidian board MVP (#52)

Run: /loop-plan on `docs/briefs/2026-09-02.I52.obsidian-board-mvp-brief.md`.
Autonomy: auto (`docs/chain-state.md`).
Standing answers from Jeremy, given in advance: Rubix review a/b YES to both; accept all skill/reviewer recommendations; any human-bound question is auto-decided by my own recommendation and logged here; MVP, every decision pivotable.

This journal is the run's gate record, chronological.
ASK and STOP entries are record-only.
BATCH and DEFAULT entries are the review obligation, each with a reversal path.

## Step 2 - open questions for planning (auto-decided)

The brief's five open questions plus the reconstruction decisions the missing plan left behind.
Each is `[gate:ASK]` by class, pre-answered by Jeremy's standing answer, so it is decided here rather than surfaced.

### D1 - Board directory name under `00_System` (DEFAULT)

- Decision: `00_System/Board`.
- Rationale: existing `00_System` children are singular title-case nouns (`Digests`, `Templates`, `Taxonomy.md`); `Board` matches. All card notes, the two `.base` views, and the health note live under it.
- Reversal: rename the directory and re-render; nothing else references the name except `board.sh`'s config edge.

### D2 - His-work filter for the scan roots (DEFAULT)

- Decision: uniform ownership heuristic, not an explicit include list. A discovered git repo is included when ANY of: it is conforming (`config/repo-state.md` exists), OR its `origin` remote URL owner matches Jeremy's identity token (`jroethel`, configurable in `board.sh`), OR it has uncommitted or unpushed local work.
- Rationale: the brief flags `~/repos` as mostly third-party clones needing a filter; a heuristic is robust to new repos where an include list rots. The three OR arms are exactly "his repos, plus anything with work that needs him," which is the board's proof-of-value. Applied uniformly to all three roots so there is one rule, not per-root special-casing.
- Reversal: swap the heuristic for an explicit include list in `board.sh`; the filter is a single function with one call site.

### D3 - CSS snippet mechanics if the spike shows a native shortfall (DEFAULT)

- Decision: if and only if the Task 1 spike shows native Bases cannot render 8 kanban-style columns with distinguishable colors, `board.sh` writes `Cortex/.obsidian/snippets/loop-board.css` (idempotent overwrite) but does not enable it (enabling needs a UI toggle). The health note prints a one-time instruction to enable it under Settings > Appearance > CSS snippets.
- Rationale: the snippet path is the brief's sanctioned cover for a native shortfall; it is the single allowed write outside the board directory, and it is gated behind the spike so a passing spike means it is never written.
- Reversal: delete the snippet file and the one-time instruction; if the spike passes, this code path never runs.

### D4 - Resume-prompt serialization format (DEFAULT)

- Decision: the renderer composes a fenced code block from the card's recorded and derived fields, no network, writing nothing outside the note. Template and entry-point mapping are fixed in the plan's Task 4.
- Rationale: best-effort and `/loop-*`-oriented per the brief; composed at render time from the Card TSV so it derives only from recorded and derived state (satisfies criterion 7).
- Reversal: edit the template block in `board-render-obsidian.sh`; it is one heredoc.

### D5 - Non-git directories under the scan roots: skipped silently or logged (DEFAULT)

- Decision: logged, not silent. Discovery counts non-git directories per root and the health note reports `skipped: N non-git dirs under <root>`.
- Rationale: matches the board's "never a silent omission" philosophy (criterion 6); the count is cheap and aids trust that discovery saw everything.
- Reversal: drop the skip count from the health note; discovery still functions.

### D6 - Discovery TSV schema, 9 fields (DEFAULT, reconstruction)

- Decision: `repo_path, repo_key, scan_root, conforming, included, last_commit_epoch, uncommitted, ahead, behind_fetch` (defined in the plan's Global constraints).
- Rationale: the plan that named the original 9 fields was never committed and is absent from git history; the brief says frontmatter maps 1:1 and lists the machine-fact anchor set (repo, column, staleness band, health, as-of stamps, join tokens). This 9-field set is the discovery half of that anchor, re-derived. Decision-table row 2 flagged "re-check at plan amendment," which this is.
- Reversal: amend the schema in `board.sh` and the Task 2 test; downstream consumers read named columns, so an added field is additive.

### D7 - Card TSV schema, 12 fields (DEFAULT, reconstruction)

- Decision: `card_id, repo_key, source, column, title, token, staleness_band, last_work_date, local_position, behind_note, health, render_epoch` (defined in the plan's Global constraints).
- Rationale: same missing-plan gap as D6; this maps 1:1 to card-note frontmatter and carries the brief's frontmatter anchor set. The resume prompt is composed by the renderer from these fields, not carried as a TSV field, because it is multi-line.
- Reversal: amend the schema in `board-cards.sh`, `board-render-obsidian.sh`, and the Task 3/4 tests together; the three share the named contract.

### D8 - Staleness band to color mapping (DEFAULT, reconstruction)

- Decision: band 1 `< 5 days` green, band 2 `< 15 days` yellow, band 3 `< 30 days` orange, band 4 `>= 30 days` red. Bands are the brief's four thresholds; the color assignment is new.
- Rationale: the brief pins the four thresholds and requires "distinguishable colors" but never wrote the mapping; green-to-red is the conventional recency ramp. Applied by the `.base` view or the conditional CSS snippet.
- Reversal: edit the color values in the `.base` template or CSS snippet.

### D9 - Column assignment for `in-session`, `awaiting-review`, `handed-off` (DEFAULT, reconstruction)

- Decision: `in-session` <- `agent:working`; `awaiting-review` <- `agent:review`; `handed-off` renders as an empty column in the MVP (its source is session/handoff recording, deferred with seam 1). Full mapping in the plan's Task 3.
- Rationale: the `agent:` label family (conventions.md) maps cleanly to five columns; `blocked-on-fact` and `handed-off` depend on deferred session-card state, so they render as empty columns, which the kanban still shows.
- Reversal: add a `handed-off` source rule when seam-1 recording lands; the column already exists.

### D10 - Git card emission and the git-tracker merge (BATCH, revised after the Rubix review)

- Decision: the git source emits one standalone `next-up` git card per included repo, EXCEPT a clean conforming repo (zero uncommitted, ahead 0 or -1) that already has a tracker card, where the git card is suppressed as redundant. The git-to-tracker token merge is NOT implemented in the MVP; it defers to seam 1 when the tokened git source (session-card docs) exists. Criterion 4 (negative join) is satisfied structurally because `card_id` embeds `repo_key`.
- Rationale: the brief's outcome is "all of his repos show as cards," so issueless and dirty repos always get a git card; suppressing the redundant clean-conforming git card cuts board clutter that the glance test punishes. Rubix Lens B finding 5 showed the merge path is dead code in the MVP (repo-level git cards are tokenless, so the merge never fires), so it is cut rather than shipped untested. This is a taste call on card topology, hence BATCH.
- Reversal (scoped re-run): re-enable an always-on git card, or implement token-adoption so the merge fires, by re-running Task 3 with the alternate rule. The Task 3 test pins the current behavior so the swap is localized.
- Supersedes the original D10 ("one git card always, merge collapses overlap"), which the Rubix review corrected.

## Step 6 - Rubix review (BATCH)

Ran the rubix-review two-lens review (Jeremy pre-approved a/b YES).
The `rubix-review` skill is not invocable via the Skill tool in this session, but it is installed on disk; I ran it faithfully by dispatching its two lenses as fresh-context, read-only subagents on different models (Lens A on sonnet, Lens B on opus), each carrying the reviewer-conduct contract verbatim, per the skill's SKILL.md.
Standing answer: accept all reviewer recommendations. Every finding below is accepted and folded into the plan; my verdict column records the fix.

Both lenses independently caught the same critical defect: my first-draft Task 3 test stub fabricated a plain-text tracker format, but `scripts/tracker.sh list` emits a JSON array `[{number, title, labels:[{name}], updatedAt}]` for every backend (verified by reading `do_list`, `local_list`, `gitlab_list`).

| # | Finding (condensed)                                              | Lens | Sev      | Verdict / fix in the plan                                                    |
| - | ---                                                             | ---  | ---      | ---                                                                          |
| 1 | Tracker output is JSON, test stub faked plain text              | A+B  | Critical | Fixed: stub emits real JSON; parse via repo's no-jq brace-scan; Tech stack noted |
| 2 | `cards.sh` fixture never mkdir's repo dirs, `cd` fails          | B    | High     | Fixed: fixture `mkdir -p`s repoA/repoB/repoFail                             |
| 3 | `done` tracker rule unreachable (agent:done closes; list open)  | A+B  | High     | Fixed: `done` is an empty MVP column; unreachable rule dropped, note added  |
| 4 | idea-label token should be `B<n>` not `I<n>`                    | A    | High     | Fixed: token rule `B<n>` for idea, `I<n>` otherwise; test asserts `B7`      |
| 5 | Non-atomic delete-then-rewrite can wipe a live vault            | A+B  | High     | Fixed: stage-then-swap, prune orphans last; atomicity test added           |
| 6 | Git-tracker merge is dead code in the MVP                       | B    | Medium   | Fixed: merge cut from MVP (defers to seam 1); see revised D10               |
| 7 | `grep -P` is GNU-only; tab guard no-ops on BSD/macOS            | B    | Medium   | Fixed: portable `awk '/\t/{exit 1}'` tab check                             |
| 8 | `ahead == -1` renders `+-1 ahead` on no-upstream repos         | A    | Medium   | Fixed: composition special-cases `-1`, omits the ahead clause              |
| 9 | bash 3.2 (macOS stock) constraint unstated                     | A    | Medium   | Fixed: Tech stack + each task pin bash 3.2, no assoc arrays                 |
|10 | Owner extraction unspecified, SSH remotes untested             | A+B  | Medium   | Fixed: sed rule for HTTPS+SSH in Global constraints; SSH fixtures in test   |
|11 | TSV free-text not sanitized for tabs/newlines                  | B    | Medium   | Fixed: `tr '\t\n' '  '` on free-text; embedded-tab fixture in Task 3 test  |
|12 | CSS snippet refreshed every render, violates criterion 5       | B    | Medium   | Fixed: create-if-absent (write once); `LOOP_BOARD_CSS` env, no docs reach   |
|13 | Failed-source assertion is a permissive OR                     | A    | Low      | Fixed: exact `health == failed` plus exactly-one-card assertion            |
|14 | Spike test checks presence, not valid YAML                     | B    | Low      | Fixed: python3 YAML-parse assertion when available                         |
|15 | Frontmatter contract duplicated with no canonical source       | B    | Low      | Fixed: declared once in Global constraints; tasks reference it             |
|16 | "plan file present" entry-point branch unspecified/untested    | A    | Low-Med  | Fixed: exact `ls <repo>/docs/plans/*.md` check specified in Task 4         |

No finding was dismissed; all 16 accepted per the standing answer.
Reversal for the whole triage: each fix lives in the plan document only (no code yet), so reverting is a plan edit; the two BATCH taste calls (D10 topology, `done` as empty) are scoped re-runs with the alternate lean if Jeremy prefers.

## End-of-chain checkpoint

BATCH and DEFAULT decisions above are auto-taken for Jeremy to accept or reverse.
The plan and this journal are committed together on branch `i52-obsidian-board-mvp`.
No STOP or live ASK fired; autonomy carried the run start to finish.

## Drive phase (loop-drive, 2026-09-03, autonomy auto)

### DR1 - Human checkpoints collapsed to one end checkpoint (ASK, resolved live)

- Decision: Jeremy's standing instruction "push any tests I need to do to the end"; Task 1's glance checkpoint and Task 5's criterion 8 defer to a single end-of-run checkpoint. T1 records a best-effort verdict (screenshot if possible, else flagged unverified-by-human).
- Rationale: explicit instruction in the launch message.
- Reversal: n/a - resolved live.

### DR2 - Topology and transport (BATCH)

- Decision: 4 waves, 5 units; T2/T3/T4 on ringer (claude-zai, glm-5.2, posterior 92% first-try over 75 code-feature tasks); T1/T5 as in-place opus Agent-tool units (need the real vault, Obsidian, gh auth). One clear shape, no 60/40 near-tie diagrammed.
- Rationale: routing chain posterior tier for the shell units; pin:taste and pin:reach for T1/T5 per the routing table footnotes.
- Reversal: scoped re-run of any unit with the alternate transport or model; units are independent and patch-applied per wave.

### DR3 - Lint finding on wave-1.json attributed as false positive (BATCH)

- Decision: `instructs_git_commit` fired on the fixture helper `commit() { ... git commit -qm c ... }` inside the embedded verbatim acceptance test; the spec's output contract says "leave it uncommitted for patch export" and the check does `git add -A` + staged-diff export with a non-empty assert. Proceeding to run without editing the spec; waves 2-3 lint clean.
- Rationale: the `git commit` runs in mktemp fixture repos, never the worktree; editing the verbatim test to dodge a heuristic would diverge the canonical check from the source plan.
- Reversal: cheap - reword the fixture helper in both spec and check and re-lint if the heuristic is judged right after all.

### DR4 - Launch under auto (DEFAULT)

- Decision: pre-launch detail menu not fired; dashboard and watch points printed inline; pre-flight executed for real (clean tree except plan artifacts, worktree probe OK, vault OK, tests/run.sh present, waves 2-3 lint clean); plan artifacts committed as the plan; wave 1 launched.
- Rationale: loop-auto DEFAULT gate under auto; invoking /loop-drive on the finished plan under auto is the execution approval.
- Reversal: git revert of the plan-artifact commit; halt waves with "stop".
