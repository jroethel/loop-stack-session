# B32 final advisory loop-review, jrit-loop port

Run 2026-09-14 from main at eab9d8e, fixed point b811337 (B32_BASE, U02's skeleton commit), 22 commits, 79 files, +4777 lines.
Advisory and non-blocking per the orchestration plan Section 6; recorded at the C7 handoff.

## Disclosure

Spec source: /home/jjrdar/repos/loop-stack-session/docs/plans/2026-09-13.B32.jrit-loop-plugin-port-plan.md (explicit path, discovery rung 1).
Standards sources: no documented standards file in the repo (no CODING_STANDARDS.md, no CONTRIBUTING.md); house style is machine-enforced by ci/*.sh and skipped; basis is the Fowler 12-smell baseline alone.
Both axes ran as fresh-context opus subagents under the reviewer-conduct contract.

## Spec

Reviewer verified in its own session: bash ci/run-all.sh exit 0 (helper SLOC 196, both validate --strict green), tests/pre-plugin-detection.sh and tests/local-mode-grammar.sh green.
Coverage against the plan is unusually complete: D1-D12 artifacts all present, D2/D4 templates verbatim, D3 paragraph byte-identical in exactly the eight skills, D5/D6 blocks verbatim, five eval cases with the three step-1 schema facts recorded.

### (a) Missing or partial

- Criterion 12 is proven on one host. Spec line 1162: "run it once per host and record each host's result separately." The receipts doc Part 2 record 6 carries RIT-UADV2223 only; docs/decommission.md Section 5 lists RIT-UADV2213 as pending. Disclosed, not closed. (Known: this is the open Mac C3 item.)

### (b) Scope creep

- ci/clean-room-npx.sh gained -g inside the Task 14 commit 5e683c9, but Task 14's ownership is "Modify: README.md (the npx version pin only)" (line 1149) and Task 3 "owns ci/** outright" (line 496). The orchestrator applied it at the gate under journal entry 35 (check-bug-class precedent), so the act is journaled; the live defect is that the script's own header still says "Installer flags verified 2026-09-13" listing only -s and -y, so the flag that makes it pass is undocumented there. One-line header fix available.

### (c) Implemented but wrong

1. The drift check's SKILL.md assertion is permanently dead for loop-drive. D9 (line 355): "skills/ contains exactly eleven directories, each with a SKILL.md." ci/version-drift.sh skips that file unconditionally when absent; its comment claims "hard once present," but nothing makes it hard. The reviewer re-derived it: deleting skills/loop-drive/SKILL.md on a copy still yields PASS: drift, exit 0. Journal entry 23's own reversal note ("git revert of the two version-drift commits once Task 9 lands") was never executed. Fix: revert the skip branch or make the assertion unconditional.
2. Gate 0's label block is not the verbatim quote its tripwire depends on. Spec line 765 requires the test to quote loop-setup's label command block verbatim so divergence is a finding; six of seven --description strings already differ (test "Backlog lane candidate" vs skill "Parked backlog item, not active work") while the test's comment asserts it is the exact block. The divergence detector shipped already-divergent. Fix: re-copy the block from loop-setup's prose into tests/receipt-gates.sh Gate 0.
3. The pre-plugin fixture now exists twice. Spec line 1013 points the eval at a copy of tests/fixtures/pre-plugin-repo/; the eval reconstructs it inline instead (disclosed in docs/eval-fixtures-are-engine-neutral.md), so Task 8's single fixture of record and the eval's inline copy are hand-synced. Fix: have the eval case read the fixture directory, or name the inline copy as the fixture of record.

## Standards

### (a) Documented-standard violations

None, because no standard is documented; house style is machine-enforced and skipped per the brief. Zero hard violations. Everything below is a judgement call.

### (b) Baseline smells

- Duplicated Code, extract_backtick_tokens: ci/structure-check.sh:58-70 and ci/clean-room-npx.sh:46-56 differ only in find skills vs find "$1"; the allowlist loader is a second copy of the same shape (structure-check.sh:49-56 vs clean-room-npx.sh:38-42). Fix: one ci/lib-references.sh sourced by both.
- Shotgun Surgery, the skill-name roster: a twelfth skill forces five edits (ci/single-resolution.sh:36, ci/clean-room-npx.sh:23, ci/version-drift.sh:50,53, ci/structure-check.sh:118, tests/pre-plugin-detection.sh:29). Fix: one ci/skills.txt or derive from skills/*/.
- Repeated Switches, [ "$MODE" = github ]: six sites in skills/loop-drive/scripts/receipt.sh (lines 51, 60, 64, 81, 95, 99). Fix: MODE-keyed function dispatch with one startup guard.
- Divergent Change, ci/structure-check.sh (293 lines): edited for eight unrelated reasons; A/B/C/F/G are generic and stable, D/E/H are per-skill content assertions that churn with the prose. Splitting would let the stable half stop changing.
- Duplicated Code, Check D vs the test: tests/pre-plugin-detection.sh strictly subsumes structure-check's Check D. Delete Check D or have CI run the test.
- Mysterious Name: hh (ci/secrets-scan.sh:40), rts/bs/bt (receipt.sh:121, :91), r2/r3 (tests/local-mode-grammar.sh:81,88). Small blast radius; rename on next touch.
- Noted, not flagged: the five duplicated references/*.md pairs are deliberate per-skill self-containment pinned byte-equal by Check C.

## Summary

Spec axis: 5 findings; worst is the permanently-dead version-drift SKILL.md assertion (c1), a silent gate that passes with the flagship skill file deleted.
Standards axis: 6 judgement-call smells, zero hard violations; worst is the five-file skill-name roster (Shotgun Surgery) that every future skill addition must hand-edit.
Per the plan's slip rules, Spec findings slip to the plan's downstream review step; nothing here blocks C7.
