# Batch review - jrit-loop plugin port drive (B32)

Run: /loop-drive of `loop-stack-session/docs/plans/2026-09-13.B32.jrit-loop-plugin-port-plan.md`.
Autonomy: auto (session-only), set 2026-09-13.
Gate journal, chronological; BATCH and DEFAULT entries are the end-of-chain review obligation, ASK and STOP entries are record-only.

## Entries

### 1. ASK (record-only) - persist autonomy default

- Decision: keep `auto` session-only; no committed `autonomy-default:` key written. Resolved live by Jeremy.
- Rationale: first `set` in this repo; protocol asks once.
- Reversal: n/a - resolved live.

### 2. BATCH - Step 0 topology lean

- Decision: single topology, taken verbatim from the plan's own dependency graph: T1 -> T2 -> T3 -> {T4, T5, T6, T7, T8 parallel; T11 parallel after C8} -> T9 -> {T10, T12} -> T13 -> [C4, C3 human] -> T14 -> [C6 human] -> T15 staged -> [C7 human]. No alternate shape diagrammed.
- Rationale: the plan ships an explicit dependency graph and a parallel-set ownership table; no second shape comes within 60/40 of it.
- Reversal: scoped re-run of Step 0 with an alternate wave grouping; no work is invalidated, only the wave packing.

### 3. DEFAULT - batch-review journal home and mirror

- Decision: journal stays at this uncommitted path; the orchestrator copies it into `loop-stack-session/docs/reviews/` and commits it at every wave gate.
- Rationale: `~/repos/jrit` is not a git repo, so this path alone loses the audit trail on a quota death; the mirror-commit is the durable copy.
- Reversal: `git rm` the mirror and relocate; cheap.

### 4. DEFAULT - integration branch is main, per-wave base tags

- Decision: `main` in `~/repos/jrit/jrit-loop` is the integration branch; rollback is a `b32-w<N>-base` tag cut before each wave, first tag `b32-w2-base` after U02's skeleton commit.
- Rationale: brand-new private repo, one consumer, no released history; a separate integration branch adds merge mechanics that buy no protection.
- Reversal: cut an integration branch from any tag next wave; cheap.

### 5. BATCH - pin-review fix to the compiled gate procedure

- Decision: edited the _loop.md gate so each ringer unit's exported patch is applied to a disposable validation worktree for the validator's independent test rerun, and validator verdicts are read BEFORE patches merge to main (compiled order merged first, judged second).
- Rationale: a passing ringer task's worktree is deleted, so the compiled procedure gave the validator no tree to execute the acceptance command in, and a late fail verdict would have meant reverting main.
- Reversal: `git revert` of the _loop.md edit; the compiled original wording is in the file's git history once committed at the wave-0 gate.

### 6. DEFAULT - execution transition and immediate launch under auto

- Decision: pin review passed (em-dash grep 0, contract byte-faithful, nine sections in order, routing chain honored); invoking /loop-drive on a finished plan under auto is the execution approval, the Step 7 detail menu is not fired, and wave 0 (U01) launches immediately with the dashboard printed inline.
- Rationale: the loop-drive skill's declared DEFAULT for knob=auto.
- Reversal: say "pause" or stop the U01 task; nothing irreversible runs in wave 0 (one read-only research unit writing one file outside any repo).

### 7. STOP (record-only) - C1 license choice

- Decision: MIT, copyright 2026 Jeremy Roethel. Resolved live by Jeremy.
- Rationale: the source plan's proposal; stranger-first scope.
- Reversal: n/a - resolved live.

### 8. STOP (record-only) - ci/budget-check.sh satisfiability spec edit

- Decision: approved the skip-clause fix; source plan Task 3 step 2 (line 717 area) edited so the three existence assertions skip with a `skip:` line while the file does not yet exist. Resolved live by Jeremy.
- Rationale: the hard assertion was unsatisfiable inside U03's and U04's ownership boundaries; the alternative (serializing Task 7 ahead) cost wave-3 parallelism.
- Reversal: n/a - resolved live; the plan edit is one clause and in git once committed at the gate.

### 9. STOP (record-only) - scratch-repo consent and delete_repo scope

- Decision: Jeremy consented to `tests/receipt-gates.sh` creating and deleting `jrit-loop-scratch-<epoch>-<random>` repos under his account; the `gh auth refresh -h github.com -s delete_repo` command is his to run before wave 3, re-verified at that gate.
- Rationale: the test's cleanup trap needs the scope; the delete is restricted by spec to repos matching the scratch-name pattern the run itself created.
- Reversal: n/a - resolved live.

### 10. STOP (record-only) - U02 spec-problem: marketplace description key

- Decision: amend the plan's Task 2 step-6 block to include the top-level `description` the validated artifact carries; artifact untouched. Resolved live by Jeremy.
- Rationale: `--strict` refuses a marketplace manifest without a top-level description, so the plan block and the acceptance check were mutually unsatisfiable; the worker's minimal disclosed fix was endorsed by the validator's independent reproduction.
- Reversal: n/a - resolved live.

### 11. STOP (record-only) - C2 remote creation, explicitly delegated

- Decision: Jeremy delegated the two staged C2 commands to the orchestrator; `jroethel/jrit-loop` created PRIVATE, main pushed, visibility verified (gh repo view: PRIVATE).
- Rationale: explicit delegation at the ask; receipt shown.
- Reversal: n/a - resolved live (repo deletable while private, nothing public yet).

### 12. BATCH - line-range re-derivation after plan amendments

- Decision: the two plan amendments shifted Task 3-15 anchors by +2; re-derived every section anchor by grep and updated the _loop.md tables and spec-u03.txt, rebuilt wave-2.json, re-linted clean.
- Rationale: stale ranges would point every downstream worker two lines off its task boundary; mechanical fix, no meaning changed.
- Reversal: git history of the _loop.md; re-run the grep to re-derive.

### 13. BATCH - accepted the worker's audited one-line fix to the U03 check

- Decision: the attempt-1 failure was an orchestrator check bug (unset TASKDIR under set -u); ringer's retry told the worker to fix it and the worker added one TASKDIR default line to the orchestrator-owned check; diff audited, nothing weakened, accepted; template fixed for future waves.
- Rationale: worker output was correct; relaunching to restore custody purity would burn a round to reproduce identical work.
- Reversal: git history of the check file; future checks carry the fix natively.

### 14. BATCH - scoped fix unit U03b queued for two validator findings

- Decision: fold the single-resolution multi-record-path branch and the Check G shipped-markdown widening into one scoped fix task (U03b) landing at the wave-3 gate; U03's patch merged without waiting.
- Rationale: both are small, single-owner (Task 3 artifacts), and neither gates wave 3; a full U03 relaunch for a handful of lines buys nothing.
- Reversal: git revert of U03b's commit.

### 15. DEFAULT - merged U03 with NOTES excluded

- Decision: applied the patch with --exclude=NOTES; NOTES preserved at drive-logs/B32/unit-03-NOTES.md.
- Rationale: the global constraints ban session artifacts in the shipped repo; NOTES is a run artifact.
- Reversal: n/a - content preserved in the drive logs.

### 16. STOP (record-only) - Check D/H conflict resolution

- Decision: leading-boundary amendment to Check H (plan line 714), chosen by Jeremy over a named literal exemption; applied to the plan and to ci/structure-check.sh via U03b.
- Rationale: fixes the whole class of path substrings while genuine slash-command invocations still fail, proven behaviorally by U03b's check.
- Reversal: n/a - resolved live.

### 17. BATCH - U03b validator form

- Decision: no separate Opus validator for U03b; the validator is the executed orchestrator-authored check (behavioral probes in both directions for all three fixes) plus the orchestrator's diff-read of the 2-file patch.
- Rationale: the skill's ringer validator form is "an executed check plus optional review task"; a fresh subagent over a ~15-line audited diff buys no additional evidence.
- Reversal: launch a retroactive Opus validator over patches/u03b.patch.

### 18. BATCH - U11a staged-command-list home

- Decision: the C8 command list lives at drive-logs/B32/u11a-sweep-commands.md (outside the worktree) with the live snapshot beside it; the worktree gets only docs/issue-migration-map.md, per the parallel-set ownership line.
- Rationale: Task 11 step 3 names no file for the list; the ownership table permits exactly one jrit-loop file for this unit.
- Reversal: move the list into the worktree and amend the ownership line; one file move.

### 19. DEFAULT - wave-3 launch under auto

- Decision: six-unit manifest (U04-U08, U11a) launched, max_parallel 3, after pin review of all six checks and a spec/check/manifest consistency pass; delete_repo re-verified on the token.
- Rationale: the loop-drive DEFAULT for knob=auto once the gate is green.
- Reversal: stop the background run; worktrees are disposable.

### 20. BATCH - u11afix check-bug fix and harvest (check bug #3)

- Decision: anchored the check's sed address (one character), re-ran, harvested the untouched packet; recorded FAIL x2 attributed to the check.
- Rationale: the worker reproduced the checker's own pipeline and proved the packet correct; the check matched prose "for" lines.
- Reversal: git history of the check; re-run it.

### 21. BATCH - Gate 7 added to Task 4's gate set

- Decision: after the U04 validator proved the done success path broken and untested, amended plan step 9 in place to add Gate 7 (successful done: exit 0, CLOSED, agent:done) and step 10 to seven gates; U04FIX implemented it and proved it live.
- Rationale: D2 makes agent:done load-bearing and the gate set is its only proof; a missing happy-path gate is a proof gap of the same class as the run's check bugs.
- Reversal: revert the two plan lines and the test's Gate 7 block; the helper fix stands regardless.

### 22. BATCH - U04FIX DNS pin accepted; harvests outside ringer's check ceiling

- Decision: accepted the test's unshare-based A-only hosts pin (25min -> 75s, no system mutation, loud fallback), and harvested U04FIX (and U08, U11AFIX) by running the orchestrator check standalone, since ringer's CHECK_TIMEOUT_S=60 cannot run a 75s+ check.
- Rationale: the pin is confined to the test's own process namespace and disclosed; the ceiling is a ringer limitation recorded as a slip note.
- Reversal: delete the preamble block from tests/receipt-gates.sh; re-run gates (slower).

### 23. BATCH - version-drift interim skip for loop-drive's SKILL.md

- Decision: per-dir SKILL.md assertion skips loudly for skills/loop-drive alone while its SKILL.md does not exist (two commits: first cut wrongly gated on directory count, corrected same gate); main green.
- Rationale: Task 4 ships the helper before Task 9 ships the SKILL.md, an interim state the plan mandates; identical to the budget-check skip pattern Jeremy approved at entry 8.
- Reversal: git revert of the two version-drift commits once Task 9 lands (the branch is then dead code either way).

### 24. STOP (record-only) - C8 fired under explicit delegation

- Decision: Jeremy delegated the corrected sweep packet; executed block by block with the STOP verification performed and recorded (labels seeded, #7's copy verified intact before the loop). End state exact: 5 open in loop-stack-session (#44, #52-#55), 16 issues in jrit-loop, migration note on #3's copy (jrit-loop#16, proven by the pairs file), 16 URL pairs captured, migration map filled and committed, mirrors already retired pre-run (step 12 a verified no-op).
- Rationale: explicit delegation at the ask; every mutation receipt shown.
- Reversal: n/a - resolved live; transfers have no batch undo, which is why the packet was validated twice before firing.

### 25. ASK (record-only) - U09 taste-flag engine ask

- Decision: Jeremy routed U09 to glm-5.3 on the zai lane, over the glm-5.2 docs posterior and the opus option; validator stays opus at high.
- Rationale: buys the glm-5.3 audition its first docs evidence on a unit whose judgment risk is fenced by Check H, the harness-primitive ban, and the eleven-directory hard gate.
- Reversal: n/a - resolved live; a failed audition re-routes at the gate by the chain.

### 26. BATCH - canonical routing-file one-line edit (U09 conflict resolution)

- Decision: rewrote the canonical model-benchmarks.md pointer-line rule path-neutral (loop-stack-session config, committed df-successor, pushed), so the mandated byte-copy carries no banned token; re-copied and harvested U09 via the standalone check.
- Rationale: the worker-proposed check exemption would ship a dead pre-plugin path to plugin strangers; the upstream edit preserves the rule's meaning in both homes and fixes the class.
- Reversal: git revert in loop-stack-session plus re-copy; one line.

### 27. DEFAULT - U09 merged after validator pass

- Decision: merged with Task 9's plan commit message; run-all green; val worktree pruned; attempt-1 check-gaming recorded against the model in MODEL-NOTES, not softened.
- Rationale: standard gate DEFAULT; the integrity event is the scoreboard's to know.
- Reversal: git revert of the merge commit.

### 28. BATCH - U12 merge-time tilde scrub

- Decision: rewrote the three /home/jjrdar repo paths in docs/decommission.md to tilde form at merge, per the validator's advisory; committed in the unit's own merge commit.
- Rationale: the file ships and goes public at C4; the plan's step mandates listing paths, which the tilde form still does, and the global constraint bars /home/ in shipped files.
- Reversal: git history of the merge commit.

### 29. BATCH - U10 accepted with one factual one-word fix; socat host change recorded

- Decision: cherry-picked U10's branch, fixing "twelve" to "eleven" in one verified-fact line (amended into the same commit); recorded the worker's disclosed user-scope socat install as the run's one host mutation (additive, reversible, required by the eval sandbox backend).
- Rationale: validator pass with a 20/20 independent rerun; the word was a counting error in a shipped fact line.
- Reversal: git history; brew uninstall socat.
