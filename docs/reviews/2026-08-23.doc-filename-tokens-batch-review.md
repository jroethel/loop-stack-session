# Batch review - doc-filename-tokens run - 2026-08-23

Gate journal for the /loop-drive run of docs/plans/2026-08-23.doc-filename-tokens-plan.md.
BATCH and DEFAULT entries are auto-taken decisions to accept or reverse at the end-of-chain checkpoint.
ASK and STOP entries are record-only.

## Entries

### 1. [BATCH] Step 0 topology: ringer transport, 4 waves, Task 7 in the orchestrator lane

- Decision: single topology - ringer manifests for Tasks 1-6 across waves 1-3, Task 7 kept in the orchestrator's own lane.
- Rationale: ringer is present (engines: codex, claude, claude-zai, opencode; root /Users/jjrdar/repos/ringer), so ringer is the default transport.
  Task 7 runs install.sh, which writes to ~/.claude/skills - outside the workspace-write sandbox and outward-facing, so it cannot be a worker unit.
  No competing shape came within 60/40: native subagents lose the scoreboard evidence lane for no gain.
- Reversal: scoped re-run with Agent-tool transport for any wave, before that wave launches.

### 2. [ASK] Class f regex vs _loop.md convention - resolved: allow optional _loop suffix

- Decision: the human chose extending the grammar with `(_loop)?` before `\.md$`.
  Spec edits applied to the source plan: global constraint, Task 1 doctrine bullet and acceptance check, Task 5 class f code, Task 5 test fixture and assertion, Task 7 dogfooding note.
- Rationale: loop-drive emits `...-plan_loop.md` into docs/plans/ dated on the cutoff date; stem_of already pairs `_loop` names with their plan, so the regex had to admit them or Task 7's zero-findings check fails.
- Reversal: n/a - resolved live.

### 3. [STOP] Pre-flight dirty tree - resolved: leave untracked

- Decision: WAYFINDER.md and docs/memos/loop-setup-virgin.md stay untracked; no unit owns either, worktrees branch from committed state.
- Rationale: untracked files outside every unit's ownership cannot collide at a gate.
- Reversal: n/a - resolved live.

### 4. [STOP] Task 7 install.sh outward gate - resolved: ask again at wave 4

- Decision: the human declined pre-approval; the run halts at wave 4 and re-confirms before install.sh writes to ~/.claude/skills.
- Rationale: outward-facing step stays the human's trigger.
- Reversal: n/a - resolved live.

### 5. [BATCH] Pin review of the compiled _loop.md - four fixes applied

- Decision: accepted the drive-compile output (routing all-glm-5.2/claude-zai on posterior evidence, custody-hardened checks for T2/T5/T6) with four orchestrator fixes.
  (a) Reformatted prose to one sentence per line (house style).
  (b) Added the base-branch rule: check out doc-filename-tokens-integration before every wave launch, since wave 2+ specs depend on earlier waves' commits.
  (c) Fixed T7's model cell and footnote: orchestrator lane is the driving session, not a dispatched Opus.
  (d) Made the lint/run manifest paths absolute (relative paths from the ringer repo root would not resolve).
  All three manifest JSON blocks verified parseable (2+3+1 tasks).
- Rationale: (a) hard house-style requirement; (b) T2's fresh-stamp assertion needs T1's template placeholder in the branch base; (c) Fable is orchestrator-tier and T7 was human-gated to the session; (d) cwd at run time is the ringer repo.
- Reversal: git revert of the _loop.md edits; the compile agent (a2530e0dd51f29e89) can be continued for a recompile.

### 6. [BATCH] Ringer lint corrections: patch-export pattern and check custody restructure

- Decision: rewrote all six worker tasks to ringer's worktrees-mode contract after lint rejected the compiled shape.
  Workers no longer commit (no-commit contract in every spec); every check exports `git add -A && git diff --cached` to `<workdir>/patches/<key>.patch` and fails on an empty patch; `expect_files` point at the exported patches; checks no longer execute worker-owned test files (T6 byte-diffs and runs a spec-embedded copy; T2/T5 verify verbatim content by grep, with owned-suite execution moved to the gate's full-suite run on the integration branch).
- Rationale: ringer lint findings (worker commits die with worktrees; relative expect_files deleted with worktrees; a worker must not be able to rewrite its own acceptance check); the substrate's documented pattern is uncommitted-plus-export.
  The compile instruction to have workers commit was the orchestrator's error, corrected here.
- Reversal: git revert of the manifest/_loop.md commit; the retry trade-off (a red owned-suite now surfaces at the gate instead of the worker retry loop) can be revisited per wave.
