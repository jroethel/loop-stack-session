# Seam 1 propagation runbook: template-version 7 across this Mac (#52)

Date: 2026-09-08 (UTC), wave 5 of the seam-1 run.

## Pre-roll state (verified before the human go)

| Repo                         | template-version | Tree before the roll                     |
| ---                          | ---              | ---                                      |
| `create/pokemine`            | 6                | 7 dirty files, none in config/           |
| `create/skills/rubix-review` | 6                | clean                                    |
| `create/ai-benchmark`        | 4                | 5 dirty files, none in config/           |
| `create/iamawriter`          | 4                | 5 dirty files, none in config/           |
| `create/substack-scraper`    | 4                | 3 dirty files, none in config/           |
| `create/sys-prompts-cc`      | 4                | 3 staged (the T8 pilot, Jeremy's commit) |
| `create/vaultwise`           | 4                | 3 dirty files, none in config/           |

No repo carried an uncommitted `config/` edit, so the reconcile's replace-both-files operation put
nothing unsaved at risk. This repo itself was re-rendered to v7 at the wave-4 gate (Task 10 Step 6,
pulled early so the version-coupled suite stayed green; journaled as D12).

## The human go

Jeremy answered the wave-5 checkpoint: roll all seven.

## Roll results (one repo at a time, rubix-review as the clean canary)

Each row verified after its roll: `template-version: 7`, tracker value unchanged, `rubix-autorun`
value unchanged, `filename-grammar-since` carried where present, the three scripts
(`session-card.sh`, `handoff-log.sh`, `take-stock.sh`) vendored executable, and `lifecycle-lint.sh`
run.

| Repo                         | Rolled | Tracker carried | Scripts | Lint after                            |
| ---                          | ---    | ---             | ---     | ---                                   |
| `create/skills/rubix-review` | v7     | github          | 3/3     | exit 0                                |
| `create/pokemine`            | v7     | github          | 3/3     | exit 0                                |
| `create/ai-benchmark`        | v7     | github          | 3/3     | exit 0                                |
| `create/iamawriter`          | v7     | github          | 3/3     | exit 0                                |
| `create/substack-scraper`    | v7     | github          | 3/3     | exit 0                                |
| `create/sys-prompts-cc`      | v7     | github          | 3/3     | exit 1, pre-existing (see below)      |
| `create/vaultwise`           | v7     | github          | 3/3     | exit 0                                |

sys-prompts-cc's lint exit 1 is four class-a findings on its own superseded 2026-08-23 plan-sets,
present before the roll and untouched by it; no class f or h anywhere. That sweep belongs to a
sys-prompts-cc session, not this run.
`lifecycle-lint-since:` was added to no rolled repo, by design: class h stays dormant per repo until
that repo adopts a date.
Each completed roll was logged on the run's session card as it happened, so a mid-loop death would
have named exactly which repos were done.

## Commit sweep (2026-09-08, the close-out session)

The roll above left every one of the seven repos with the v7 files uncommitted - a `git checkout`
in any of them would have discarded it. Each repo now carries one path-scoped commit,
`config: roll template v7 ...`, holding only `config/repo-state.md`, `config/conventions.md`, the
vendored scripts, and the regenerated mirrors; the pre-existing dirty files each repo already had
(pokemine's app work, vaultwise's untracked HTML) were left untouched. sys-prompts-cc's commit also
carries the T8 pilot's handoff dispositions, which were uncommitted for the same reason.
`template-version: 7` is verified in HEAD in all seven. The seven commits are local: pushing them is
a separate step.

## Live board evidence (criteria 3 and 5, from the wave-5 renders)

- Lanes after the first render: `handed-off` 9 cards (live handoffs across the conforming repos),
  `done` 61 (closed issues inside the 60-day window plus closed session cards), `blocked-on-fact` 0
  because no session card anywhere was closed `blocked` - correct emptiness.
- Scratch proof, produced and reverted, both renders recorded: a card closed `blocked` rendered into
  `blocked-on-fact` and left after deletion; a backdated open card rendered `marker: died-mid-work`
  and left; a future-dated scratch commit rendered `marker: went-stale` on this repo's git card and
  left after `git reset`.
- Real signal that stayed: `iamawriter`, `pokemine`, and `substack-scraper` render `went-stale` on
  their git cards - their live handoffs date before their newest commits, which is exactly the
  skipped-recording visibility the convention exists to provide.
- The marker's only view surface is the rendered title prefix (`[went-stale]` / `[died-mid-work]`)
  plus the filterable `marker` frontmatter property; `assets/board/by-lane.base` was deliberately not
  changed - adding `marker` to the view orders is a one-line follow-up if prefix-filtering proves
  clumsy.

## Criterion 7 verdict (Jeremy, the closing judgment, answered 2026-09-08)

- 7a recording tax: acceptable; the doctrine stands as shipped.
- 7b resume answer: yes, after two live adjustments made during the checkpoint - git-card titles now
  show the directory name instead of the scan-root path (shipped in this run), and the Entry-vs-Next
  wording in the resume block was explained (Entry = which /loop-* door, Next = the literal first
  commands) with a rename candidate added to the close-out list.
- 7c died-mid-work threshold: signal; 24 hours stays.

## Close-out list (carried on the next-thread handoff)

- Rename or caption the resume block's `Entry:` / `Next:` lines so the distinction is self-evident.
  Done 2026-09-08: the lines now read `Start here:` and `First commands:`.
- The `is_date` guard in take-stock's authoritative-handoff gather (wave-2 validator note).
  Done 2026-09-08.
- sys-prompts-cc's four pre-existing class-a plan-set findings, swept in a session there. Still open;
  that repo's uncommitted v7 roll was committed in the sweep above, the lint findings were not.
