# Batch review journal: seam 1 recording convention run (#52)

Date opened: 2026-09-07.
Chain: handoff resume prompt -> seam-1 brief -> /loop-plan -> (this journal spans through /loop-drive).
Autonomy: `auto` (docs/chain-state.md); ASK-class gates surfaced to Jeremy, DEFAULT/BATCH auto-taken and journaled here with reversal paths.

## User decisions (ASK, answered live 2026-09-07)

- U1 Session-card terminal statuses: `done, blocked, handed-off, parked, aborted`.
- U2 Session-card write path: helper script `scripts/session-card.sh` plus conventions doctrine.
- U3 Done-lane closed-issue lookback: 60 days (Jeremy's own value; the offered options were 30 and 14).
- U4 The one-time handoff backfill rides this run, dispositions confirmed at a human checkpoint before any move.

## Auto-taken (DEFAULT / BATCH)

### D1 - Brief written directly, no /loop-brainstorm dialogue (DEFAULT)

- Taken: the seam-1 brief was drafted directly from the parents, per the handoff's "bounding, not invention".
- Reversal: the brief is one file; rewrite through /loop-brainstorm if the bounding missed content.

### D2 - Handoff count correction carried into the brief (DEFAULT, reconstruction)

- Taken: the handoff says 11 deferred decisions; the MVP table holds 12 defer verdicts; the brief records 12.
- Reversal: none needed; disk is the source.

### D3 - Plan drafted by a fresh-context writer at the plan-draft role pin (DEFAULT)

- Taken: decompose + draft + self-review dispatched per the loop-plan skill; the driving session then re-verified
  the writer's factual claims by a second route (lint baseline, sys-prompts-cc handoff set, anchors, test fixtures).
- Reversal: the plan is one file; any task is re-cuttable before drive.

### D4 - Two plan defects fixed inline at plan review (DEFAULT)

- Taken: (a) Task 9 now folds the live `conventions.md` rubix-autorun doctrine block into the template before the
  refresh copy - a bare copy would have deleted shipped doctrine; (b) the seam-1 driving handoff is kept live through
  the run and consumed at Task 10 close instead of being archived mid-run by Task 6.
- Reversal: both are plan-text edits, revisable at the Task 6 checkpoint or before drive.

### D5 - Rubix review runs (DEFAULT; `rubix-autorun: ask` under autonomy auto)

- Taken: the two-lens review dispatched on the plan (Lens A impacted professional on sonnet, Lens B cold craft on
  opus), matching the MVP run's precedent; findings triaged as BATCH below when they land.
- Reversal: findings are advisory; the cost of having run it is tokens only.

### D6 - Rubix triage, 25 findings (BATCH)

Lens A seat: Jeremy as solo owner living with the convention. Verdicts applied to the plan in place; every
accepted change is a plan-text edit, reversible before drive.

| #   | Sev      | Verdict | Disposition                                                                  |
| --- | ---      | ---     | ---                                                                          |
| A1  | critical | accept  | Doctrine pointer added to repo-state.template + context-map (Task 9)         |
| A2  | high     | decline | Went-stale, died-mid-work, and unrecognized-status-to-next-up already        |
|     |          |         | surface all three failure shapes; a session lint class is redundant          |
| A3  | high     | accept  | `handoff-log.sh disposition <h> all <d>` bulk verb added (Task 2)            |
| A4  | high     | accept  | Task 6 checkpoint split into Step 3 and Step 3b, one decision each           |
| A5  | moderate | decline | The status vocabulary is now Jeremy's live decision (U1), superseding the    |
|     |          |         | WSL record; reconciliation pull stays possible before any WSL roll          |
| A6  | moderate | accept  | 24h threshold named as a calibration question at the criterion-7 checkpoint  |
| A7  | moderate | accept  | Archive collision auto-suffixes `-2`, matching session cards (Task 2)        |
| A8  | low-mod  | accept  | Log cadence floor pinned: at minimum once per commit (Task 9 doctrine)       |
| A9  | high     | accept  | Per-repo session-card log line inside the Task 10 propagation loop           |
| B1  | critical | accept  | Heading prefix-match + numbered-item enumeration; Task 8 rewritten to real   |
|     |          |         | per-action dispositions (premise re-verified against disk: B1 was right)     |
| B2  | high     | accept  | Zero-action files derive live; action-0 bulk line is the only consume path   |
| B3  | high     | accept  | Announcement paths print exactly as given; test grep fixed                   |
| B4  | high     | accept  | Same-second open-card tie now exits 7 naming the tied files; the first fix   |
|     |          |         | attempt (lexical tie-break) was itself wrong (`-2.md` sorts before `.md`)    |
| B5  | high     | accept  | Done/aborted session cards bounded by DONE_LOOKBACK_DAYS; graduation rule    |
|     |          |         | for docs/sessions declined (one file per session is immaterial growth)       |
| B6  | high     | accept  | Clean-tree precondition per repo at the Task 10 checkpoint; sys-prompts-cc   |
|     |          |         | commit is Jeremy's explicit gate there                                       |
| B7  | moderate | accept  | Backfill routed through `disposition all` so records read back `consumed`;   |
|     |          |         | criterion 1 exercised live on sys-prompts-cc's real numbered handoffs        |
| B8  | moderate | accept  | In-task regression gates scoped to affected suites; full run at wave ends    |
| B9  | moderate | accept  | Fourth fixture repo (clean + went-stale) tests the suppression exception;    |
|     |          |         | vacuous repoB assertion replaced with a suppression assertion                |
| B10 | moderate | accept  | Authoritative-handoff tie-break pinned: same date, lexically last filename   |
| B11 | moderate | accept  | Phantom by-lane.base step replaced; title prefix named the only surface      |
| B12 | moderate | accept  | Commit-day read pinned to UTC via TZ=UTC + --date=format-local              |
| B13 | moderate | accept  | Closed path inherits wayfinder skip + B-token rewrite, with test assertions  |
| B14 | low      | accept  | docs/sessions/ added to the hardcodes sweep ALLOW list (Task 1)              |
| B15 | low      | accept  | take-stock call gets </dev/null inside the discovery read loop               |
| B16 | low      | accept  | rubix-autorun carry-forward validates against ask/off/on before sed          |

Also folded while editing: `consume` refuses zero-action files (vacuous-consume guard, an implication of B2);
`handoff-log.sh` archive destination derived by segment replacement so cross-repo paths archive in their own repo.
Declined findings carry no plan change; their reversal is to reopen them at the criterion-7 checkpoint.

### D7 - Plan and journal committed on main as the chain checkpoint (DEFAULT)

- Taken: commit 79f3988 (brief + plan + journal), matching the house pattern of docs artifacts on main.
- Reversal: `git revert 79f3988`.

### D8 - Drive route and worker lane split (BATCH)

- Taken: multi-wave dependent build; ringer present (codex, claude, claude-zai, opencode; root `~/repos/ringer`).
  Tasks 1, 2, 3, 4, 5, 9 dispatch to workers (cheap executed checks); Tasks 6, 7, 8, 10 stay orchestrator-lane
  (human checkpoints, writes outside this repo, the run's own session card - they fail the checkability gate
  or the outward-facing rule, not the difficulty test).

```
orchestrator (this session)
  wave 1: [T1 session-card] [T2 handoff-log+lint-h] [T4 tsv-field-13]   -> validators -> gate
  wave 2: [T3 take-stock]                    + orch: T6 backfill (human checkpoint 3/3b)
  wave 3: [T5 board wiring]                  + orch: T8 sys-prompts-cc pilot
  wave 4: [T9 template roll v7]              + orch: T7 dogfood this repo
  wave 5:                                      orch: T10 live board + propagation (human gates)
```

- Reversal: the lane split is per-wave revisable at any gate; a stalled orchestrator-lane task can be re-cut
  into a dispatchable unit by adding an executed check.

### D9 - Pin review of the compiled _loop.md: pass, two deviations accepted (BATCH)

- Taken: check custody via canonical-overwrite (T1/T2/T3) and canonical-assertion-presence (T4/T5/T9) instead of
  barring workers from test files - the source plan is test-first by design and the judging text lives in the
  manifest check, out of every worker's reach, so the rule's substance (a worker cannot edit its own success
  criterion) holds. In-place integration branch in the primary checkout accepted because Tasks 8 and 10 name
  that absolute root. Routing: glm-5.2 on claude-zai for all six worker units (79-task posterior, 91% first
  try), opus validators, effort per the routing table.
- Reversal: any unit re-routes at its gate by the same chain; the branch is deletable.

### D10 - Launch under auto (DEFAULT) and the pre-flight context-map commit (BATCH)

- Taken: Step 7's pre-launch ask is never fired under auto; dashboard and watch points print inline, launch
  proceeds after the dry-run pre-flight passes. The uncommitted `config/context-map.md` user edit (one line,
  research-corpus path, correct per the source plan's Task 9 note, surfaced to Jeremy at session start) is
  committed on its own before wave 1 because T9 patches that file from a worktree forked off committed state;
  committing it verbatim preserves it exactly, which is the substance of the dirty-tree gate.
- Reversal: `git revert` the standalone context-map commit.

## Wave 1 gate (opened 2026-09-08 UTC)

Launch: run `i52-seam1-recording-convention-20260908T014647Z-p68001`, three glm-5.2/claude-zai workers.
Run JSON verdicts: t1 PASS attempt 1, t2 PASS attempt 1, t4 FAIL after 2 attempts.

### D11 - t4 FAIL attributed to a check bug, not the model (BATCH)

- Attribution, re-derived from the raw worker log: the check's required-assertion audit grepped all eight
  messages in `tests/board/cards.sh`, but the source plan places six of them in `tests/board/render.sh`;
  the check also ran only the cards suite as acceptance. The worker's second attempt appended an inert
  comment block to cards.sh purely to satisfy the bad grep - forced junk, not committed.
- Taken: fixed the check in `emit.py` (per-file assertion audit, both suites run), re-emitted, committed
  (8ce260f), relaunched t4 as a one-task manifest with its dead worktree force-removed first.
  Not counted as a failed round for glm-5.2; MODEL-NOTES gets a check-bug attribution line at gate close.
- Reversal: the retry manifest is `wave-1-t4-retry.json`; a genuine second failure re-routes by the chain.

t1 and t2 patches applied and committed (6211ab5, 2ed3dd6); both suites re-run green by the orchestrator
on the integration branch. Opus validators for t1 (med) and t2 (high) launched; gate closes when t4's
retry and both verdicts land.

### Gate 1 closed (2026-09-08 UTC): all three units pass

- Verdicts: t1 pass (validator walked every exit code and edge, test verbatim-identical to the plan);
  t2 pass (live positive control on the shipped adoption date 2026-09-07; single-home derivation
  confirmed; live-repo lint exactly at baseline, zero class h); t4 pass with repairs=1, both repair
  rounds attributed to orchestrator check bugs (D11 plus a second: the portability grep matched a
  pre-existing header comment naming the banned constructs - fixed in emit.py, comment restored
  verbatim before commit aee79c1, MODEL-NOTES carries both attributions, committed in the ringer repo).
- Integration branch: 58/58 suites green at 426e4e7. AGENT STATUS receipts updated on issue 52.
- Distilled into the templates before wave 2: per-file assertion audits, comment-exempt portability
  grep. Validator side notes logged: t1's trailing-space-on-empty frontmatter values (covered - the
  plan's take-stock fm() shape strips trailing whitespace); t2's flag that class h needs handoff-log.sh
  vendored alongside lifecycle-lint.sh (covered - T9 vendors all three new scripts); t4's note that my
  validator brief named two nonexistent test files (validator ran the plan's real glob; brief text
  corrected for reuse).
- The class-a lint baseline text changed meaning mid-run as expected: the three class-a findings now
  read "superseded by the 2026-09-07 cycle" because this run's plan-set exists; same three files, same
  count, recorded as the baseline going forward.

## Wave 2 gate

Launch: t3 dispatched (its known `instructs_git_commit` lint false positive on the fixture heredoc
re-attributed, per the pre-flight authorization); T6 executed orchestrator-lane in parallel.

### U5 / U6 - The two wave-2 STOPs, answered live 2026-09-08

- U5 backfill: Jeremy confirmed all 19 proposed dispositions unedited; the moves ran via
  `handoff-log.sh disposition <f> all <d>`, all announced; `docs/handoffs/` now holds exactly the
  live seam-1 driver; spot-checked archives derive `consumed`; lint shows class f cleared, zero
  class h, class a exactly at baseline. Record: `docs/reviews/2026-09-07.I52.handoff-backfill-table.md`.
- U6 plan-set (separate prompt): leave the three class-a-flagged plan files live in the baseline;
  archival lands at run close-out.

t3 PASS first try; patch applied and committed, its suite plus the full 59/59 run green, and the live
`take-stock.sh .` names the authoritative handoff with a correct went-stale flag (no session card
exists until T7 - the flag is honest, not a bug). Opus validator (med) launched; gate closes on its
verdict.

### Gate 2 closed: t3 validated pass (mutation-tested, 4/4 catches)

Two non-blocking validator findings, recorded and slipped to close-out rather than churned mid-run:
died-mid-work fires at the 25th idle hour (hour truncation; an in-spec reading, and the criterion-7
checkpoint already asks whether the threshold reads right); a non-date-named file in `docs/handoffs/`
could hijack the authoritative-handoff line (no conforming repo hits it, lint class f enforces the
grammar; the one-line `is_date` guard is a close-out candidate). MODEL-NOTES and AGENT STATUS
receipts written and committed. Wave 3 launches: t5 board wiring dispatched; T8 sys-prompts-cc pilot
runs orchestrator-lane.

## Wave 3 gate

t5 FAILed twice on a third check bug, environmental this time: the scoped gate ran the full
`tests/run.sh` inside a fresh worktree, where the gitignored `ISSUES.md` mirror cannot exist and
`tests/repo-state/live.sh` fails regardless of the work. Attribution per the slip rule: the preserved
worktree's acceptance suite passed under my own run and the diff touched exactly the four owned files,
so I exported and committed the audited work myself, fixed the check (worktree runs skip live.sh -
also pre-fixing t9's identical hazard), and wrote the MODEL-NOTES attribution. Not a model fault.
Orchestrator follow-up commit (2c2ee02): the one-line `_health.md` suppression-exception prose the
worker correctly left outside its ownership, plus the emit.py fix.
T8 ran orchestrator-lane: sys-prompts-cc proof of value PASSED live (criterion 4) - authoritative
handoff named, both older handoffs consumed per-action through `handoff-log.sh` (real numbered
sections, 5 actions each), auto-archived announced, that repo left staged for Jeremy, evidence in
`docs/reviews/2026-09-07.I52.sys-prompts-cc-proof-of-value.md`.

### Gate 3 closed: t5 validated pass (high effort, 11 independent probes)

Validator highlights: mutation test proved the `</dev/null` stdin guard load-bearing; real
end-to-end take-stock-to-board-cards run against this repo carried both markers; the worker's
disclosed open-wins guard weakens no contract (backends keep the lists disjoint) and was the
lower-risk move given the verbatim test block. Observations recorded, none blocking: duplicated
days-civil arithmetic (justified, awk-embedded original), ambient-locale date compares in the two
pre-existing scripts (harmless, fixed-width), a plan-mandated semicolon and usage-line spacing.
Wave 4 launches: t9 template roll dispatched; T7 dogfood runs orchestrator-lane.

## Wave 4 gate

T7 ran orchestrator-lane and passed: the run's session card is live with per-task log lines, the
next-thread handoff exists with real `## Next actions`, and criterion 2's standalone resume evidence
is recorded. The session survived a quota break mid-wave and resumed from git alone.

### D12 - t9's two ringer ERRORs attributed to an unsatisfiable check; salvage and early Step 6 (BATCH)

- Attribution: the scoped gate ran `docs-gitlab.sh`, which asserts the live `config/repo-state.md`
  matches the template version - a file outside t9's ownership - so the check could never pass once
  the worker bumped the template. Both ERROR verdicts were the check failing with no patch export,
  not engine faults. Fourth check bug of the run; MODEL-NOTES carries it.
- Salvage: attempt 1's owned work was audited and committed restricted to the seven owned files;
  attempt 2's out-of-ownership `repo-state.md` hack (which dropped `lifecycle-lint-since` and
  `tracker:`) was rejected unseen. Retry pressure pushed the worker across its ownership boundary -
  worth remembering.
- Task 10 Step 6 (this repo's v7 re-render) pulled to this gate for version consistency: the real
  reconcile preserved all five keys exactly once, `conventions.md` is byte-identical to the template,
  full suite 59/59. Step 6 was never behind the propagation STOP, which covers the seven other repos.
- Validator: pass, 22/22 criteria, unsatisfiable-check diagnosis independently reproduced from the
  suite source.
- Reversal: both commits revert independently.

### D12a - An orchestrator `git reset --hard` cost two uncommitted records (BATCH, self-attribution)

- The went-stale probe cleanup (`git reset --hard HEAD~1`, meant to drop the scratch commit) also
  discarded the then-uncommitted first draft of this Wave-4 journal section and the session card's
  task-9 log line. Both re-derived and re-added; nothing else was uncommitted at that moment.
- Distilled rule for future gates: commit the journal before any `reset --hard`, or reset by
  `git revert`-equivalent paths only.

## Wave 5 gate and run close (2026-09-08)

### U7 - Propagation go: all seven repos (STOP, answered live)

- The checkpoint table showed no repo with uncommitted `config/` edits; Jeremy chose roll-all-seven.
  Every roll verified (v7, keys preserved, 3 scripts vendored, lint clean; sys-prompts-cc's exit 1 is
  four pre-existing class-a findings of its own, untouched by the roll) and logged per repo on the
  session card as it happened. Record: the propagation runbook.

### U8 - Criterion 7 (STOP, answered live): PASS in full

- 7a tax acceptable; 7b yes after two live adjustments (git-card titles show the directory name -
  shipped this run; Entry/Next rename to close-out); 7c 24h stays, called signal.

### D13 - Live-board criteria closed at wave 5 (BATCH)

- Criterion 5: blocked-on-fact/handed-off/done all populate (9 handed-off, 61 done at first render;
  blocked-on-fact proven by a scratch card that appeared and left). Criterion 3: both markers proven
  by produced-and-reverted probes; three repos left genuinely went-stale - real signal, kept.
- Run close: the seam-1 driving handoff consumed (`disposition all executed`) and archived announced;
  the run's session card closed `done` with the criterion-7 verdict; lint exactly at the approved
  baseline; take-stock shows one live handoff (the next thread) as the authoritative pointer.

### D14 - Advisory /loop-review, two axes, non-blocking (BATCH)

- Standards: zero hard violations; four judgment smells, all previously dispositioned or established
  idiom; one low note (the emitter's host-specific patch path, a one-shot orchestration artifact).
- Spec: six findings - four were journaled gate decisions, the died-mid-work hour truncation is the
  behavior Jeremy approved at 7c with the 25h number in front of him, and one new item
  (consumed-unarchived derived by exclusion) joins the close-out list on the next-thread handoff.
- Nothing reopens the run; both full reports live in the session transcript, the dispositions here.
