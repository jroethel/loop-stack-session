# Handoff: board MVP shipped, seam 1 (recording convention) is next (#52)

Date: 2026-09-07.
State: the Obsidian board MVP is merged to main (PR #57, merge 24896aa) and live in the Cortex vault.
Criterion 8 (the human glance) passed live 2026-09-07: "Board is good enough."

## What exists now

- One command renders the board: `scripts/board.sh` (discover | board-cards.sh | board-render-obsidian.sh).
- Entry point in Obsidian: `00_System/Board/Board.md`, live preview - 8 lanes side by side, per-lane embedded Bases views, horizontal scroll, emoji-titled cards, sticky lane headings.
- 56/56 test suites; four board suites under `tests/board/`.
- Run record: `docs/reviews/2026-09-02.I52.obsidian-board-mvp-batch-review.md` (DR1-DR15, every auto-taken decision with its reversal path).
- Integration evidence: `docs/reviews/2026-09-03.I52.board-mvp-integration-runbook.md`.
- Spike findings (Bases behavior on this host): `docs/spikes/2026-09-02.I52.board-bases-spike-findings.md`.

## What seam 1 delivers

Three lanes render empty because their data source does not exist yet.
Seam 1 is that source: the recording convention.

- Session cards (`docs/sessions/`): a per-session record each work session writes - what ran, where it stopped, why.
  Feeds `blocked-on-fact` (closed blocked), `handed-off` (handoff records), and the session half of `done`.
- Transition log and take-stock: lifecycle events between sessions plus the sweep that turns them into board facts.
  Went-stale and died-mid-work cards ride this.
- Lint and template roll: 11 deferred decisions from the plan's 26-decision table land here -
  D5, D9, D12-D18, D20 (take-stock script), D25 (rubix-autorun drift fix), D26.
  Includes template-version 6 to 7 and the lifecycle-lint-since adoption date.
- Then propagation to the other conforming repos, and the full sys-prompts-cc handoff case (the brief's original proof-of-value, parked for exactly this slice).

Requirements already live in the parent briefs:
`docs/briefs/2026-08-30.I52.resume-board-brief-rubix.md` (seam 1 definition) and
`docs/briefs/2026-08-31.I52.board-view-tech-brief.md`.

## Open advisory findings (slipped from the run's /loop-review, none blocking)

- `board.sh` greps the dated spike doc at runtime to set the CSS verdict; belongs in a config key.
- `docs/spikes/` is a new lane absent from `config/conventions.md`'s import-sweep exclusion list.
- `_health.md` omits conforming repos whose tracker succeeds with zero cards.
- Resume `cd` paths reconstruct as `$HOME/<repo_key>`; wrong if a non-`$HOME` scan root is ever added.
- Board.md renders empty in Obsidian reading mode (base embeds do not mount there); live preview only.
- `done` lane population needs a bounded closed-issue lookback (named post-MVP pivot).

## Resume prompt for a fresh session

```
Seam 1 of #52: the recording convention.
Read docs/handoffs/2026-09-07.I52.board-mvp-to-seam1-handoff.md first.
Then the seam-1 requirements in docs/briefs/2026-08-30.I52.resume-board-brief-rubix.md and the deferred-decision
table (verdict "defer") in docs/briefs/2026-09-02.I52.obsidian-board-mvp-brief.md.
First artifact: a seam-1 brief (bounding, not invention - the parents carry the content), then /loop-plan,
then /loop-drive. The board MVP is merged and live; do not re-verify it.
```

## Transitions

- 2026-09-08T14:18:34Z action 0 executed - seam 1 shipped
