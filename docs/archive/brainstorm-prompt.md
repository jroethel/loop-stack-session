Captured, committed, pushed (93df34b). The reframing now lives in open question 4, dated and marked agreed, with the explicit instruction to use it in the
  upstream PR.

  When you're back, resume with:

  cd ~/create/loops/loop-stack-session && claude --model claude-fable-5
  sed -n '/^\/loop-brainstorm/,$p' model-routing-brainstorm-prompt.md | pbcopy

  then paste. Everything's on origin/main, so the work PC gets it all with the stash-and-pull too.
